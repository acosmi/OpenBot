# CLAUDE.md

OpenBot 全量 Rust 重写 —— 仓库级 AI 协作指引，入仓**首读这一份**。本仓 **public**。

> 真源 = `docs/2026-08-21-OpenBot全量Rust重写终版研究与实施方案.md`（**v4** = v3 就地修订至 §28.1 R200，2026-09-05；架构 / 能力 / 旅程）+ `docs/2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md`（**v2**；GUI 视觉 / token / 布局 / 主题 / i18n / a11y / 视觉闸门）。本文件只摘约束与理由，细节一律以两份方案章节为准；两者冲突时以方案为准（视觉归设计系统文档、架构归后端方案），并同 PR 修订本文件。`docs/2026-08-28-OpenBot-TauriGUI-ElectronChromium-GrokBot大面积Rust迁移-v4修订计划-用户裁决版.md` 已被 R115–R125 吸收，只作历史记录，不是实施依据；真源五层优先级见后端方案 §28.5。

---

## 1. 真源与现状

- **唯一实施真源**是上述方案 v4（§28 为修订记录；**R115–R125 是 2026-08-28 架构裁决，R126–R129 是 2026-08-29 macOS/Windows/runsc/Xvfb P1 实施裁决，R130–R132 是 approval realtime/PG/session浏览器，R133–R181 是 Admin/Audit/People/Boundaries/Identity Providers/Home/Sign-in/Root-App Layout/Direct Bot/Components治理、用户Agent lifecycle、Desktop transport、Agent正式Web journey、channel/thread unary、structured-event host bridge/actual commands/WASM host selection/aggregate budget/native lifecycle/Desktop Local authority+PG provisioning+instance-bound bootstrap+source/bundle/start-lock+SCRAM OS key store+process lifecycle+fixed business database/bootstrap composition+Vault master key+共享ApplicationService assembly+Tauri background/Agent/window-last assembly+run-wide token/cost-upper-bound/user-cap accounting、并发排队取消复核、并发tool runtime budget、AG-UI error纵向、reasoning终态保留期、error台账身份纠正、bounded untrusted projections、tool-result production projection、interrupt/resume typed+durable vertical与MCP admin/private-egress/skills/grants/for-Agent backend、legacy browser call退役、private OAuth egress lifecycle、admin-delete vendor compensation/runbook、OpenAI官方recorded trace、RMCP run protocol cancel及其2026-07-28 wire纠偏、browser runtime LRU/idle selector与Rust residency owner——实施前必读**）。两份输入文档仓内不存在，只登记了 SHA-256；在 Phase 0 把原件归档到 `docs/inputs/` 之前，**不得**以"输入文档里写过"作为依据（§1.1）。
- **R182 也是实施前必读**：它只关闭Golden RGBA/PNG比较与manifest/diff gate，明确把`ready=false`结构绿和formal visual verify分开；245张baseline、固定Linux/CJK、CDP/xcap、AX/键盘/reduced-motion仍todo。
- **R183 固定CDP输入pure-plan边界**：具名键码是含空格的17项，未知多UTF-16单元键preserve为0；pure plan不等于authenticated engine/live CDP，T-BROP-0037–0044与P2仍todo。
- **R184覆盖R126的当前engine wire版本**：protocol/release epoch现为2/2，七种ordinary input已在macOS双真实role闭合；secret typed effect、Page.startScreencast/ScreenHub、production assembly与Windows/runsc runtime仍todo。
- **R185再覆盖当前frame wire**：protocol/release epoch现为3/3，OBFRAME2/76-byte metadata、Page start/stop/ACK与Rust size-one latest buffer已落；viewer/WS、多viewer、fps/latency/fallback与跨平台仍todo。
- **R186固定ScreenHub核心与production transport分界**：唯一Engine source、共享latest、多viewer sequence-gap、128-bit/30秒/hash-only/single-use且actor/generation/origin/window绑定的ticket核心已落；Server/Desktop真实WS、production auth失效hook、连接限额/2秒停流、fps/p95/fallback/assembly与跨平台仍todo，T-BROP-0027不勾。
- **R187固定viewer coordinate单位与production input分界**：官方DevTools证明CDP mouse/wheel只除screen zoom，pageScale只用于document hit-test后加scroll；Rust contain mapper、黑边拒绝、DPR/pageScale pure矩阵与macOS双role drag/IME/wheel journey已落。Leptos/WS/stale-frame、非1 scale硬件、resize/tab/production/跨平台仍todo。
- **R188固定Server Screen transport与完整path-stream分界**：typed ApplicationService发票、same-origin POST、query-free exact双protocol/base-only binary WS及真实Engine纵切已落；production manager source、PG cookie/TLS、Desktop、input/限流/停流仍todo，T-BROP-0027不勾。
- **R189固定Server Screen预算与Engine停流分界**：8MiB/s/单帧burst、control20/10条每秒、10秒Ping/30秒idle、1秒write/100ms close与permit释放已落；真实macOS Engine按默认计时通过。旧文/fixture误写16MiB已按实际8MiB单源纠正。production source/auth hook、Desktop、2秒Engine停流及fps/paint仍todo；全范围顺序见`docs/2026-09-04-v4全范围闭合工作单.md`，不以单批完成结束总目标。
- **R190固定Screen需求生命周期**：protocol/epoch4，shim595/600；唯一Rust owner按active viewer自动pause/resume且保留页面，排队input执行时重验ControlService，旧registration不可清理替代源。macOS双role与Server WS→自动pause已实跑；production ComputerManager/auth hook、Desktop、性能与Windows/runsc仍todo，不关闭G7。
- **R191固定开源共建与受控Alpha方向**：首版工作台、AI工具、浏览器/原生电脑三类均需真实闭环；自有项目只允许固定提交复制处理、不改源仓，macOS/Windows均有已合并实现但移植后分别真验。开源先审范围/来源/许可，根LICENSE仍待实际切换；Alpha不等于v4 G0–G8/DoD全闭合。优先级见开源共建研究方案。
- **R192固定AG-UI官方fixture与全关分界**：T-FIX-0010经官方SDK0.0.57源文件blob/bytes/SHA、33类型三向集合与39行/三scenario回放闭合；外部候选的多package.json、报告漂移和归属/验签表述已修正。fixture5/Agent57绿，fixtures现35/20/55；不是vendor recorded或PG/UI/网络证明，G4/G6/G8仍红。
- **R193固定Plugins管理页面与完整GUI的分界**：三条Server Web路由、Rust认证三态、custom/OAuth/grant与串行写入/unknown读回已落；PG43、UI187、Contracts105、Server233及中英release浏览器绿。production PG/session→GUI整体权限、Bearer完整产品面、Desktop transport/Local OAuth/宿主、Skills与golden/AX仍缺，三route和G6不勾。见Batch117。
- **R194补齐Desktop Plugins基础framing**：connections读取与curated启用改走既有typed command，host actor/fresh/admin、closed共享DTO/查询拒绝与body限额已测；Desktop112、Contracts105、Server233绿。Windows只cross-check，真实Wry/GUI/Local OAuth与三route/G6仍todo；overlay现1271/433/2/6，parity与fixtures不变。
- **R195纠正GUI密钥输入所有权**：Plugins OAuth/OIDC/Agent Authorization原RwSignal明文已改DOM-owned SecretInput；普通Input不再允许Password，响应式状态仅校验事实/代次。提交清空、失败不回填、DTO/Serde/临时JSON零化且await前释放已测；旧视觉遮挡不能作所有权证明。Contracts108/UI188/Server233/Desktop112与release三表单绿；不承诺浏览器底层副本物理擦除，不关闭G2/G6。见Batch119。
- **R196落地凭据管理工作链**：四API/三audit/五原测试与credentials route在shared PG/Vault/Server/Desktop路径闭合；manual只model/connector/mcp，受管OAuth撤销原snapshot不重绑定，local/vendor状态分开。PG18、Server235/Desktop113、Contracts109/Domain372/Application166/UI188和真实PG/session GUI通过；parity886/826/1712、fixtures35/20/55、overlay1234/470/2/6。Windows仍仅cross-check，G2/G6/G8与Alpha不勾；详见Batch120。
- **R197验收provider外部交付**：Anthropic两份官方body独立核来源/字节/许可，production replay3/0/0、provider units32/0/0、Infra Clippy/fmt/guard通过，T-FIX-0056后fixtures36/20/56。recorded现OpenAI+Anthropic2/3；Google所审bodySegments非原始SSE，接受调查但保持实现缺口。strict160/0/0，parity/overlay不变；E–K分范围预留，I/K无需联网调研，均未预记启动。无live/平台/完整G4或Alpha通过，详见Batch121。
- **R198落地Rust出口网关库**：unique loopback/token、SafeDialer私有hop、HTTP/CONNECT/WS和预算/撤销join在macOS15项实跑通过；IPv6数值地址改typed host，原dialer14/provider32/recorded4回归。fixtures37/20/57、strict160/0/0；Linux仅check，Windows server-runtime卡MSVC C头文件未通过。production ComputerManager/tenant policy/Engine/bootstrap/namespace/kernel和TLS内层策略仍缺，Engine仍epoch4黑洞网络，不勾G5/G7/Alpha；详见Batch122。
- **R199修复Engine父环境继承**：Unix从黑名单改env_clear+五键/cwd，Windows只读OS目录并构造固定九键UTF-16 block、不再枚举父环境。真实macOS两role main/renderer四canary由全true变全false，原完整输入/帧/cleanup通过；Computer67/fixture5、Windows portable4与cross-check/Clippy绿。fixtures38/20/58；Windows/runsc真机和更广filesystem/helper-exec/kernel仍缺，完整G5/G7/Alpha不勾，详见Batch123。
- **R200收紧macOS main直接读取**：新SBPL只放scope/bundle/具名OS内容与ancestor metadata，dyld只补literal /锚点；旧3条跨scope读取实修为5负向拒绝、scope read/list/link正向成立，另有真实两role完整兼容。F0059后fixtures39/20/59；helper exec/Mach/FD/UDS/localhost、Windows/runsc、production与完整G5E/Alpha仍缺，详见Batch124。
- 阶段进度：**Phase 0（Evidence Freeze）产物已落地** —— `parity/*.yaml`（9 份；条目数随实施推进增长，真源是各台账自己的 recount，由 `cargo xtask recount` 逐条实跑，**不在本文件钉死**）、`provenance/sources.spdx.json`、`fixtures/**`、`tools/pins.toml`、十个业务/parity 核心 crate 骨架（R127 后另有唯一 Win32 安全边界 crate）、`cargo xtask parity-check`（§19.3）。CI 必须拒绝未归类项与没有证据的 `done`。
  **G0 仍有一项未闭合**：§1.1 要求把两份输入文档原件归档到 `docs/inputs/`，仓内与本机都不存在原件，只有 SHA-256 —— 在补齐之前不得宣称 G0 通过。
- **G1（Rust Core 与 PostgreSQL）四条判据本轮全部达成**（§24，四条缺一不可）：① 十个业务/parity 核心 crate + R127 唯一 Windows 安全边界 crate 的 locked build 绿（业务入口与 parity owner 仍只有原十个）；② 同一个 `Arc<dyn ApplicationService>` 经 Axum 与 in-process 两条 transport 结果一致（当前 `cargo test -p openbot-testkit --test transport_parity` = 8 passed，Batch30新增channel detail与port scope逐字段对拍）；③ 28 表 / 13 migration 映射对走完 13 条 migration 的真参照库逐字段相等，read checksum 168/168 行逐字节相同；④ tracing span + 关联字段 + 脱敏 + Prometheus metrics 从首个 vertical slice 生效。
  **W-4 已关掉四项旧缺口**：production PostgreSQL/单用户 `AuthResolver`、独立 `main.rs`、`/metrics` session 访问控制、MatchedPath route span/metrics 均已落地；people/audit/policy 命令另有同一 Arc 的 Axum/in-process 对拍；非 loopback 明文 HTTP 同时打启动告警并在 readiness 投影 `insecure_transport:true`。W-5 batch 7 已补 production Tenant Package loader/sync。G3 batch 1–5 依次落 native DB、thread/live、history/memory、run runtime/WS 与 importer。G3/G4/G5/G6 batch 6–51 已把 pure Rust Agent、三provider、RMCP/OAuth/Drive/approval、GUI地基/Tauri/27primitive/46图标/14业务组件、session sign-out、channel activity/detail/shared thread、ChannelRow、Agent roster/detail、channel create/routing/native first turn、`/channel/new`、Composer draft/queue纯状态、channel snapshot/SSE/plain transcript/idle send、durable跨副本Stop+production mount-local queue、actor-scoped Memory Controls/GUI、Settings Preferences稳定队列、完整Settings 200px secondary sidebar、actor Connected Accounts、Components Gallery治理读面/Quote+Cards+Charts+Activity十一ordinary renderer、compiled component `for-agent`/call-time decision、policy/ACL/audit受控的Activity data registry/`/call`、ordinary 11项production provider registration/closed args/durable conversation projection/safe runtime mount、Activity两种report的Agent-bound follow-up ask与exact retry、Decisions durable控制面、Agent `AwaitingHuman`暂停恢复、13项provider manifest与Approval/Choice两个pending/complete renderer、sandboxed component草稿/发布/revision/sample原子治理、dynamic sandbox per-Agent provider/call-time authorize/Web runner/production conversation/共享RefusedCard/Admin Playground，以及G5 HumanLease control状态机、authority epoch fencing与closed BrowserInput协议。**以下仍未闭合，不得算进去**：read checksum/Drizzle 账本深校验；三家 recorded/live vendor trace、acting Approval完整thread/cancel/computer集成、完整run-wide budget；Desktop Local OAuth、MCP private egress/admin UI、完整AG-UI事件；browser/file/shell及各自协议级cancel；actual customer export/legacy production drills（固定上游只有known-thread读取、无合法枚举/event/memory export面，等待经许可API/数据）；G5前多用户readiness；package removed；AppSidebar剩余skills/完整admin destinations，完整跨页面/channel Composer（sources/附件/per-channel draft/steer/markdown/tool boundary/Screen）与Agent Desktop structured realtime/native-window journey、external identity/Tauri binary/window lifecycle、其余route/component/brand/runtime/golden/a11y。Batch51 targeted：`openbot-computer` **8/0/0**，all-target/all-feature Clippy `-D warnings`、fmt、parity与recount均绿；Cargo.lock新package0。browser-operations **7/39/46**、API/components/UI **66/13/87 done**、parity **693/993/1686**、fixtures **17/22/39**、strict **158/0/0**。无UI变化，沿用Batch50 bundle **1400622/97848/740216/1/0** 与CSS余456B；不冒充新browser/golden。Electron/Chromium进程、authenticated engine framing、CDP/input执行、ScreenHub/viewer ticket与Desktop独立renderer仍未完成；Batch50的sandbox args/JS/network/callback/channel正向证据同样未完成。没有运行`cargo xtask ci`。fixture/native latest仍为 **0023**，见 R42/R43/R49–R114。
- **上段 Batch51 数字是保留的历史快照，不是当前口径**：Batch106/R181 后 native latest=`0029`，schema=`47表/478列/342 NOT NULL/269约束/97索引`，fixtures=`26/22/48`，parity=`861/849/1710`，tests=`470/577/1047`，API=`94/80/174`，events=`48/44/92`，overlay=`1286/416/2/6`；MCP private-egress/admin page/custom/delete/refresh、skills/grants/for-Agent、custom private user-OAuth egress lifecycle、admin-delete vendor compensation/runbook及run→RMCP list/call protocol cancel均已闭合，无调用者legacy browser call已退役，真实工具只走typed Agent run管线。RMCP现代2026-07-28以关闭exact response stream取消且notification POST=0，只有自动降级2025-11-25才发送exact-id `notifications/cancelled`；R179相反的wire叙述已由R180推翻。Browser runtime已闭合13条固定上游LRU/idle selector与manager-owned完整scope/代次/active-lease residency状态机，但production Server/Desktop→EngineProcess/Supervisor配置与CPU/RSS/pids/disk仍缺，不能称完整computer runtime budget。OpenAI官方recorded trace已闭合三家中的`1/3`；Anthropic、Google与live credential仍缺，T-FIX-0013也未被function-only trace替代。fixed upstream strict recount最近证据=`160/0/0`，R181未重跑。完整MCP产品面仍缺四个Plugins/Skills UI route、Desktop Local OAuth、完整官方RMCP conformance，以及G8 typed operator-attestation/最终secret retirement。完整G4仍缺其余两家recorded trace、acting Approval/computer等；computer/file/shell protocol/process-tree cancel与完整computer runtime budget仍缺；reasoning/open projection与interrupt正文只在active run保留，terminal同事务逻辑清除；WAL/backup物理保留仍属G8。
- **Batch107/R182 当前口径覆盖上一条的R181尾部**：native/schema/API/events不变；fixtures=`26/22/48`、parity=`861/849/1710`、tests=`470/577/1047`，overlay因三条真实revalidation变为`1283/419/2/6`。Golden core/gate已闭合，但manifest仍`matrix=245/masks=0/ready=false`，formal verify必须红；245张baseline与capture均未完成。Cargo.lock新增4个testkit-only PNG链包、总829；六target bans/sources绿，cargo-vet仍373 unvetted且未加exemption。非strict recount=`71/0/89 skipped`；没有配置上游，strict未重跑。
- **Batch108/R183 当前口径**：parity/tests/browser/API/events与overlay不变；新增T-FIX-0049后fixtures=`27/22/49`。CDP mapping=`11/0/0`、Computer=`54/0/2 ignored`，但engine wire/live effect/ScreenHub明确false，八条Browser input parity继续todo。Cargo/package/schema/native/UI/workflow不变；strict仍因无上游未跑。
- **Batch109/R184 当前口径**：protocol/release epoch=`2/2`，shim=`483/600 LOC`；T-BROP-0037–0043与T-FIX-0050 done后browser=`14/36/50`、parity=`868/842/1710`、fixtures=`28/22/50`、overlay=`1276/426/2/6`。macOS真实role=`2/0/0`，但T-BROP-0044、正式Screen与production assembly及Windows/runsc runtime继续红；strict仍未跑。
- **Batch110/R185 当前口径**：protocol/release epoch=`3/3`，shim=`596/600 LOC`；T-BROP-0030–0032与T-FIX-0025 done后browser=`17/33/50`、parity=`871/839/1710`、fixtures=`29/21/50`、overlay=`1273/429/2/6`。正式screencast/ACK/单consumer latest-buffer已落，完整G7仍缺viewer/WS、多viewer/fps/latency/fallback/production与跨平台；strict仍未跑。
- **Batch111/R186 当前口径**：protocol/epoch/shim与browser/parity/overlay不变；新增T-FIX-0051后fixtures=`30/21/51`。Engine-backed ScreenHub、双viewer共享latest、viewer gap、ticket authority与generation invalidation核心在macOS双真实role闭合；真实conformance=`5/0/0`。Server/Desktop WS/production hook/连接限额/2秒停流/fps/p95/fallback/assembly与Windows/runsc仍红；strict仍未跑。
- **Batch112/R187 当前口径**：browser/parity/overlay仍不变；新增T-FIX-0052后fixtures=`31/21/52`。coordinate pure=`4/0/0`、Computer lib/fixture=`64+4/0/2 ignored`、真实conformance=`6/0/0`；macOS双role mapped button/down-move-up/IME/wheel绿。production与非1 scale硬件仍红；strict仍未跑。
- **Batch113/R188 当前口径**：新增T-API-0175/0176与T-FIX-0053后API=`96/80/176`、parity=`873/839/1712`、fixtures=`32/21/53`、overlay=`1275/429/2/6`。Server screen=`4/0/0`、真实Engine→WSS=`1/0/0`；完整T-BROP-0027/G7仍红，strict未跑。
- **Batch114/R189 当前口径**：parity/API/browser不变；fixtures=`33/21/54`、overlay=`1273/431/2/6`；Server=`233/0/0`、Screen=`11/0/0`、真实Engine默认heartbeat/idle=`1/0/0`。固定上游已恢复，strict=`160/0/0`。只关闭Server transport budget，T-BROP-0027/G7与其它总目标余项不勾；三份外部任务只作待派发预留。
- **Batch115/R190 当前口径**：parity/API/browser不变，fixtures=`34/21/55`，overlay仍`1273/431/2/6`；Computer lib67+fixture5、Server233、Contracts105、xtask103均绿，三条Engine host case与Server WS1均通过。完整G7与总目标保持未完成。
- **P0-code Batch52 已完成（2026-08-28）**：R124 exception-only overlay 已由 `parity-check` join 台账、按 branch diff 强制 done target revalidate；`HumanLeaseEpoch::next` 改 checked，耗尽后清 lease、输入/acting fail-closed，只有 `ComputerGeneration` 前进才恢复；`grok-inventory --check` 同步钉死 tree 的 **2,110** 文件；`engine fetch|verify` 在 macOS arm64 对官方 zip实得 **122,102,881 B / ee939d… / v43.3.0**；browser/components 新增 6 条 Engine T-ID。Batch52 退出时 parity **693/999/1692**、`openbot-computer` **9/0/0**；详见 `docs/2026-08-28-P0-code-batch52.md`。P1 状态以紧随其后的 Batch53 为准。
- **P1 Batch53 macOS Engine 基线已闭合，但 P1 整阶段仍未通过（2026-08-29）**：clean-room shim 恰 3 文件 / **404 非空 LOC** / 唯一零依赖 `package.json`；contracts descriptor → generated protocol/hash 双向一致；Rust-only bundle 已真组装 ASAR（官方 Pickle + 4 MiB block integrity）、九 fuse `000011001`、外层 app/executable/bundle ID rebrand、`ElectronAsarIntegrity`、release epoch 1、ad-hoc signature 与 sidecar manifest digest-before-spawn。`openbot-computer` 已落双 role scope digest、stdin 4 KiB boot token、双 UDS peer PID+live child、hello/ready deadline、独立 binary JPEG frame、malformed/stale/scope/generation/sequence 拒绝、macOS SBPL（main executable 精确继承 profile；只有四个 Electron Helper 与 crashpad 五个精确 literal 可用 `with no-sandbox` 脱离父 profile，再由 Chromium 自沙箱接管）。真 Electron Browser/Component 两 role 各自 start→1280×800 frame→stop→shutdown 通过；`ProcessMetric.sandboxed=true` + creationTime 证明 renderer OS sandbox，主/全部后代 TCP LISTEN=0、退出后全部 PID 与 profile lock=0。当前单元 `openbot-contracts` **88/0/0**、`openbot-computer` **18/0/0（另 2 条 host conformance 显式 ignored，单独实跑 2/0/0）**、xtask **90/0/0**；overlay 当前 **1674/16/2/0**，parity 仍 **693/999/1692**。**不得宣称 P1 绿**：Windows Named Pipe peer credential/Job Object/restricted token spike 与 Ubuntu 24.04 x86_64 + runsc/Chromium layer-1+2 spike 尚无本机证据；未进入 P2。详见 `docs/2026-08-29-P1-engine-macos-batch53.md`。
- **P1 Batch54 Windows 可执行探针已落，但 Windows 真机与 P1 整阶段仍红（2026-08-29）**：新增第 11 个 `openbot-windows-sandbox`，它按 §5.1 独立安全边界/feature graph 例外成为唯一允许 Win32 unsafe 的 crate，核心十 crate 继续 `unsafe_code=deny`。Engine host 在 Windows 使用 current-user+Restricted-Code/low-label 双 Named Pipe并逐条校 PID+100ns creation FILETIME；child 以 `DISABLE_MAX_PRIVILEGE|LUA_TOKEN|WRITE_RESTRICTED`、Restricted Code SID且保持 medium integrity的 token suspended 启动，经 handle allowlist 原子进入 32-process/4-GiB/kill-on-close Job 后 resume；profile/temp ACL、main Electron creationTime exact、renderer 同 Job 均 fail-closed。`engine bundle` 已实现 fixture exe rename 与官方 JSON 的 PE `Integrity/ElectronAsar` transaction write/read。macOS 本轮实跑 bundle/verify 与双 role `2/0/0` 回归；Windows target check/Clippy 三面绿。**没有 Windows 机器实际运行三条 boundary test、bundle 或双 role conformance，故不称 Windows spike 完成**；runsc可执行闭包随后由Batch55补上，但runtime/pin同样仍红。详见 `docs/2026-08-29-P1-windows-boundary-batch54.md`。
- **P1 Batch55 runsc 证据闭包已可执行，但没有 Linux runtime，P1 仍红（2026-08-29）**：新增非产品Cargo example `engine-runsc-probe` 与 `cargo xtask engine runsc-spike`。外层只接受校完整release tarball SHA/exact version/sidecar目录的root Ubuntu24.04 x86_64 OCI，强制 systrap、network/host-UDS/FIFO none、sidecar release ALWAYS + usage STRICT、gVisor marker、readonly root/bind、零capability、64 pid/6GiB；R129另允许且只允许runsc内部Xvfb作为Electron本地display（fixed`:99`/1280×800×24/`-nolisten tcp`，不是VNC），binary hash与dpkg version也必须随PASS产证据。内层真实跑Browser/Component两role，Linux不接受Electron自报sandbox，逐renderer要求main `Seccomp!=2`负对照、renderer `Seccomp:2`/`NoNewPrivs:1`与PID+network或user namespace layer-1，再验JPEG/listener/orphan/lock。只有全绿才打印pin candidate且不自动改pins。macOS本轮只能取得xtask `93/0/0`、Linux target probe/xtask Clippy与macOS双role `2/0/0`回归；**runsc/Xvfb未运行、版本仍未钉**。详见 `docs/2026-08-29-P1-runsc-harness-batch55.md`。
- **G4 Batch56 已关闭 approval critical realtime 子项，但 G4/G6 整关仍红（2026-08-29）**：closed `ToolApprovalActivityEvent`只含100-row bounded `pendingCount`；ApplicationService subscription与PG coordinator按current actor整页重读，同进程Notify/1秒durable poll跨replica唤醒，每轮重验AuthGeneration/role/revoke，失效发stable error后EOF且旧auth新handshake拒绝。Axum exact `openbot.tool-approvals.v1` + trusted Origin + read-only1008；Desktop归Critical。Leptos以hint→权威GET替代1秒poll，连接/重连前GET、30秒fallback、checked epoch拒stale response，并把decision/refresh固定到Page owner，真实浏览器捕获并修复“卡片卸载取消成功status”的生命周期缺陷。PG17.11 SCRAM=`1/0/0`，approval Server=`4/0/0`，六crate核心unit=`88/150/307/213/133/80`；release浏览器subscription1、card1→0、status保留、console0。parity=`695/999/1694`、overlay=`1600/92/2/0`、strict=`159/0/0`，bundle=`1384716/97848/740216/1/0`。**真实PG浏览器同一竖切与完整thread集成仍未闭合**；未运行CI/Actions。详见 `docs/2026-08-29-G4-approval-critical-realtime-batch56.md`。
- **G4 Batch57 已关闭 approval真实PG浏览器竖切，但不关闭完整thread/G4/G6（2026-08-29）**：既有`required-features=testkit` UI host新增可选PG模式，且构造性只接受`127.0.0.1`与`openbot_ui_approval_fixture_`前缀专用库；默认memory不连库、PG proof API 404。最终独立PG17.11 host-SCRAM专用库施加正式baseline/native并接production coordinator/waiter；release浏览器先见真实pending权威target/`[redacted]`，click后proof精确为`granted/pending/summariesCleared/requestedAudits/grantedAudits/waiter=1/0/1/1/1/granted`，card0、status保留、console0、hard reload card0；server明确打印waiter granted，实例与临时文件全清。implementation=`2cecfeab7e225cb0ad3bd4ae134c26193aa0c30b`；无新产品API/T-ID，parity仍`695/999/1694`。auth仍是testkit fixed identity，production session-cookie浏览器与完整thread/cancel/computer旅程仍todo；详见 `docs/2026-08-29-G4-approval-real-pg-browser-batch57.md`。
- **G2/G4 Batch58 已关闭 approval production session-cookie resolver浏览器子项，但不冒充登录协议（2026-08-29）**：PG fixture seed用production `SessionTokenHash`只存`sh1_` keyed hash并改用真实`PostgresSessionAuthResolver`；仅testkit PG模式挂无输入303，设置host-only HttpOnly/Lax/no-store cookie。最终PG17.11 SCRAM实得无cookieapproval401/bootstrap303；浏览器bootstrap前card0+load error，后card1，grant proof保持R131并新增`sessions/hashedSessions=1/1`，status/console/hard reload全绿。default memory的PG proof/session API均404。implementation=`1fdc65c4261c9dfa4aaf4f5df8ce58b94a24630a`，unit=`2/0/0`，无产品API/T-ID。真实OIDC/SAML登录与完整thread/cancel/computer旅程仍todo；详见 `docs/2026-08-29-G4-approval-session-cookie-browser-batch58.md`。
- **G6 Batch59 已接Admin shell/Home/Audit三条route，但完整AdminSidebar/golden仍todo（2026-08-29）**：global AppSidebar只按生产admin status probe显示Admin入口；AdminShell在gate通过前不构造child，200px secondary nav只列Overview/Audit/Playground三个真实destination。Audit helper固定50条keyset、字段/唯一ID/64KiB object payload边界；release浏览器Home/global link、Audit50→52/payload/hard reload、Playground同shell、main1/nav2/current1/overflow0/console0。真实PG session原子降为user并同步generation后global link0，`/admin`与`/admin/audit`统一NotFound、nav0/child0。implementation=`f071fc807a8a886a97d8cc45670de7e9af6fe097`；UI=`136/0/0`、Server=`213/0/0`、bundle=`1439871/99181/740216/1/0`。只关闭T-ROUTE-0004/0012/0013；T-UI-0028与T-UI-0132/0139保持todo。routes=`11/21/32`、总parity=`698/996/1694`、overlay=`1597/95/2/0`；详见 `docs/2026-08-29-G6-admin-shell-audit-batch59.md`。
- **G6 Batch60 已接真实 People 管理route，但完整AdminSidebar/golden仍todo（2026-08-29）**：共享contracts只让role/access body携`role`/`revoked`；UI固定50条keyset、250ms server-side search、页内/跨页ID唯一、checked epoch与PeoplePage稳定owner。release memory fixture实得50→52、首屏外搜索唯一命中、self/configured-admin禁改、role与remove/restore双向、hard reload、中英、600/1280 overflow0；浏览器还真实暴露并修复worker-owned row signal分页后dispose的WASM `unreachable`。PG17.11 SCRAM+production keyed session/PostgresPeopleAdministration浏览器双向后DB精确`generation/role/revoked/targetSessions/roleAudits/revoke/restore/actorHashedSession=3/user/0/0/2/1/1/1`；当前actor降user并同步generation后新tab仅status403×2、People API请求0、global/admin nav/child0。implementation=`aeb38f90b43b1796fd961fc02a3a10cb1b71f359`；UI=`139/0/0`、Server lib=`213/0/0`、fixture=`3/0/0`、bundle=`1498159/100390/740216/1/0`。只关闭T-ROUTE-0020；T-UI-0028/T-UI-0140保持todo。routes=`12/20/32`、总parity=`699/995/1694`、overlay=`1596/96/2/0`；详见 `docs/2026-08-29-G6-admin-people-batch60.md`。
- **G2/G6 Batch61 已接真实 Boundaries 管理route，但完整AdminSidebar/golden仍todo（2026-08-29）**：`ActionPolicyResponse`保留`policy:null`/default-deny，CEL 4096-byte上限由contracts/domain/UI单源。首次向导无默认选择，admin显式选严格`allow=[]`或固定上游兼容`allow=["true"]`；之后可切enforce/dry-run、顺序deny preset/自定义/移除与两基线，自定义allow只读保留。release memory浏览器两preset、Enter、重复拒绝、baseline双向、hard reload、中英、600/1280全绿，并真实发现/修复成功PUT后stale draft alert。PG17.11 SCRAM+production keyed session/PolicyStore预置custom allow，页面覆盖按钮0；add password deny+dry-run后DB精确`mode/deny/allow/updatedBy/rows=dry-run/[password]/[actor custom]/fixture-actor/1`，reload保持；降当前admin后status403×2、policy GET/PUT0、child0。domain upstream/CEL/Application/Server/transport=`28/6/1/4/1`全绿。implementation=`ae8e9c73f46b17d97c54380a29931feb612dd5ca`；UI=`143/0/0`、Server lib=`213/0/0`、fixture=`4/0/0`、bundle=`1527597/103671/740216/1/0`。只关闭T-ROUTE-0014；T-UI-0028/0133保持todo。routes=`13/19/32`、总parity=`700/994/1694`、overlay=`1595/97/2/0`；详见 `docs/2026-08-29-G6-admin-boundaries-batch61.md`。
- **G2/G6 Batch62 已接真实 Identity Providers 管理route，但完整AdminSidebar/golden仍todo（2026-08-29）**：公开 list/delete DTO 上收到 wasm-safe contracts，类型构造性装不下 client secret、SAML metadata/certificate；浏览器注册体 serialize-only，infra secret 输入继续不可 Clone/Serialize。SAML/OIDC exclusive body、provider/domain/issuer/URL/metadata/client 上限与形态在请求前闭合，OIDC discovery 只能由 issuer 派生；成功写入后权威 refetch。release 浏览器空提交5字段拒绝，真实PG17.11 SCRAM+production keyed session/DynamicSsoService登记SAML后得`providers/saml/v2/plaintext/registerAudit=1/1/1/0/1`，hard reload、英中、OIDC password secret与取消清除均绿；删除后`providers/removeAudit=0/1`。降actor为user并同步generation后统一NotFound且IdP HTTP探针保持`list/register/remove=1/0/0`。Browser viewport控制层未应用600 override，未冒充已跑；1280 overflow0。implementation=`a90c0891b222822c403fbdbb8f3de11407abf139`；contracts/UI/Server/fixture/infra SSO=`90/147/213/5/4`，bundle=`1585631/105118/740216/1/0`。只关闭T-ROUTE-0019；T-UI-0028/0138保持todo。routes=`14/18/32`、总parity=`701/993/1694`、overlay=`1594/98/2/0`；详见 `docs/2026-08-29-G6-admin-identity-providers-batch62.md`。
- **G3/G6 Batch63 已接真实 Home Composer route，但跨页面完整Composer/golden仍todo（2026-08-29）**：根`/`不再临时指向Approvals；Home严格取`!mine&&public` Explore与`explore[0]??agents[0]` fallback。Textarea opt-in形成ARIA combobox，`@`候选只由Server roster产生，选择保留structured Agent ID并移除旧mention；显式选择零模型且route audit失败也不推翻用户选择，无mention走production routing，transport/audit失败回固定fallback。Home与`/channel/new`共用create→native BeginRun executor，未知create禁止重发、begin失败复用channel/run。release浏览器实得explicit/inferred/fallback/explicit-audit-failure四分支、三条主旅程channel count52→55、真实thread/首消息/导航、reload/Explore零create、中英、600/1280 overflow0；PG17.11 create/begin=`2/0/0`、routing/audit=`1/0/0`。implementation=`07bff3a2b0ab010f2d4f4d78702a61d03aa3d1d5`；contracts/domain routing/application routing/UI/Server/fixture=`90/12/6/152/213/6`，bundle=`1605538/108118/740216/1/0`。只关闭T-ROUTE-0006；T-ROUTE-0010、T-UI-0020/0030按diff重验，T-UI-0043/0130保持todo。routes=`15/17/32`、总parity=`702/992/1694`、overlay=`1579/113/2/0`；详见 `docs/2026-08-29-G6-home-composer-batch63.md`。
- **G2/G6 Batch64 已接真实 Sign-in 与 authenticated guard，但官方品牌SVG/live OIDC/golden仍todo（2026-08-29）**：`AuthenticatedBoundary`只把`/api/me`401解释为未登录，其他失败显式可重试且child零构造；偏好/Sidebar/runtime全部移入边界。capabilities把环境provider闭成Google/Microsoft/Okta且动态SSO只投影bool；`/sign`支持三按钮、email route ticket→continue、provider点名错误，已登录exact `/sign`回`/`；Input email自动autocomplete且IME-safe Enter。Desktop custom protocol新增host-bound typed `/api/me`。PG17.11 release浏览器证明guard前protected增量0、sessions/hashedSessions=1/1、authenticated mount/reverse redirect；最终干净计数`4/2/1/1/5/0`。implementation=`5c51626d2f588e2b52b5adcfbed99f05f1743e5a`；contracts/UI/Server/fixture/Desktop=`92/155/213/7/80`，bundle=`1659274/109378/740216/1/0`。关闭T-ROUTE-0002/0031与五条auth测试；Okta官方press kit无Okta SVG，故T-UI-0039不伪关；T-TEST-0167/0170与formal golden保持todo。routes=`17/15/32`、tests=`377/670/1047`、总parity=`709/985/1694`、overlay=`1570/122/2/0`；详见 `docs/2026-08-29-G2-G6-sign-in-guard-batch64.md`。
- **G6 Batch65 已闭合 Root/App 两条 pathless layout，但 AppSidebar总项/golden/Tauri window仍todo（2026-08-29）**：新增具名RootLayout在auth外唯一包sign与全部route，host首帧theme/locale与per-Tooltip compound provider承接上游root provider；新增具名AppLayout只在auth成功后构造并拥有唯一preference/SidebarProvider/AppSidebar/topbar/main。CSS固定root full-height、app one-viewport、main inner scroll。release浏览器1280 sign实得root/app/main/nav=`1/0/1/0`且root/body800/800；auth app1280=`1/1/1/1`且app/main/body=`800/756/800`；最小1024×640=`1/1/1/1`且`640/596/640`，均overflow/log0。implementation=`d614a6213755a47ce4d5399ce0ae4af1b2124447`；UI=`156/0/0`，bundle=`1651268/109435/740216/1/0`。关闭T-ROUTE-0001/0003；routes=`19/13/32`、parity=`711/983/1694`、overlay=`1568/124/2/0`；详见 `docs/2026-08-29-G6-root-app-layout-batch65.md`。
- **G3/G6 Batch66 已闭合 Direct Bot route 与七条 remembered-thread 判据，但 formal Bot golden/AppSidebar总项仍todo（2026-08-29）**：`/bot?agent=`只消费Server roster/detail；无query取deployment-backed roster稳定首行，零硬编码Agent。每Agent namespaced localStorage只收有界UUID；known=true保持、false才fresh、检查失败保持并告警；New chat只在Server mint成功后swap。Channel/Direct共用唯一ConversationSurface并只切typed anchor；fresh identity首run前零snapshot/SSE，begin成功后恢复普通404 fail-closed。release浏览器证明首发、hard reload、New chat、双Agent key隔离/切回恢复，中英、1024×640/600×900全绿；PG17.11 thread begin/conversation/directory=`3+1+1/0/0`。implementation=`794c6ec5f2b42d5deccadae77cd285f54d9947e2`；UI/fixture=`164/8`，bundle=`1683002/109859/740216/1/0`。关闭T-ROUTE-0008/T-TEST-0173–0179；routes=`20/12/32`、tests=`384/663/1047`、parity=`719/975/1694`、overlay=`1560/132/2/0`；T-UI-0127与G6整关不勾；详见 `docs/2026-08-29-G6-direct-bot-chat-batch66.md`。
- **G4/G6 Batch67 已闭合 Admin Components index/detail、Playground route 与 compiled component 治理写面，但 formal golden/Desktop sandbox renderer仍todo（2026-08-29）**：六条same-origin/fresh/admin endpoint经唯一typed ApplicationService写Agent withholding、function grant与draft/publication，成功后只接纳权威receipt；PG SERIALIZABLE/row lock/audit同事务并证明rollback、幂等created_at、publish提升draft与unpublish不清独立grant。R141固定sandboxed draft/publication只走Playground、Components页只允许Agent withholding且function构造性拒绝；浏览器同时发现并修复CSP缺`frame-src 'self'`与Playground只按name keyed导致发布后stale row。release index16卡、compiled全写、sandboxed禁用矩阵、Playground draft→publish/hard reload、中英与1024/600 DOM/overflow/runtime均绿。implementation=`ecfaf09746bbaf131411b9a8d66fb2d1ecb6c1bc`；contracts/application/Agent/infra/Server/fixture/Desktop/UI=`93/151/34/307/214/8/81/166`，PG component catalogue=`2/0/0`；bundle=`1741699/112331/740216/1/0`。关闭T-API-0043–0048、T-ROUTE-0015/0016/0021、T-TEST-0437–0439/0443–0448/0454–0455；API=`73/97/170`、routes=`23/9/32`、tests=`395/652/1047`、parity=`739/955/1694`、overlay=`1540/152/2/0`；详见 `docs/2026-08-29-G6-admin-components-playground-batch67.md`。
- **G4/G6 Batch68 已闭合用户 Agent lifecycle/customer Authorization backend，但正式浏览器/Desktop journey仍todo（2026-08-29）**：七条fresh same-origin HTTP写面经唯一ApplicationService；`AgentAdministrationScope`把tenant/actor/admin/AuthGeneration送入PG事务复核，owner/admin/package竞态、Vault v2 credential、lifecycle与hash-chain audit同事务。package-null remote/managed Agent进入runtime，Authorization每run fresh解封并只经SafeDialer；private owner/admin/non-owner run判据一致，audit只记origin。UI已有create/edit/copy/hide/recover/delete/start和checked stale-response fencing，但Browser URL policy阻断正式journey，故T-ROUTE-0007/T-UI-0031/0032及Desktop custom protocol不勾。implementation=`70da77374b59e3ac89bd6113b06d69e85c6464c8`；PG当前受影响真库=`13/0/0`，bundle=`1792687/113662/740216/1/0`。关闭T-API-0021–0026/0029与62条测试；API=`80/90/170`、tests=`457/590/1047`、parity=`808/886/1694`、overlay=`1450/236/2/6`、strict=`159/0/0`；详见 `docs/2026-08-29-G4-G6-agent-lifecycle-batch68.md`。
- **G4/G6 Batch69 已闭合 Desktop Agent custom-protocol framing，但正式native-window journey仍todo（2026-08-30）**：host-bound window AuthContext承载Agent list/detail与test/create/update/duplicate/hide/unhide/delete；GET无需fresh，七写面/探针在JSON前要求fresh。query/path/body均closed，64KiB Agent body在成功/malformed/oversize三路覆零，response精确200/201/204与Server envelope且secret canary 0。业务仍只经同一InProcessTransport/ApplicationService。implementation=`5351ec57ec60edd2fdf4ec98cc49fe479b82a9ae`；contracts/application/Desktop=`95/153/83`，Clippy、macOS host、Windows target、Tauri guard、parity/recount全绿。没有T-ID变化；callback token、channel create/run、正式窗口/Windows runtime/golden仍todo；详见 `docs/2026-08-30-G4-G6-desktop-agent-lifecycle-batch69.md`。
- **G4/G6 Batch70 已闭合 Agent callback 与正式Web lifecycle journey，但native-window/golden仍todo（2026-08-30）**：Desktop补POST/DELETE callback-token并保持host authority、fresh-before-body、空body闭集与拒绝路覆零；Web callback一次明文只存component-local zeroizing DTO，rotate/Dismiss/unmount/reload/revoke均不进query cache。release Browser闭合create/edit/duplicate/hide/recover/delete/start、cancel password与callback全矩阵，并修复字段纠正后总警报因`get_untracked`不响应的缺陷；cancel后不重填profile `hasAuth=false`，显式重填为true且明文0。implementation=`2311aa8a62ecc77e8ce0a11495b4e372dced3e32`；Contracts/Application/Desktop/UI/fixture=`95/153/83/169/9`，bundle=`1815510/114965/740216/1/0`。关闭T-ROUTE-0007/T-UI-0031–0034；routes=`24/8/32`、UI=`91/61/152`、parity=`813/881/1694`、overlay=`1445/241/2/6`、strict=`159/0/0`。T-UI-0126 formal golden、Desktop native-window/channel、Windows runtime仍todo；详见 `docs/2026-08-30-G4-G6-agent-callback-panel-batch70.md`。
- **G4/G6 Batch71 已闭合 Desktop channel/thread unary framing，但structured realtime/native-window仍todo（2026-08-30）**：custom protocol在generic404前接channel list/detail/create、route与thread mint/status/begin/cancel/conversation九条面，全部只分派既有AppCommand。query/path闭集；用户文本总body上限1MiB且成功/坏JSON/超限/generic fallback原始Vec覆零。Server写面只要求OriginAuthenticated，custom scheme天然同源，ordinary bound window不虚加fresh。首次begin201/replay200、cancel202/terminal200；events/ws继续404。implementation=`502302066c506f8c7373daf14476009197cf1922`；Desktop=`85/0/0`，Clippy、macOS host、Windows target、Tauri guard、parity/recount全绿。无T-ID/依赖/UI变化；未重跑Trunk/Browser/Engine。该批留下的host bridge由Batch72补齐；正式native-window、Windows runtime/golden仍todo；详见 `docs/2026-08-30-G4-G6-desktop-channel-run-batch71.md`。
- **G4/G6 Batch72 已闭合 Desktop structured-event host bridge primitive，但actual native-window仍todo（2026-08-30）**：`DesktopSession`自然EOF不再静默，精确发`upstream_ended` terminal；host close发`subscription_closed`并同步摘route。checked subscription id与injective internal label允许同一实际window并行closed stream；wire只含stream/sequence/gap/typed event/terminal，不含authority，wrong family/thread在IPC前拒绝。`DesktopTauriProtocol`复用bound AuthContext，checked binding nonce在await后复核，unbind关全部登记流；真实`tauri::ipc::Channel` callback与sink消失均有测试。implementation=`c055bcbf749d8d0a37cc6c42cb03d8b2a61bab88`；Desktop default/all-feature=`80/98`且0失败，Clippy、macOS/Windows compile、Tauri guard、parity=`813/881/1694`、overlay=`1445/241/2/6`、strict=`159/0/0`全绿。无T-ID/依赖/UI变化。actual command由Batch73补齐；WASM host选择、正式native-window、单Webview聚合队列预算、Windows runtime/golden仍todo；详见 `docs/2026-08-30-G4-G6-desktop-structured-events-batch72.md`。
- **G4/G6 Batch73 已闭合 Desktop structured-event actual open/close commands，但WASM/native journey仍todo（2026-08-30）**：closed wire、两命令名与open/close receipt单源上收到wasm-safe contracts。Wry `register_tauri_protocol`同时manage同一Arc、注册exact二命令handler与custom scheme；open只取host Webview label，spawn后台Channel pump后立即回host id；close只在同一host label内消费该id，跨window/unknown统一false。stable invoke error不泄露internal label；二项audit allowlist与command attribute/`generate_handler!`机械join。implementation=`61689c4a0169e25eda700bb3d4db668656fa44b8`；Contracts/Desktop default/all-feature=`98/80/102`且0失败，Contracts WASM、Clippy、macOS/Windows compile、Tauri guard、parity/strict/Grok全绿。diff required revalidate19已展开为粗前缀Agent/thread done并以full Contracts复跑，未冒充PG重跑。无T-ID/依赖/UI变化。WASM host selection/callback cleanup、正式window bind/unbind assembly、单Webview聚合256预算与Windows runtime/golden仍todo；详见 `docs/2026-08-30-G4-G6-desktop-event-commands-batch73.md`。
- **G4/G6 Batch74 已闭合 WASM structured transport host selection，但native-window journey仍todo（2026-08-30）**：同一release WASM只在Tauri注入的`window.isTauri=true`时，经exact `__TAURI_INTERNALS__`调用Batch73 open/close；普通Web继续原WebSocket/EventSource。Tauri Channel frame由host发送JSON string，避免任意u64先经JS Number失真；adapter按callback index bounded257重排，要求terminal后exact end，再校receipt/stream/sequence/gap。receipt前frame、invoke reject、integrity fail与Drop-before/after-receipt均最终exact close。AppSidebar、Approval、Conversation三owner已切入，generation变更先Drop旧connection。implementation=`b6520820f71a3cc59b0d134da0d7f95be5e218ae`；Contracts/Desktop/UI=`99/102/175`且0失败，WASM、Clippy、macOS/Windows compile、Tauri guard、双份release bundle逐字相同与普通Web三页回归全绿。parity=`813/881/1694`、overlay=`1445/241/2/6`、strict=`159/0/0`。无T-ID/依赖/视觉变化；正式window/session bind-unbind、单Webview聚合256预算、Windows runtime/golden仍todo；详见 `docs/2026-08-30-G4-G6-wasm-structured-transport-batch74.md`。
- **G4/G6 Batch75 已闭合单Webview structured aggregate budget，但native-window journey仍todo（2026-08-30）**：同一actual Webview的全部internal stream共享256个queued event-ref permit；event入mpsc前原子取permit，`DesktopSession::next_frame`出队即回，raw receiver最迟随frame drop回，send/receiver failure也回收。terminal不占256且各route reserve保持。live+pending subscription同窗上限256，第257条在ApplicationService前拒绝；window close取消全部pending subscribe、关闭registered route，binding cancellation+token防同label重建旧open。implementation=`24d9a5b1e051386b3ae4ed581e5054824da3a18f`；Desktop首次`101/4/0`后最终`106/0/0`，两流400个critical只入队256、第257个显式terminal，256并发pending可被close直接取消，另一window预算独立。Clippy、macOS/Windows compile、Tauri guard、parity/strict/Grok全绿；diff revalidate0，无T-ID/依赖/UI变化。正式binary/window runtime、Windows真机与golden仍todo；详见 `docs/2026-08-30-G4-G6-webview-aggregate-budget-batch75.md`。
- **G6 Batch76 已闭合品牌无关 native window lifecycle primitive，但发行binary/runtime仍todo（2026-08-30）**：`VerifiedDesktopWindowAuthority`只包Rust已验AuthContext；lifecycle先bind，再用actual `WebviewWindowBuilder`创建，build/registry失败destroy+unbind。主Webview固定devtools false、deny new-window/download；navigation在macOS只收`<scheme>://localhost`，Windows另精确接Tauri/Wry默认`http://<scheme>.localhost`，userinfo/非默认port/错host拒绝。managed lifecycle只在真实`Destroyed` unbind，Focused等事件不提前撤权，shutdown一次撤全部。implementation=`c424228a85b088f5167cdaa64493c6f6cf6e1b53`；MockRuntime/host最终`109/0/0`，invalid label rollback、两个window shutdown、managed state均有actual builder证据。Clippy首跑lifetime失败后绿；Windows中间dead-code warning修复后最终无warning；Tauri guard、parity/strict/Grok全绿。无`tauri.conf`、session source、产品identity、真实Wry event loop/T-ID/UI变化；详见 `docs/2026-08-30-G6-native-window-lifecycle-batch76.md`。
- **G2/G6 Batch77 已闭合 Desktop Local app-instance authority source，但PG/setup仍todo（2026-08-30）**：infra而非transport从host断言的current-user app-data绝对路径加载/创建closed v1 non-secret 256-bit instance；不用`USER/USERNAME`。candidate完整写+fsync后hard-link noclobber，32并发store只选一个winner；匹配crash temp回收，corrupt/symlink/宽权限拒绝，Unix root/file实得0700/0600。deployment/tenant绑定instance，actor固定`desktop-local-user`，generation0、single-user Admin+User，不复用Server兼容键。implementation=`fcab8951ece371670307687672ccad698d6fb344`；新测首次`3/1/0`（guard自匹配）后`4/0/0`，完整infra lib=`315/0/0`；Clippy首跑chunk API建议后绿。parity首次粗`auth/mod.rs`红13，改归single-user owner且不加overlay后最终0违反/revalidate0。Windows infra图未跑，PG principal/package membership与lifecycle setup未接；详见 `docs/2026-08-30-G2-G6-desktop-local-authority-batch77.md`。
- **G2/G6 Batch78 已闭合 Desktop Local PG principal与package membership，但setup仍todo（2026-08-30）**：参数化既有single-user事务内核而不改Server固定键；Desktop authority以独立non-routable canonical profile原子upsert user、保留generation并收敛sole admin，再生成exact single-user Tenant Package context。专用PG17.11 TCP SCRAM实得Desktop principal/repair/package membership=`1/0/0`与Server dev_actor=`3/0/0`；完整infra sandbox首跑300/15全为loopback EPERM，宿主重跑315/0/0，Clippy绿。implementation=`e47932debc254a50700f644b7b88e6162b7a578c`；临时PG已fast-stop删除。Windows未跑；app-data与sidecar同根、Tauri setup/真实window仍todo；详见 `docs/2026-08-30-G2-G6-desktop-local-provisioning-batch78.md`。
- **G2/G6 Batch79 已闭合 instance-bound PG17 data-dir与pre-window bootstrap，但supervisor/setup仍todo（2026-08-30）**：asserted app-data root只派生0700直接子目录`postgresql-17-<instance>`，symlink/非目录/宽权限拒绝；任何schema写前向活库反查canonical data_directory、major17、数值loopback/current local address、password mode与HBA host全SCRAM。Server fresh/legacy/native初始化上收infra共用，随后才principal+exact-tenant package。自包含PG17.11真实first Fresh/membership1、restart RustManaged/grant0、第二instance mismatch写前拒绝=`1/0/0`；infra=`319/0/0`、Server=`216/0/0`、Clippy绿。implementation=`9e1600dca21a815f7c4bfd71ec7635cb711c5a0c`；临时data/socket清零。测试口令不是OS key-store证据；Windows、sidecar pin/supervisor/start-lock/OS key store、真实Tauri app-data/setup/window与reviewed动态tenant package仍todo；详见 `docs/2026-08-30-G2-G6-desktop-local-sidecar-bootstrap-batch79.md`。
- **G2/G6 Batch80 已闭合 PGDG source pin、release bundle attestation与single-instance start lock，但process/key-store仍todo（2026-08-30）**：官方17.11 tar.gz exact size/SHA/checksum copy由`cargo xtask postgres fetch-source|verify-source`校单root/PAX/COPYRIGHT/关键源码；Desktop只接受signed-core manifest digest+reviewed signing identity，锁platform/arch/source SHA/epoch/min-core/三个program与全文件inventory，extra/symlink/非lowercase/hash漂移拒绝。start lock以instance+manifest+CSPRNG nonce、create_new、0600、file+dir fsync排他，Drop只bounded比自己的字节，被替换/崩溃残留不删。implementation=`c6a6fde8415e2e61694c1e69110a4f58638068cf`；Desktop=`113/0/0`、xtask=`95/0/0`、Clippy/Tauri guard/Windows all-feature compile绿，Linux feature-only compile绿但有既有2 warning。Cargo packages823不变。未构建/签名平台PG binary，OS key store/process/ready/shutdown/Tauri setup仍todo；详见 `docs/2026-08-30-G2-G6-postgres-sidecar-bundle-lock-batch80.md`。
- **G2/G6 Batch81 已闭合 PostgreSQL SCRAM OS key-store边界，但process/Windows真机仍todo（2026-08-30）**：只有持有Batch80 start lock者可按reviewed canonical lowercase service+instance account load/create；existing必须64 lowercase hex，none由32-byte CSPRNG生成，写后立即回读并CT相等，否则reconciliation。所有Rust read先进入`SecretBytes` owner，Debug/Clone/Serialize关闭。macOS `security-framework 3.7.0`独立private Keychain真create/read/restart/delete；Windows继续由唯一unsafe crate封装Cred generic/current-user/local-machine，OS blob volatile清零再CredFree，public owner Drop zeroize。implementation=`b11f7b3d179275d5c680cbfdd1ec965f6e3927bb`；Desktop=`115/0/0`、macOS/Windows all-target Clippy绿，Windows真机用例ignored未跑；key-store/deny/Tauri guards绿。Cargo packages823→825；Vet mac272/Windows269红且exemption0改。platform PG binary/process/ready/shutdown/Tauri setup仍todo；详见 `docs/2026-08-30-G2-G6-postgres-key-store-batch81.md`。
- **G2/G6 Batch82 已闭合 PostgreSQL verified process→SCRAM ready→graceful shutdown，但bootstrap/setup仍todo（2026-08-30）**：exact app/data path与私权后持start lock；三个verified program在5s内语义验17.11且version失败写secret0。持锁取OS secret；empty-only initdb以stdin两次喂secret、整体30s，argv/env/file无secret；closed numeric-loopback/SSLoff/all-host-SCRAM配置，direct Child spawn并在10s内以TCP SCRAM `SELECT 1`才ready。verified pg_ctl fast+exact child wait后才释锁；unclean Drop先留stale lock再kill。implementation=`cd66ed46c0255137bac2de0c91f5077503b48461`；Desktop=`119/0/1`、真实host fixture另`1/0/0`，fresh/existing/clean/unclean/fault全证；mac/Windows Clippy绿，Linux compile既有2 warning。fixture复制Homebrew三binary但依赖/share仍外部，不是release bundle。Windows runtime、release PG build/sign、Batch79 schema/package组合、Tauri setup仍todo；详见 `docs/2026-08-30-G2-G6-postgres-sidecar-process-batch82.md`。
- **G2/G6 Batch83 已闭合 ready child→fixed business DB→Batch79 bootstrap composition，但Tauri setup仍todo（2026-08-30）**：`desktop_admin`上收contracts单源；wrong tenant在建库前拒绝。Unattested admin只能做R153 live attestation，通过后才可创建/核验owner/UTF8/C locale闭集的`openbot`并对create error重读reconcile；随后shared migration→principal→package。data-plane owner共同持pool/child，clean先关pool再verified shutdown。infra拆`desktop-local`与Server runtime/SSO，Desktop三平台TLS/SAML/OpenSSL=0且Windows Clippy绿。implementation=`798cdbe59c0f952c182cacae14002b03bfc3e10b`；Desktop=`120/0/2`、infra=`321/0/0`、Server=`216/0/0`、composition host=`1/0/0`、SCRAM infra all tests=`514/0/0`、agent runtime=`8/0/0`。parity0违反，overlay=`1289/397/2/6`。release PG、Windows runtime、真实Tauri path/ApplicationService/window仍todo；详见 `docs/2026-08-30-G2-G6-postgres-bootstrap-composition-batch83.md`。
- **G2/G6 Batch84 已闭合 Desktop Vault master-key OS key-store前置，但ApplicationService/Tauri仍todo（2026-08-30）**：唯一`OsSecretStore`承载macOS Keychain/Windows sole unsafe Cred wrapper，PG/Vault各有独立格式。持锁data-plane按per-instance account load/create version1+32-byte master，corrupt写0、new回读CT；随后构造tenant Vault、audit、run assertion、MCP OAuth材料并drop master。audit/MCP labels上收domain并钉旧Server向量。implementation=`fc2085cffd8ceeb096ba576f228e41a88e5172aa`；Domain=`371/0/0`、Desktop=`123/0/2`、Server=`216+7`、PG+Vault host=`1/0/0`；macOS两条private Keychain真跑、Windows Clippy绿但runtime未跑、Linux compile既有2 warning。Cargo825、parity/overlay不变。Linux Secret Service、rotation、production ApplicationService/Tauri仍todo；详见 `docs/2026-08-30-G2-G6-desktop-vault-master-key-batch84.md`。
- **G2/G6 Batch85 已闭合共享 production ApplicationService adapter assembly，但 Desktop/Tauri consumer 仍todo（2026-08-30）**：`openbot-infra::application_assembly`只收host已验证typed输入，组装全部PostgreSQL application adapters与唯一`Arc<dyn ApplicationService>`；不读env、不拥有Axum/listener/Tauri/window。Server删除重复wiring并精确消费一次；Desktop data-plane只从持锁sidecar borrowed SCRAM bytes构造无password `String`、Debug脱敏的listener hand-off。implementation=`5ba28673d6d03340095d74b2511d9438f5dcfb5f`；Infra=`323/0/0`、Server=`216+7`、Desktop=`123/0/2`、真实PG shared assembly=`1/0/0`、真实sidecar/bootstrap/listener-config/shutdown=`1/0/0`；macOS/Windows Clippy绿，Linux compile保留既有2 warning。Cargo825、parity=`813/881/1694`、overlay=`1289/397/2/6`不变，0违反；无上游目录故recount=`71/0/88 skipped`、strict未跑。真实Tauri app-data/background/shared-assembly consumer/window-last仍todo；详见 `docs/2026-08-30-G2-G6-application-service-assembly-batch85.md`。
- **G2/G4/G6 Batch86 已闭合 Tauri app-data background owner、Agent/RunRelay 与 window-last代码/宿主纵向，但正式发行/Wry仍todo（2026-08-30）**：event loop零窗启动，setup唯一读`AppHandle::path().app_data_dir()`；静态预建窗拒绝。authority→verified PG→bootstrap→keys→policy→shared ApplicationService→BuiltInAgentRuntime/RunRelay→protocol→首窗，pending protocol 503且single-install；最后`Destroyed`按authority→transport→Agent/relay→MCP→sidecar停机并在完成前阻止exit。Server/Desktop分别注入PG/local-file preference adapter；Desktop provider HTTPS/SafeDialer、零env/HTTP fallback，managed source拒绝。implementation=`35ab92b12b7ae85ec8bce0c02366ea0a1c0c8b76`；Desktop=`130/0/3`、Infra=`323/0/0`、Server=`216+7`、真实background PG=`1/0/0`（run真实到`failed/provider_authentication`）、shared assembly PG=`1/0/0`。macOS Clippy绿；Windows最小图绿但full cross在ring/MSVC CRT前置红，Linux最小compile既有2 warning。Cargo825、parity/overlay不变。正式tauri.conf/capability/identity、release PG、Windows full-runtime/真机、真实Wry/golden仍todo；详见 `docs/2026-08-30-G2-G6-tauri-background-assembly-batch86.md`。
- **G3/G4 Batch87 已闭合 run-wide normalized token accounting 子项，但完整 cost/concurrency/computer budget 仍todo（2026-08-30）**：native0024只给`runs`追加9列/3 CHECK，按lease/fencing与连续sampling index原子累计input/output/total；同last sampling精确回放，异值/跳号/derived ceiling漂移冲突。超预算sample先如实记账再落`failed/run_token_budget_exceeded`，per-sampling超限同样不丢实际usage。derived ceiling只等于权威per-sampling cap×最多9次sampling，不冒充用户费用上限。implementation=`7592caeae379ef78fefff06259c6ecfbd7614c34`；Agent/Application/Domain/Infra=`35/153/371/323`，PG run runtime=`5/0/0`，native0016/0023/0024=`3+1+1/0/0`，schema0024=`45表/437列/320 NOT NULL/246约束/91索引`。四crate Clippy、fmt、parity/recount绿；parity=`813/881/1694`、fixtures=`18/22/40`、overlay=`1283/403/2/6`、0违反，strict未跑。Cargo/Grok/npm/workflow不变；详见 `docs/2026-08-30-G4-run-wide-token-accounting-batch87.md`。
- **G3/G4 Batch88 已闭合 operator-attested provider cost upper-bound accounting，但用户cap/并发/computer预算仍todo（2026-08-30）**：零内置/联网价格；package `model.yaml::pricing`或managed六个`BOT_PRICE_*`绑定provider/model/3位currency/maximum input-output micro-unit rate/credential-free HTTPS source+SHA/observed-at。native0025在同usage事务冻结snapshot并以whole micro-unit+millionth remainder无浮点累计；缺rate=NULL/unpriced，future时点双拒，rate/currency/source漂移conflict。由于三家Usage无稳定cache细分，它只称保守上界、不冒充vendor账单；不同currency不换算。implementation=`6d46cb0d2ecd6d8d4b03c4c7d5d46ac3e2da91aa`；Application/Agent/Infra/Server/Desktop=`157/36/323/217/130`（Desktop另3 ignored），Server bin=`7`；PG context/native24/native25/run=`1+1+1+5`，schema0025=`45表/447列/320 NOT NULL/247约束/91索引`、SHA=`f72fe0…`。五crate Clippy、SafeDialer/Tauri/deny/fmt绿；parity=`819/881/1700`、env=`55/25/80`、fixtures=`19/22/41`、overlay=`1289/403/2/6`、0违反；recount=`71/0/89 skipped`、strict未跑。Cargo825，Grok/npm/workflow不变；详见 `docs/2026-08-30-G4-run-cost-upper-bound-batch88.md`。
- **G3/G4/G6 Batch89 已闭合 actor-scoped per-run cost cap，但并发tool/computer预算仍todo（2026-08-31）**：closed preference/API只含cap null或3位currency+positive canonical micro-unit字符串；native0026在BeginThreadRun同事务冻结cap，设置后改不漂移已有run。cap缺rate或币种不匹配在provider effect前稳定拒绝，cost upper bound超限先记账再拒绝；三类拒绝都写只含stable code的`agent.run_cost_budget_refused` hash-chain audit。Server/Desktop/Settings同typed ApplicationService；UI无float且明确非账单。implementation=`29c4201b7908bf2d2e9268e0ea4abf223d255813`；Contracts/Domain/Application/Agent/Infra/Server/UI/Desktop=`100/371/158/38/323/218/180/130`且0失败（Desktop3 ignored）；PG native26/context/assembly/run=`2+1+1+5`，schema0026=`46表/455列/326 NOT NULL/253约束/92索引`、SHA=`ad5375…`；bundle=`1868812/115524/740216/1/0`。parity=`823/881/1704`、fixtures=`20/22/42`、overlay=`1293/403/2/6`、0违反；recount=`71/0/89 skipped`、strict未跑。Cargo825，Grok/npm/workflow不变；详见 `docs/2026-08-31-G4-user-run-cost-cap-batch89.md`。
- **G4 Batch90 已闭合 Agent semaphore 排队取消与 Batch89 transport 穷举回归，但完整并发tool/computer预算仍todo（2026-09-03）**：已激活run在取得permit前收到cancel时，不再只清reservation；统一从Queued经reducer/`DurableTextRun`提交Cancelled，再释放per-run tool sequence。新增`max_concurrency=1`回归修前稳定超时、修后Agent=`39/0/0`。`transport_parity.rs`显式登记Batch89两个预算命令，修前`E0004`、修后=`8/0/0`，无wildcard棘轮保留。implementation=`d97f232bdd497057a028203c63e7394ff9667833`；Agent+testkit与Desktop定向Clippy、fmt、CEL corpus=`6/0/0`、SafeDialer deadline=`1/0/0`、parity=`823/881/1704`、fixtures=`20/22/42`、overlay=`1293/403/2/6`、0违反。native/schema/API/T-ID/Cargo/Grok/npm/workflow不变；strict未跑、`cargo xtask ci`/Actions未跑。审计其余结论按R164分类，详见 `docs/2026-09-03-G4-Agent并发排队取消与审计复核-batch90.md`。
- **G4 Batch91 已闭合 process-wide并发tool runtime budget，但computer runtime budget仍todo（2026-09-03）**：host-only scheduling默认serial；只有build-owned 11个ordinary compiled component显式parallel，human/remember/MCP/Drive/sandbox/unknown均serial。stable wave只并行resource-lock不相交调用，跨run同key互斥；全run共享默认8、typed上限256的tool semaphore。等待permit取消零effect→Cancelled；任一parallel child已启动则abort+drain后reconciliation；human detached waiter持permit到durable retirement。成功结果仍按provider index写exchange。implementation=`f71087fc0aad923a0c94a55578fcb529930587a7`；Contracts/Agent/testkit=`101/46/17`且0失败（testkit9 ignored），真实PG17.11 Agent全套=`9/0/0`并证明production双component峰值2与DB/provider顺序一致；五crateClippy、Contracts WASM、fmt、parity全绿。parity/fixtures/overlay不变，native/schema/API/T-ID/Cargo/Grok/npm/workflow不变；strict/CI/Actions未跑。详见 `docs/2026-09-03-G4-concurrent-tool-runtime-budget-batch91.md`。
- **G4 Batch92 已闭合 AG-UI error production vertical，但其余AG-UI族仍todo（2026-09-03）**：`RUN_ERROR`的remote message/code在decoder边界压成无载荷`GenerationFailed`，malformed/unknown/sequence-invalid压成`InvalidResponse`；Agent只提交`provider_generation_failed`/`provider_invalid_response` stable terminal，UI只收closed notice。真实PG17.11+SafeDialer/SSE同一remote Agent连续跑success/error/malformed三run，messages/run_events/audit canary0，invoked3。implementation=`1f684126e38c5ed81ca954e59c2d65c4af4128e7`；Agent=`47/0/0`、PG Agent=`9/0/0`、testkit=`17/0/9 ignored`、UI定向=`1/0/0`，Agent/Testkit Clippy与fmt绿。只关闭T-EVT-0011；parity=`824/880/1704`、events=`36/52/88`、fixtures/overlay不变，native/schema/API/deps/Cargo/Grok/npm/workflow不变；strict/CI/Actions未跑。详见 `docs/2026-09-03-G4-AGUI-error-production-batch92.md`。
- **G3/G4 Batch93 已闭合 AG-UI reasoning retention，但其它AG-UI族和G8物理retention仍todo（2026-09-03）**：decoder只输出visible reasoning，encrypted value正文在边界丢弃；active run按sequence可重放，任一terminal统一在同一事务将reasoning收敛为`{channel:reasoning,delta:"",retained:false}`，不删event或改cursor/terminal。native0027回填四种历史终态且保持active/text/identity/sequence/time，schema0027与0026字节相同。implementation=`d39e6a4bf2c18414bdd6803cb1ca0d321a9c3d48`；PG native0027 regeneration开/关各`1/0/0`、run runtime=`5/0/0`、remote SafeDialer/SSE→Agent→PG=`1/0/0`、Infra/Agent/testkit/UI=`324/47/17(+9 ignored)/1`且0失败；四crate Clippy、fmt、parity、Grok inventory绿。T-EVT-0008/T-FIX-0043 done后parity=`825/879/1704`、events=`37/51/88`、fixtures=`21/22/43`、overlay=`1293/403/2/6`、0违反。recount=`71/0/89 skipped`，Cargo825/API/env/视觉/npm/workflow/Grok tree不变；strict、CI、Actions未跑。详见 `docs/2026-09-03-G4-AGUI-reasoning-retention-batch93.md`。
- **G4 Batch94 已纠正 AG-UI error 的机器台账身份，计数不变（2026-09-03）**：Batch92 docs commit误把error证据挂到相邻T-EVT-0003并把tool-call-result置done，真正T-EVT-0011仍todo；代码/R166/真PG结论本身正确。correction=`2050fab0369bbc1537f94347eb1f74b75ffb5820`将T-EVT-0003恢复todo、T-EVT-0011置done并移动原证据。parity仍`825/879/1704`、events仍`37/51/88`、0违反；不能用总数相等掩盖T-ID身份错误。无代码/schema/deps变化；strict/CI/Actions未跑，详见 `docs/2026-09-03-G4-AGUI-error-ledger-identity-fix-batch94.md`。
- **G3/G4/G6 Batch95 已闭合五类 AG-UI bounded untrusted projection，但tool-result/interrupt仍todo（2026-09-03）**：state/messages/activity/step/raw-custom经private-field、non-serde `ProviderRemoteProjection`，local family/source与显式untrusted wrapper不可由remote覆盖，单event≤1MiB、session≤4096/8MiB。projection与text共用sequence，terminal同事务清内容；messages不覆盖权威transcript，UI只校验隔离。implementation=`20331887e7260c5be3749b6bb955dda7931bb6f8`；Application/Agent/Infra/UI/testkit=`161/50/324/181/17(+9 ignored)`，PG run+conversation=`5+1`、完整Agent=`9`且0失败。七crate Clippy、fmt、WASM、钉版offline bundle与同源二次SHA绿；bundle=`1874461/115524/740216/1/0`。五T-ID done后parity=`830/874/1704`、events=`42/46/88`、fixtures=`21/22/43`、0违反，recount=`71/0/89 skipped`。native/schema/API/env/Cargo/npm/Grok/workflow不变；strict/CI/Actions未跑。详见 `docs/2026-09-03-G4-AGUI-bounded-untrusted-projections-batch95.md`。
- **G4 Batch96 已闭合 AG-UI tool-result production projection，但interrupt-resume仍todo（2026-09-03）**：production component catalogue/context真实向remote RunAgentInput提供showQuote schema；START/ARGS/END/RESULT先复核completed+offered+nonduplicate，再只写R169 untrusted projection。`NoAgentToolInvoker`下run仍completed，messages role=tool与tool_calls合计0；active binding/canary=1，terminal marker=10/canary=0。evidence=`f7988cabf4fcb11a6296aaf020f0d7c855c17494`；真PG定向=`1/0/0`、testkit=`17/0/9 ignored`、Clippy/fmt绿。T-EVT-0003 done后parity=`831/873/1704`、events=`43/45/88`、0违反，recount=`71/0/89 skipped`。无production代码/schema/API/deps/UI变化；strict/CI/Actions未跑。详见 `docs/2026-09-03-G4-AGUI-tool-result-production-batch96.md`。
- **G4 Batch97 已闭合 AG-UI interrupt/resume typed contract前置，但T-EVT-0010仍todo（2026-09-04）**：known-field-only interrupt、unique/bounded batch、resolved/cancelled resume、local/protocol run identity与parentRunId/resume[] encoder已落，unknown authority丢弃；runtime仍保留provider_generation_failed。implementation=`c90aba9fc0dfcdf5c370ed8ec1fee5ed26814a74`；Application/Agent=`163/53`且0失败，Application/Agent/Infra Clippy与fmt绿。parity计数不变；native/resolve/audit/AwaitingHuman/第二次SafeDialer/UI未落前不勾T-ID。详见 `docs/2026-09-04-G4-AGUI-interrupt-resume-contract-batch97.md`。
- **G4/G6 Batch98 已闭合 AG-UI interrupt/resume durable production vertical，但G4/G6整关仍红（2026-09-04）**：native0028专表、Rust UUIDv7 handle、DB 30分钟TTL、fresh actor/generation/membership/fencing、request/resolve/cancel/expire hash-chain audit、AwaitingHuman lease/cancel/deadline、initial+8 continuation预算、local/thread/Bot/endpoint lineage与第二次SafeDialer request已落；Server GET/PUT、Desktop与conversation UI共用typed ApplicationService，terminal同事务清descriptor/answer。implementation=`9164ac03775e0618712fcdd5648939679a9c04e5`；schema=`47/477/342/268/97`、fixture SHA=`7c6c2351…`；真PG Agent=`10/0/0`、coordinator/native/assembly=`1+1+1`、run-runtime=`5/0/0`，Agent/UI=`55/182`，九crate Clippy、WASM、deterministic bundle绿。parity=`838/872/1710`、events=`48/44/92`、fixtures=`22/22/44`、0违反；T-EVT-0010与AG-UI十一事件族done。三家trace、acting Approval/computer、完整G6/golden等仍todo；详见 `docs/2026-09-04-G4-AGUI-interrupt-resume-durable-batch98.md`。
- **G4/G6 Batch99 已闭合 MCP admin backend/private-egress纵向，但完整MCP Admin/UI仍红（2026-09-04）**：native0029只给custom server保存canonical numeric CIDR，NULL/空默认public-only；同一集合进入v2 transport fingerprint、真实`tools/list`与每次`tools/call` SafeDialer。typed GET page/custom add/delete/general refresh走Server/Desktop同一ApplicationService；配置、credential rotation与hash-chain audit原子，delete本地撤credential/grant/server。implementation=`5070283b5f61c4780405de3f614435363042e138`；schema=`47/478/342/269/97`、fixture SHA=`c661463f…`；真实SCRAM PG+TLS RMCP=`3/0/0`，OAuth=`2/0/0`，Agent callback=`2/0/0`，schema baseline=`13/0/0`。核心=`103/164/326/220/131`且Desktop3 ignored，transport parity8；Clippy/WASM/guards绿。API=`88/86/174`、parity=`842/868/1710`、fixtures=`23/22/45`、0违反；strict recount=`160/0/0`。skills/grants/for-agent/legacy-call、四个Plugins/Skills UI route、custom private user-OAuth与admin delete后vendor revoke runbook仍todo，不关闭G4/G6；详见 `docs/2026-09-04-G4-G6-MCP-admin-private-egress-batch99.md`。
- **G4/G6 Batch100 已闭合 MCP skills/grants/for-Agent backend，但完整MCP Admin/UI仍红（2026-09-04）**：personal/deployment skill、MCP/skill grant与actor-specific discovery走Server/Desktop同一typed ApplicationService；global/MCP双层current-admin gate，personal要求skill+Bot同owner。current user/Agent/skill/catalog行锁、Repeatable Read投影、MCP五项current identity+active state、closed hash-chain change audit、stale/soft-delete admin cleanup已落；普通用户`grantedTo`不枚举private/cross-tenant Bot，skill内容Debug脱敏。implementation=`62d26501233b843ab9b34092b8c16b8a71d0f68c`；真PG17.11 SCRAM=`1/0/0`且覆盖三类binding漂移、15行hash-chain与audit rollback。核心=`104/372/165/326/221/131`且Desktop3 ignored，transport parity8；八crate Clippy、WASM/guards绿。API=`93/81/174`、parity=`847/863/1710`、fixtures=`23/22/45`、0违反，strict=`160/0/0`。legacy-call、四UI route、Desktop OAuth、custom private user-OAuth与vendor revoke runbook仍todo，不关闭G4/G6；详见 `docs/2026-09-04-G4-G6-MCP-skills-grants-batch100.md`。
- **G4 Batch101 已退役无调用者 legacy MCP browser call，但完整G4/G6仍红（2026-09-04）**：固定上游明确该route无仓内调用者且旧browser loop已迁server-owned；三字段wire缺run/call/sequence/fencing/budget authority，故Server/Desktop保持unmatched404并锁定application effect0。真实能力继续由AgentToolGateway→typed InvokeTool执行，本轮PG17.11+TLS RMCP重跑完整grant/CEL/decision+attempt/capability/vendor/outcome/audit纵向。implementation=`00b3ae92e46e0ff332d59df68183c5d0a2d73623`；Server/desktop=`7+1/0/0`、Agent/Application=`2+3/0/0`、真PG=`1/0/0`，Clippy/fmt/parity绿。T-API-0094改`替代/remove/done`；API=`94/80/174`、parity=`848/862/1710`、0违反，strict=`160/0/0`。四UI route、Desktop/private OAuth、vendor revoke runbook与完整G4/G6仍todo；详见 `docs/2026-09-04-G4-MCP-legacy-call-retirement-batch101.md`。
- **G4 Batch102 已闭合 custom private MCP user-OAuth egress lifecycle，但完整G4/G6仍红（2026-09-04）**：单一canonical CIDR reader后，同一server authority进入registration、PRM/AS、code、runtime refresh、immediate revoke与pending retry；state v3与begin/callback/rotation current locks/CAS阻断pre/post-token配置漂移，disconnect不再复用旧网络快照，Drive拒绝private override。implementation=`3745c4f6f10395d7ced7c214f5f74e97ca49a36b`；PG OAuth/credential/RMCP=`2+12+6/0/0`，Infra=`327/0/0`，Clippy/fmt/guards绿。T-API既有四条evidence重验证，parity/API仍`848/862/1710`与`94/80/174`，0违反，strict=`160/0/0`。Desktop OAuth、四UI route、admin-delete vendor compensation与完整G4/G6仍todo；详见 `docs/2026-09-04-G4-MCP-private-OAuth-egress-batch102.md`。
- **G4 Batch103 已闭合 MCP admin-delete vendor compensation/runbook，但完整G4/G6/G8仍红（2026-09-04）**：删除事务冻结versioned resource/transport/exact CIDR/retained client ID，不复制secret；先撤本地token/join/全部server-prefix grant并写actor local-disconnect+configuration hash-chain audit，再由reconciler只用旧上下文做RFC7009，绝不回落同ID replacement。vendor失败保持pending；删除前/后本地材料损坏转`operator_required`且停止无限重试；成功后擦user-token网络上下文，最后一条完成或零用户时client转人工vendor registration删除。bearer同样显式人工处置；错绑credential不被误撤。implementation=`caea34d88504cbbd567a79a8c8fb2e99c06fe872`；PG OAuth=`2/0/0`、RMCP=`6/0/0`、Infra/Server/Desktop=`327/222/131`且Desktop3 ignored，三crate Clippy、fmt与相关guards绿。无新T-ID，parity/API仍`848/862/1710`与`94/80/174`，strict=`160/0/0`；native/schema/deps/Cargo/npm/Grok/workflow不变。Desktop OAuth、四UI route、protocol cancel与G8 typed operator-attestation/最终secret retirement仍todo；详见 `docs/2026-09-04-G4-MCP-admin-delete-vendor-compensation-batch103.md` 与 `docs/runbooks/mcp-server-removal-vendor-revocation.md`。
- **G4 Batch104 已闭合 OpenAI 官方 recorded trace 1/3，但完整G4仍红（2026-09-04）**：固定`openai/openai-dotnet@19d0a3cb…`公开RecordedTest，13,211-byte source record的36段ResponseBody逐字节提取为9,070-byte SSE并独立`cmp`相等；source commit/blob/SHA、fixture SHA、MIT/copyright、消毒范围进入一对一provenance、SPDX/NOTICE与offline guard。production OpenAiProvider经SafeDialer/HTTP/SSE以整块、非规则、逐字节chunk得到同一function-call/usage/唯一terminal。实录done事件缺name的兼容只在added已有name时开放，改名/索引漂移/全程无name三条负向均锁死。candidate=`cb6e2d69b5ea70d4a046d31aea00c3b2bdd02e77`，hardening=`5becad90ee71fd6b84f1bbc6f47d1e24fdb3a1dc`；OpenAI unit/recorded/Infra lib=`7+1+328/0/0`，Clippy/fmt/guard绿。T-FIX-0046 done后parity=`848/862/1710`、fixtures=`24/22/46`、strict=`160/0/0`。Anthropic/Google、live credential、T-FIX-0013与完整G4仍todo；详见 `docs/2026-09-04-G4-OpenAI-recorded-trace-batch104.md`。
- **G4 Batch105/R180 已闭合 Rust run→RMCP list/call protocol cancel并纠正现代wire，但完整RMCP conformance/G4仍红（2026-09-04）**：private/non-serde first-reason-wins channel以Rust-minted ToolCallId在process-local registry登记，Server/Desktop gateway与tool control从共享Application assembly消费同一实例；pre-cancel socket0。client lifecycle首选`server/discover`/2026-07-28并自动回落2025-11-25：现代fresh list/call通过关闭exact response stream取消、notification POST=0；只有legacy fallback发送exact requestId `notifications/cancelled`。固定rmcp 3.1.4首event前worker stall由唯一SafeDialer仅在modern tool request上drop exact future规避。5秒合作收口期间续lease，call transport boundary后仍落Unknown/reconciliation，signal失败使用`mcp_cancel_signal_unknown`。原implementation=`9b03cca38b0841bce46acc6ed1132b2633064cdd`，R180 correction=`3a694658a34ef3b9cbeba60fc32907eba8b50a2b`；Infra/Agent=`328/57`，RMCP no-PG=`8/0/3 ignored`、PG17.11+official RMCP=`11/0/0`，两crate Clippy与RMCP/SafeDialer/MCP OAuth/Application/Tauri/SAML guards绿。T-FIX-0047保持done，fixtures=`25/22/47`，parity仍`848/862/1710`；R179关于modern notification的断言已废止。T-FIX-0018完整官方conformance、computer/file/shell cancel与完整G4仍todo；详见 `docs/2026-09-04-G4-RMCP-run-protocol-cancel-batch105.md`。
- **G4/G5 Batch106 已闭合 browser LRU/idle selector与Rust residency owner，但完整Computer runtime budget/G4/G5仍红（2026-09-04）**：固定OpenBot commit的13条browser-eviction测试逐项移植；`BrowserRuntimeManager`以完整ComputerSecurityScope digest+ComputerId+generation持有opaque driver handles，同scope cold start合并，cap/idle/generation/stop只实际close无activity lease实例，全slot active时effect前`computer_runtime_busy`，launch失败恢复victim，shutdown先关闭new lease。fixture明确production Server/Desktop EngineProcess/Supervisor assembly与CPU/RSS/pids/disk均false。selector=`2385493d5e2c6472b4470e39d167e60f272c4f6f`，manager=`b92c401a540281460c98542a8f29583343746578`；Computer=`43/0/2 ignored`、Clippy/fmt绿。13条T-TEST与T-FIX-0048 done后parity=`861/849/1710`、tests=`470/577/1047`、fixtures=`26/22/48`、overlay=`1286/416/2/6`、0违反/警告。两个COMPUTER配置、production process与完整资源预算仍todo；详见 `docs/2026-09-04-G4-G5-browser-runtime-residency-budget-batch106.md`。
- **G6 Batch107/R182 已闭合Golden比较器与PNG/manifest/diff gate，但245张baseline与G6仍红（2026-09-04）**：外部pure core候选只择取单commit为`79d849f0db7a4b822a56ec4369e638b229345c8a`，PNG gate=`a69b3efcf41edd1c7dd0e089885fbd6143fa02a7`。seed-derived exact 245-path、PNG magic/尺寸/allocation、无symlink、reviewed mask与确定性diff均fail-closed；formal verify先要求固定容器sha256/CJK版本，当前`ready=false`按预期拒绝。core/gate/all-features=`18+7+137/0/10 ignored`，xtask=`102/0/0`，Clippy/依赖/target guards绿。Cargo lock新增4个testkit-only零build.rs包、总829；cargo-vet仍373 unvetted。T-FIX-0003 evidence重验证，无新增fixture；overlay=`1283/419/2/6`，recount=`71/0/89 skipped`且strict未跑。Web CDP、Desktop xcap、baseline、AX/键盘/reduced-motion继续todo；详见 `docs/2026-09-04-G6-golden-PNG-gate-batch107.md`。
- **G5 Batch108/R183 已闭合BrowserInput→CDP pure plan，但live engine/P2/G5仍红（2026-09-04）**：候选`1ca08993…`的16键表与unknown-key拒绝被固定上游推翻，主控不落错误中间commit；修正后`b9e464013072b4d190e204aebc0f8afd861ba223`逐项保留17键（含空格）与unknown multi-unit=0。mouse/wheel/key/insertText为closed plan，SecretInsert普通路径拒绝，Debug不含key/code/text。fixture=`1315 B`/SHA-256 `e6cf14b9…`且三项unfinished=false；mapping=`11/0/0`、Computer=`54/0/2 ignored`、Clippy/fmt绿。新增T-FIX-0049，fixtures=`27/22/49`，parity/overlay不变。八条T-BROP、authenticated engine/live effect、坐标/ScreenHub仍todo；详见 `docs/2026-09-04-G5-CDP-input-plan-core-batch108.md`。
- **G5 Batch109/R184 已闭合七种ordinary input的macOS真实engine语义，但P1/P2/G5/G7仍红（2026-09-04）**：不兼容wire显式升protocol/epoch2；fresh non-Clone receipt在Rust重验scope/generation/tab/执行时expiry，shim只收literal CDP闭集。ACK与frame独立；双Seatbelt role逐项观察hover/active/text/Backspace/F1=0/insert/scroll，cross-scope/expired/ordinary-secret frame0。implementation=`71e7feb7005100accf190e16b4bb3f4c79209149`；protocol hash=`ef213bb4…`，official archive=`122102881 B/ee939d…`，ASAR=`22819 B/cb289d04…`，真实role=`2/0/0`，Contracts/Computer/xtask=`104/56/103`。T-BROP七条与T-FIX-0050后parity=`868/842/1710`、browser=`14/36/50`、fixtures=`28/22/50`、overlay=`1276/426/2/6`。secret typed effect、Page.startScreencast/ScreenHub、production assembly、Windows/runsc runtime仍todo；详见 `docs/2026-09-04-G5-engine-protocol-v2-live-input-batch109.md`。
- **G5/G7 Batch110/R185 已闭合正式screencast/ACK与单consumer背压，但P1/P2/G5/G7仍红（2026-09-04）**：protocol/epoch3、OBFRAME2/76-byte完整metadata；shim Page start/stop且不提前ACK，Rust验证并publish进size-one latest后才回frame_ack。400ms慢消费者多次实得received=ack 49..52、drop37..40；stop exact replay、frame task失败闭合、双role无listener/orphan。implementation=`c5f22f424efec517f851c3b495d3de215be1e957`；protocol hash=`16cc1f4d…`、shim596/600、ASAR=`29806 B/02972340…`；Contracts/Computer/xtask=`104/58/103`，真机=`4/0/0`。T-BROP三条与T-FIX-0025后parity=`871/839/1710`、browser=`17/33/50`、fixtures=`29/21/50`、overlay=`1273/429/2/6`。viewer/WS、多viewer、fps/latency/fallback/production assembly与Windows/runsc仍todo；详见 `docs/2026-09-04-G7-screencast-backpressure-batch110.md`。
- **G5/G7 Batch111/R186 已闭合Engine-backed ScreenHub/ticket核心，但P1/P2/G5/G7仍红（2026-09-04）**：launch强制ScreenAudience，active Engine session只交一个带完整stream key与共享ingress state的source；Hub双viewer共享Arc latest并按sequence gap计coalescing。ticket为OS CSPRNG 128-bit、30秒、hash-only/single-use，绑定actor/auth generation/stream及Server origin或Desktop window；generation推进关闭旧viewer。复核并修复source消费不推进共享ingress sequence的虚假drop缺陷。implementation=`ddf49ee5b2b09b940528614330a228ab68077c0a`；fixture=`2368 B/8559fec9…`；Computer=`60+3/0/2 ignored`，真机=`5/0/0`（role2），Browser/Component received=ack为68/69、engine drop均37。新增T-FIX-0051后fixtures=`30/21/51`，parity/browser/overlay仍`871/839/1710`、`17/33/50`、`1273/429/2/6`。T-BROP-0027、Server/Desktop WS、production hook/限额/停流、fps/p95/fallback/assembly与Windows/runsc仍todo；详见 `docs/2026-09-04-G7-ScreenHub-viewer-ticket-core-batch111.md`。
- **G5/G7 Batch112/R187 已闭合viewer coordinate pure与macOS input组合journey，但P1/P2/G5/G7仍红（2026-09-04）**：官方DevTools commit=`036dd84b…`固定mouse/wheel screenZoom与DOM-only pageScale/scroll公式。Rust contain mapper拒letterbox/坏aspect/非finite/越界，DPR恰消，viewport/document坐标分离且携frame sequence；真实双role完成mapped button、pressed→moved→released、`日本語🔐` insertText及wheel scroll。首版origin0误拒1/3与pageScale错误期望均在最终证据前修正。implementation=`49da4d9c8da7db25ae631c332de5fdc517dba8a1`；fixture=`2707 B/8a03fd68…`；Computer=`64+4/0/2 ignored`，真机=`6/0/0`（role2），received=ack69/drop37。fixtures=`31/21/52`，parity/browser/overlay不变。Leptos/WS/stale-frame/production、非1 scale硬件、resize/navigation/tab及Windows/runsc仍todo；详见 `docs/2026-09-04-G7-viewer-coordinate-input-journey-batch112.md`。
- **G5/G7 Batch113/R188 已闭合Server ScreenSession/binary WS，但完整path-stream/P1/P2/G5/G7仍红（2026-09-04）**：ApplicationService发票只收computer/generation/tab，Origin由Rust transport注入；ticket Debug脱敏/Drop清零。POST同源/body前置/no-store；WS拒query、exact base+ticket且base-only响应，在101前单次消费，binary latest、1KiB inbound、client data与generation撤权1008。Server main共用Hub/port；真实Electron Browser frame跨UDS/Hub/Application/Axum TCP到客户端。implementation=`5a7444e1db068d504157260da66ba9696f513dc6`；fixture=`1970 B/6e7365a0…`；Server screen=`4/0/0`、真实=`1/0/0`，Contracts/Application/Computer/Server/Desktop=`105/166/65/226/131`。API/parity/fixtures/overlay=`96/80/176`、`873/839/1712`、`32/21/53`、`1275/429/2/6`。production manager source、PG cookie/TLS、Desktop/input/限流/停流/跨平台仍todo；详见 `docs/2026-09-04-G7-Server-screen-WebSocket-batch113.md`。
- **G2（Auth/Vault/Policy/Audit）已从纯规则推进到真实 OIDC/SAML 网络/FFI 竖切**，四条判据（§24 G2）逐条如实：
  ① **CEL corpus 对等 —— 达成**：`fixtures/policy/cel-corpus.json` 的 69 条逐条在 Rust `cel 0.14.3` 上实跑，与 `cel-js@0.8.2` oracle 的分歧集合**恰好等于**一张写死的 6 条台账（`BTreeMap` 双向相等 + `evaluated == 69` 防跳过：多一条 = 既有规则语义悄悄翻转，少一条 = `cel` 改了行为）。6 条全是 `error → 非 error`，deny 侧放宽 2 条、allow 侧放宽 4 条。
  ② **acting before durable decision = 0 —— 通用 application 边界与真实 MCP/Memory/Drive 集合成员达成，整项仍未闭合**：`InvokeTool` 走 domain 十二段状态机；policy 结论只能由 domain 构造；decision+attempt commit 与 capability CAS 都发生在 `ToolControlPlane::execute(AuthorizedToolCall)` 之前；raw args 只能随字段私有信封到 executor；outcome+audit 同事务。Batch 11–15 把 official RMCP、actor OAuth、Drive、durable approval 与可点击 Web presentation 接入；acting MCP 实得 pending→human grant→approval-linked decision/attempt→vendor success，以及 human deny→attempt/vendor 0。**仍不得宣称整项/G4 通过**：browser/file/shell、Approval完整 thread/cancel/computer 集成与第一次外部安全审计尚未闭合（R41/R74–R78）。
  ③ **v1 credential/SSO decrypt + v2 rotation —— 代码路径达成，Server KMS/HSM 尚未闭合**：v1 解密由**真跨语言互操作**证明（逐字节复制上游 `credentials.ts` 跑 node 产出 7 条信封 → Rust 解开明文逐字节相等 → 测试里内联的 fixture 再喂回上游 `decryptSecret`，双向闭合），每条正向断言配错密钥 / 翻密文位 / 翻 tag 位 / 翻 IV 位四种负向对照。W-7b 又把 `sso_providers` plaintext/v1 同事务迁成 AAD 绑定 v2 并回读校验；当前 key ring 仍由 `KEY_ENCRYPTION_KEY` 单版本提供，未冒充 §6.4 的 Server KMS/HSM。
  ④ **OIDC/SAML/session/role/group/revoke 全矩阵 —— 生产代码、本机矩阵与 Linux CI 历史证据已接通，整项仍因外审/Windows/KMS 未闭合**：W-7a 有唯一 safe dialer、环境三家 OIDC、跨副本 state/token/JWKS/group/keyed session；W-7b 已接动态 admin 注册/更新/删除、匿名 HMAC routing ticket、动态 OIDC 跨 replica、SAML SP metadata/Redirect+POST/ACS、根级 XMLDSig、SHA-2 allowlist、Destination/Audience/Recipient/request/time/replay、account-anchor cleanup 与 v2 SSO config。W-7c 首次真实 Ubuntu 24.04 x86_64 CI 连续暴露缺 `rg`、pipefail/SIGPIPE 与 deadline 测试服务端错误假设，逐项修复后自动 PR run `32762651186` 的 gates/supply-chain 全绿，workspace **1083/0/114**。R63 按用户额度指令重新关闭自动 Actions；这不撤销既有 Linux 证据，也不允许未经重新授权派发。**仍缺第一次独立 SAML/XSW 与整体安全外审、Windows 原生构建、Server KMS/HSM；需要 SP private key 的 signed AuthnRequest 也必须与 KMS 同批，不在当前输入面假装支持**，不得宣称 G2 全矩阵/整关闭合（R48–R50/R61/R63）。
  **W-1/W-2 已闭合**既有 schema/repository 地基；**W-3a/W-3b/W-4/W-6/W-7a/W-7b/W-7c**证据保持。当前 G2 专项 **165/69/234**。当前 tests **470/577/1047**、browser-operations **7/43/50**、env **55/25/80**、events **48/44/92**、tables **59/0**、API **94/80/174**、components **13/11/24**、routes **24/8/32**、UI **91/61/152**、整个 parity **861/849/1710**，fixtures **26/22/48**，overlay **1286/416/2/6**。R64–R68固定G3 data/realtime/migration；R69–R181固定Agent/G4/G5、GUI与Engine阶段门/实施裁决。run-wide normalized token、operator-attested cost upper bound、actor-scoped用户cap/API/UI、并发tool runtime budget、browser LRU/idle selector与Rust residency owner、AG-UI十一事件族、MCP admin/private-egress/skills/grants/for-Agent/custom private OAuth egress lifecycle/admin-delete vendor compensation+runbook、OpenAI官方recorded trace **1/3**及run→RMCP protocol cancel已落，legacy browser call已安全退役。仍未闭合Anthropic/Google recorded trace与live credential、acting Approval完整集成、production完整computer runtime budget、Desktop Local OAuth、Plugins/Skills UI、完整RMCP官方conformance、computer/file/shell protocol/process-tree cancel、G8 typed operator-attestation/最终secret retirement、Agent Desktop真实Wry journey/sidecar binary build-sign+Windows process/key-store runtime+Linux Secret Service+正式tauri.conf/capability/identity/发行binary与golden、完整跨页面/channel Composer与AppSidebar、官方provider品牌SVG、sandbox args/JS/network/callback/MessageChannel正向执行、完整CDP/ScreenHub/viewer ticket/Desktop正式sandbox renderer、其余admin正式journey/golden、GUI/golden与经许可legacy drills。**其它未闭合项**：G4余项/G5余项/G7、G2外审/KMS/Windows、G6/G8（R52–R181）。
  **R182更新**：上述能力计数不变，overlay现为**1283/419/2/6**；Golden compare/gate已落，但245张baseline、capture与formal verify仍红，故G6/G8结论不变。R64–R68固定G3，R69–R182固定后续Agent/G4/G5/G6、GUI与Engine实施裁决。
  **R183更新**：fixtures现为**27/22/49**；CDP pure plan已落但browser-operations仍`7/43/50`，P1/P2/G5/G7结论不变。R69–R183为当前后续实施裁决范围。
  **R184更新**：browser-operations现为**14/36/50**、parity **868/842/1710**、fixtures **28/22/50**、overlay **1276/426/2/6**；只关闭ordinary engine-level input七项，P1/P2/G5/G7整关结论不变。R69–R184为当前后续实施裁决范围。
  **R185更新**：browser-operations现为**17/33/50**、parity **871/839/1710**、fixtures **29/21/50**、overlay **1273/429/2/6**；只关闭screencast/ACK/单consumer latest-buffer，完整Screen/G7结论不变。R69–R185为当前后续实施裁决范围。
  **R186更新**：parity/browser/overlay不变，fixtures现为**30/21/51**；只关闭ScreenHub/multi-viewer/ticket/invalidation核心，T-BROP-0027与production Screen/G7结论不变。R69–R186为当前后续实施裁决范围。
  **R187更新**：parity/browser/overlay不变，fixtures现为**31/21/52**；只关闭coordinate pure与macOS drag/IME/wheel组合journey，production Screen/input与G7结论不变。R69–R187为当前后续实施裁决范围。
  **R188更新**：API/parity/fixtures/overlay现为**96/80/176**、**873/839/1712**、**32/21/53**、**1275/429/2/6**；只关闭Server ticket/binary WS，T-BROP-0027与完整G7结论不变。R69–R188为当前后续实施裁决范围。
  **D-1 已正式裁决**：RUSTSEC-2023-0071 只做一条窄豁免；`tools/check-rustsec-waivers.sh` 锁死精确四节生产依赖链、openidconnect feature 零扩张和 RSA 私钥符号零命中。Batch 15 另有两条仅 `informational=unmaintained`、`patched=[]` 的编译期宏窄豁免（paste/proc-macro-error2），由 `tools/check-ui-dependencies.sh` 锁 proc-macro 消费边界与修复状态；它们不冒充漏洞修复或维护性消失。Batch 16 的 Tauri release graph 另有 runtime UNIC unmaintained×5，尚未 ignore；target-aware advisories 正确保持红。Cargo.lock-only 多报的 Linux GTK/proc-macro-error/glib 十条在六个发行 target 均不可达，由 `check-tauri-dependencies.sh` 负向锁定。见 R44/R78/R79。
  **D-5 已建立棘轮**：CI 钉 `cargo-vet 0.10.0` 并跑 `cargo vet --locked`；Google exact/delta import 锁定 15 个 fully audited。350 个是 R45 bootstrap exemptions；W-7 TLS 新增 20 个、W-7b SAML 新增 30 个、G3 WebSocket 新增 3 个、G4 Batch 11 RMCP/schema 新增 32 个精确 exemption，逐条带 `owner=security` 与 `not a full source audit`，合计 **435**，不冒充审计。Batch 15 GUI baseline 在当前 target-aware no-all-features 口径为 **181 unvetted**；Batch81引入的macOS-only Keychain两包使all-features为macOS **272** / Windows **269**（对Batch16分别+2/+0）。没有批量补 exemption，故cargo-vet明确红；新增/升级未覆盖版本仍当场判红。见 R45/R48/R50/R67/R74/R78/R79/R155。
  **D-3 已正式采用 `zeroize`**：`SecretBytes` 内层是 `Zeroizing<Vec<u8>>` 并标记 `ZeroizeOnDrop`，drop 清除当前 length+capacity；历史扩容 allocation 与调用方副本仍不冒充可擦除。见 R46。
  **D-2 已按真实契约消费者裁决**：Attempt/Capability/Catalog/Auth 四类型穿 application/infra 边界，收口到 contracts 且不 serde，`AuthContext` 不再用裸 `u64` 表示代际；CredentialGeneration 尚未穿 port。SecretId/ServiceId 在 W-7b/R59 后由 infra adapter 就地构造并只传给 domain `RecordBinding`，不是 application/transport contract，继续留在 vault domain；第一次穿 port 时必须同批上收。见 R47/R59。
- §28.1 当前 **200** 条（R1–R200）。实施时除既有安全/G2裁决外，必须读R61–R114、R115–R125、
  **R126–R129（macOS/Windows/runsc/Xvfb P1）**与**R130–R188（后续实施裁决）**；R180覆盖R179的
  RMCP现代取消wire，R182区分Golden，R183–R188依次区分CDP pure/live、正式screencast、ScreenHub、viewer coordinate与Server/production余项。AppSidebar/
  完整跨页面Composer、provider品牌、P1 Windows/runsc真机、P2 CDP/ScreenHub、P3 Desktop renderer、
  Desktop OAuth、完整RMCP conformance、完整computer资源预算、computer/file/shell取消、G8 secret
  retirement与经许可legacy drills仍未完成。
- 上游对照固定在 `CopilotKit/openbot@891df72f1827454d8b353d108fe5dd2313b7e30d`，不引用会漂移的 `main`（§1.2）。

## 2. 目标定义（为什么是这条线）

"全量 Rust"的定义固定为（§0.1，R115 / R117 / R118 精确化）：GUI、业务、内置 Agent、策略、数据库访问、线程与记忆、实时事件、认证、凭据、审计、Supervisor、Desktop authority / window ACL / coordinator、Browser/Computer scope、engine 生命周期、ScreenHub、HumanLease、file/shell 执行域、平台 sandbox helper、egress、进程树与全部高权限控制面由 Rust 实现。**零 JavaScript 业务控制面、零 JavaScript authority、零 Node coordinator / host / Agent / local-exec**；Chromium/Electron 只是受监管、可替换、被 Rust 拥有的 OS 约束包住的浏览器执行引擎，承担两种 role：Browser Computer 与 Desktop sandboxed component 渲染（§11.1）。

允许的非 Rust 例外**只有**：Leptos/WASM 由系统 WebView 渲染；PostgreSQL / Chromium / Electron / OS keychain 作为外部引擎；R129的Xvfb只在Server runsc内部作Electron本地display backend（`-nolisten tcp`，不是VNC/Screen UI，版本/hash随Server image pin）；Electron主进程自带的Node runtime是引擎事实而非第一方控制面；用户自己的HTML/CSS/JS组件作为**不可信数据**在零权限沙箱里跑；用户接入的远程AG-UI Agent任意语言；第一方非Rust源码只剩**最小Electron engine shim**（clean-room、≤600 LOC、无业务裁决权）。构建期工具是钉sha256的二进制、不进发行物；**工作区构建链零npm/Node**——Electron以官方release zip+sha256获取，仓内唯一允许的`package.json`是shim manifest，`grok-bot/`内manifest不参与构建（§0.1、§11.3，R117/R129）。

理由：TypeScript 控制面、CopilotKit Intelligence 真源、跨用户 profile、MCP 过度实现、双数据库、多 driver 是上游的结构性风险（§27）；Rust 不做唯一控制面就不叫重写。

两个发行物共用同一 Rust core 与同一份 Leptos GUI：`openbot-server`（Axum，多用户）与 `openbot-desktop`（Tauri typed in-process；远程模式走同一 Axum API）（§0.2）。

## 3. 固定基线（改任一项 = 新建 delta audit，禁止静默升 lockfile）

| 项 | 钉死值 |
| --- | --- |
| Rust | `1.98.0`，edition 2024（2026-08-22 由 `1.94.1` 升级，delta audit 见 `docs/2026-08-22-Rust工具链1.94.1升1.98.0-delta审计.md`） |
| Tauri / Leptos | `2.11.5` / `0.8.19`（0.8.20 已存在，不升） |
| Leptos 生态 | `leptos_router` **`=0.8.13`**（0.8.14+ 要求 `leptos ^0.8.20`，升 Leptos 必须同 PR 升 router）；`leptos_meta 0.8.6`；`leptos_i18n 0.6.2` |
| GUI 构建工具 | Tailwind CSS standalone CLI `4.3.3`（sha256 表在设计系统文档 §12.1）；trunk `0.21.14`（`--offline`，缺工具即红不下载）；binaryen `version_132`；wasm-bindgen CLI = `Cargo.lock` 版本 |
| GUI 资产 | Inter Variable `4.1`（OFL-1.1）；Lucide `1.33.0`（ISC AND MIT，Feather 衍生子集），只随包 allowlist 里的图标 |
| RMCP | `3.1.4` |
| CEL | crate **`cel`** `0.14.3`（"cel-rust"是仓库名，不是 crate）；oracle = `cel-js@0.8.2` |
| OIDC / SAML | `openidconnect 4.0.1` / `samael 0.0.22` |
| HTTP TLS | `rustls 0.23.43` + `ring 0.17.14` + `webpki-roots 1.0.9`；ring 非纯 Rust，38 Perl/17 预生成对象与 20 条非审计 exemption 由 R48 guard 锁定 |
| WebSocket | Axum `0.8.9` `ws` + `tokio-tungstenite/tungstenite 0.29.0` + RFC6455-only `sha1 0.10.7`；3 条非审计 exemption、build.rs=0、unsafe token=4/0/5 由 R67 guard 锁定 |
| Intelligence bundle | AES-256-GCM `aes-gcm 0.10.3` + HKDF-SHA256 `hkdf 0.12.4` + Ed25519 `ed25519-dalek 2.2.0`；新 package=0，one-shot CLI/最终 runtime exclusion 由 R68 guard 锁定 |
| Browser engine | Electron `43.3.0`（2026-08-04 发布，Chromium `150.0.7871.212`、Node `24.18.1`）：官方 release zip 五平台 sha256 钉在 `tools/engine-pins.toml`（上游 `SHASUMS256.txt` 副本 = `tools/electron-v43.3.0.SHASUMS256.txt`）；不经 npm；critical/high 修复 72 小时内升级（§11.3，R117） |
| `grok-bot/` 参考树 | tree `86f5a85f560f721677fa7e587a67ac0ffc036cb5`（R116 移除两个原始安装包 LFS 指针后）。Anysphere（Cursor）Grok Bot 0.18.0 的反编译重建，**不是** xAI Grok Build；只作架构 / 执行 / 状态机参考，规格先行吸收、不翻译不复制（§11.5）；改动它 = 新 hash + R 行 |
| runsc | 版本在 P1 spike（§19.1）实测后钉入 §1.2 与 `tools/engine-pins.toml`；判据见 §24 G5 / R121 |
| 数据库 | PostgreSQL major 17，**唯一**语义；Desktop sidecar build source钉PGDG 17.11 exact size/SHA，Rust监管本机进程；不需要pgvector |
| 数据库驱动 | `tokio-postgres 0.7.18` + `deadpool-postgres 0.14.1` + `postgres-types 0.2`。**不用 `sqlx`** —— 它的 `query!` 宏让 `cargo build --locked` 的答案取决于跑在哪台机器上（构建期连库或 `.sqlx` 离线元数据二选一）。SQL 手写，由对真库的集成测试验证（G1 裁决 D3） |
| ID 类型 | §5.3 的十五个核心 ID 里，`ComputerGeneration` / `DocumentGeneration` 是 **`u64` newtype**，其余 13 个是 `String` newtype 且**不做 UUID 校验**（R23）。另有四个不 serde 的内部跨层 contract：Attempt/Capability/Catalog/Auth generation（R47） |

上游 oracle 运行时版本（copilotkit 1.68.3、ag-ui 0.0.57、better-auth 1.7.1、mcp sdk 1.30.0、playwright 1.62.1 …）以 §1.2 表为准，fixture 与 golden 只认这些版本。

## 4. 架构约束

- **十个业务/parity 核心 crate + 一个窄 Win32 边界**（§5.1 / R127）：`contracts / domain / application / infra / agent / computer / server / ui / desktop / testkit` 保持业务入口与 parity owner 封闭域；`openbot-windows-sandbox` 是唯一第 11 个例外，只因独立安全边界+Windows-only feature graph存在，公开面不暴露 raw handle/pointer。再建 crate 仍只有四个理由：独立安全边界、独立发布单元、明显不同的 feature graph、可单独复用的纯协议；其余用 module。
- **唯一业务入口** = `openbot-application::ApplicationService`（`execute` / `subscribe`）。Axum、Tauri、测试、迁移工具只做认证、framing、大小限制、错误映射，**不得**各自实现业务规则，不得接受自由 method string、renderer 自报角色或任意 SQL（§5.2）。
- **ID 默认 string newtype**，不限定 UUID；唯一例外是 `ComputerGeneration` / `DocumentGeneration`，二者是派生 `Ord` 的 `u64` newtype（R23，旧 generation 失效依赖数值序）。兼容端必须接受其余上游既有字符串。`AuthContext` 只能由 Rust 从 session / peer / DB ACL 构造，外部传来的同名字段都是不可信输入（§5.3）。
- **Agent reducer 必须 pure**：`reduce(state, event) -> (state, effects)`；DB、provider、MCP、browser、file、shell 都是 effect。每 thread 一个 foreground actor 串行处理；后台工作是独立 durable run（§7.2）。
- **工具只有一条执行管线**（§8.1）：validation → 权威 actor/target → effect 分类 → CEL + 内容策略 → 审批 → **事务写 decision + attempt** → 单次 capability → 执行 → outcome + commit_state。decision 写失败即不执行；执行了但 outcome 写不进去 → `ReconciliationRequired`，不自动重试。
- **两种执行域、两种 engine role**（R118 / R120）：`ExecutionRealm::{HostLocal, ScopedContainer}`——Desktop Local 只有 HostLocal（§10.3 fidelity 门控），Server 与 Desktop Remote 的 shell/file 是 Server 的 ScopedContainer（Supervisor + runsc），两者之间**没有隐式 fallback**，不引入 Docker Desktop；`EngineRole::{BrowserComputer(ComputerSecurityScope), SandboxedComponent(ComponentRenderScope)}`——组件 engine 每 Desktop 应用实例恰一个，render session = 该 engine 的一个 TabId，独立 in-memory partition 与 opaque origin，预算（≤ 8 活跃 / 256 MiB / 5 s 首帧 / 5 fps / 100 console error）与三层零 egress 写死，帧 / 输入 / HumanLease 全部复用 ScreenHub / ControlService（§3.3、§10.6、§11.2）。
- **`grok-bot/` 只能"规格先行吸收"**（R116）：读参考 → 在方案里写出状态机 / 不变量 / 错误语义并登记 `source_lineage` → Rust 实现 + 本项目自己的 fixture；禁止逐文件 / 逐函数翻译与文本复制；census 只有 tier-1 文件级 inventory，不进任何分母；v4 不新增任何 Grok Bot 产品能力（§11.5）。
- **parity 与新增必须分开标注**。示例：tool step cap = 8（parity）、`AGENT_STALL_TIMEOUT_MS`（parity）、`OPENBOT_RUN_DEADLINE_MS` 默认 30 min（**新增**）（§7.2）；MCP 四个上限只有 20,000 字符是 parity（§9.1）；memory 页是 31 route 之外的 +1（§3.1）。理由：把新增写成"当前行为"是 v2 审计里最重的一类错误（§28.1 R1）。
- **数据真源**：Rust/PostgreSQL 是 thread、message、run、memory、realtime cursor、run lock 的唯一真源；未设任何 `INTELLIGENCE_*` / `COPILOTKIT_*` 变量时产品必须完整运行；Intelligence 只用于一次性导出导入，不做双写、不留隐藏 fallback（§4.1）。
- **Schema 兼容期只允许 expand**：新表、nullable column、backfill、index、非破坏性 constraint；禁止 drop / rename / 类型收紧 / 主键改写；无 downgrade migration（§14.3）。审计表不做分区，hash chain 以追加 nullable 列落地（§8.6）。
- **环境变量三档** preserve / rename / remove 已在 §15.4 裁决；被 remove 的变量出现在生产配置里必须**启动报错**，禁止"读不到就当没设"。
- **错误语义固定**（§15.3 + R65）：未登录 401；角色不足 403；资源不可见统一 404（防枚举）；policy refusal 403 + stable code；stale generation / request-idempotency / lease 冲突 409；空 thread history 200 + 空列表。文案可本地化，code / status / audit 类型不变。

## 4a. GUI 视觉约束（真源 = 设计系统文档；2026-08-22 三条裁决：自有设计系统 / Tailwind v4 standalone 零 Node / 中英双语）

- **视觉不是 parity 对象**：旅程 / route / 组件行为对上游 parity，外观是本项目自有设计系统；视觉 oracle = 自家 golden 截图（设计系统文档 §10），不是上游截图。v3 G6 的 "web/desktop visual parity" 指同一 bundle 在两宿主一致，已改写为可判定定义（同 bundle 摘要 + 各平台各自 golden，不做跨引擎逐像素比对）。
- **7 条设计原则**（设计系统文档 §3）：chrome 恒中性，零彩色背景 / 边框，唯一实心按钮 = primary；语义色只落文字 / 图标 / 状态点；选中态 = 文字色 + 对勾；卡片无边框不上浮，阴影只有 popover / dialog 两级；图标一律 Lucide 矢量（品牌标唯一例外）；密度偏紧（正文 14/20）；动效只解释状态变化，`prefers-reduced-motion` 下全静止。
- **token 单一来源** `crates/openbot-ui/design/tokens.toml` → 生成 CSS 三块与 Rust 常量；组件只用 token utility，禁止字面颜色 / 任意值 / `dark:` 变体；改 token 必过 `token_contrast_wcag_aa`（文字对背景 ≥ 4.5:1，ring / chart ≥ 3:1）。
- **主题三态** `system`（默认，新增）/ `light` / `dark`：`<html class>` 由 Rust 在首帧改写（Axum 读 cookie、Tauri 读本地设置），`index.html` 零内联脚本。
- **i18n**：`leptos_i18n`，`en` 为源、`zh-CN` 首版；缺键在库里只是 warning，闸门是 `xtask i18n-check`（两份 locale 键集合逐字相等）；文案不进 domain / application，错误以 code 穿越边界后在 GUI 本地化；术语表 `locales/GLOSSARY.md`。
- **a11y** WCAG 2.2 AA 的机械子集（对比度单测 + CDP AX 树检查 + 键盘旅程 + reduced-motion 终态相等）；唯一豁免 = Desktop sandboxed component（v3 §3.3）。
- **上游的 6 个运行时 JS 库**（base-ui / motion / streamdown / prompt-area / boring-avatars / tw-animate-css）全部有替代方案（设计系统文档 §6.3），新增第 7 个即需修订该表。
- 反向 grep 闸门 `xtask design-lint`（禁 `dark:`、禁字面色、语义色不落背景 / 边框、阴影只两级、图标 allowlist 两向零漂移、生产无 `/_design` 画廊）、`css-check`（class 必须是源码字面量）、`bundle-budget`（wasm gz ≤ 3.5 MiB / css ≤ **128 KiB**，120 KiB 警戒只 warning / 字体 ≤ 800 KiB；R123）。
- Batch 15–35 已完成 Web 地基、Approval 可点击竖切、Server/Desktop 偏好、opt-in Tauri、
  27条primitive、46条Lucide mapping、4条layout业务、2条AgentPresence、2共享线稿、生产session
  sign-out/status、channel realtime/detail、独立ChannelRow、Agent roster/AgentCard、RecipientField、
  plain conversation、durable Stop与mount-local queue；Batch48后UI ledger当前87/65。浏览器
  fixture 只作 AX/键盘/视觉/host framing 证据，不能替代 Batch 14 PostgreSQL approval 或 Batch 16
  PostgreSQL preference 生产证据；compile-only `/_design` 也不算生产 route/golden。外部 identity/tauri.conf/binary、其余 route/component/golden 与
  MPL/UNIC/Vet 仍红，完整 G6 仍未勾。

## 5. 发布级不变量（任一违反 = P0，立即停发，不允许风险接受豁免；§17.2）

1. Rust 是 actor、target、policy、approval、capability、audit 的唯一铸造者。
2. 任一 acting effect 之前都有 durable decision + attempt。
3. deny 优先；空 / 坏 / 未知 policy fail-closed。新安装没有隐式 `allow: ["true"]`。
4. browser target 显式，不默认 active tab；snapshot ref 绑定 document generation。
5. profile 不跨 credential principal（`ProfileScope = tenant + bot + credential_principal`，`bot_id` 不够，§10.1）；workspace 不跨 thread/channel。
6. engine restart/reset 使旧 ref、ticket、approval、capability、lease 全失效。
7. human lease 期间 Agent acting **立即拒绝、不排队**。
8. secret 不进模型、GUI state、browser event、普通日志、trace、screen URL。
9. non-idempotent unknown commit 不自动重放。
10. renderer/XSS 不能扩大 Tauri capability；remote content 无 Node/Tauri/Electron API。
11. Server browser 无直连互联网，只经 per-scope egress gateway；`runsc` 在多用户 Server 是强制项，起不来 readiness 就失败。
12. 任一跨 scope 数据 / 帧 / 凭据泄漏是 P0。

## 6. 明确不做（已裁决删除的过度设计，§2.3；提出即需重新立项）

Firecracker / youki · Restate / Temporal 等 durable execution 平台 · Codex AppServer JSON-RPC 全协议复制 · MCP stdio / resources / prompts / tasks / elicitation 与常驻 ConnectionSet · SQLite 第二数据库 · xAI 专用 crate · ACP / Rig / Goose · 全域事件溯源 · Electron + chromiumoxide 两套 driver · 用户 JS 进主 Tauri WebView · 持久化原始 provider stream / HTTP body / screen frame / secret · Playwright 与 CrabCode engine 两套生产实现。

同样**不复活**上游已否决的 customer document index / ACL 索引；`knowledge.sources` 只解析、不执行（§2.2）。

2026-08-28 R115 / R125 追加（§2.3 条 13–16），且对来自任何参考源（`grok-bot` / Codex / Grok Build / CrabCode）的条目同样生效——inventory、吸收或"参考源里有"都不构成重新立项：Docker Desktop / 本地容器 / VM 作为 Desktop 隔离层 · VNC / `<webview>` 作 Screen UI · npm / Node 构建链、Electron `autoUpdater`、`--no-sandbox` / `sandbox:false` / `webviewTag:true` · 依赖 Grok Bot 云后端的能力（cloud agents / forever-box / box-store-sync / managed-setup / webauthn-proxy / host-upgrade / cross-user-sharing / teach-recording）与 Statsig / analytics / telemetry 家族 · **v4 不新增任何 Grok Bot 产品能力**（automations / subagent 面板 / terminal / permission 三态 UI 等只进 §11.5 的无承诺候选表）。

## 7. 上游缺陷不得照译（§2.4）

#36 redirect/DNS rebinding · #44 malformed AG-UI content 崩 UI · #53 credential rotate 孤儿 · #72 空 history 500 · #106 stale grant 复活 · Drive disconnect 未实现 · **`allowed_groups` 从 no-op 变为真控制**（`all` / 具名组 / 空列表三档，单用户模式语义见 §6.5；上游包声明的 channel 对所有人不可达，官方示例用的就是 `[all]`）· MCP 审计在 vendor 调用之后 · **channel 可见性不得 join `intelligence_channel_mappings`**（上游 `list` 的分页段只 join membership、hydration 段多 join 它，两处判据不一致；Intelligence 退役后这个 join 会把 §6.5 刚补上 membership 的包 channel 原样过滤回不可达，等于静默撤销 §6.5 的修复。Rust 版只查 materialized membership，且分页与 hydration 共用同一判据 —— §28.1 R22）。每条的 Rust 版确定语义以 §2.4 表为准，实现时不得"先照译再修"。

## 8. 证据与文档纪律

- 进入代码、commit、文档的每个判断都要有**本轮亲自跑出来的证据**；"应该 / 大概 / 按理说"不是依据。subagent 报的结论必须自己 grep / read 复核并再追一层。
- **每个计数都必须能被一条命令复算**（§28.4 就是范式）；复算不上的数字不写。
- 位置引用用符号名（函数 / 常量 / 文件），方案正文禁止裸行号（§28 审计表里的行号是证据记录，不是契约）。
- "当前行为"断言必须 grep 到上游常量或调用点；"上游没有 X"必须配正向对照（例如 `grep` 命中为空 **且** 同一命令在存在 X 的文件上能命中）。
- 新文档落 `docs/<YYYY-MM-DD>-<主题>-<形态>.md`（日期取本机当天，机器在 America/Los_Angeles）；修订方案必须在 §28 风格的修订记录里写"v_n 表述 / 问题 / 修订 / 证据"四列。
- 本仓 public：写入任何文档前确认不含 CrabCode 内部路径、错误字符串字面量、私有 URL 或凭据；CrabCode 相关表述的粒度不得超过方案 §1.2 / §11.4 已有水平。

## 9. 来源、许可与品牌（§23）

- OpenBot 上游 MIT，衍生实现保留 `Copyright (c) 2026 CopilotKit` 与 MIT 文本；Codex / Grok Build 为 Apache-2.0，复制文件保留 SPDX、来源 commit、修改声明；Grok Build 里源自 Codex/OpenCode 的工具必须回溯原始来源。**`grok-bot/` 与 Grok Build 是两个不同的东西**：前者是 Anysphere（Cursor）Grok Bot 0.18.0 的反编译重建，无上游源码许可（其 NOTICE 自述），Apache-2.0 不覆盖它；用户已裁决权利状态不作为技术计划的阻断项，风险登记在方案 §23.1 条 8，方法固定为规格先行吸收、不翻译不复制、每次吸收记 `source_lineage`、原始安装包不入仓（§11.5 / R116）。
- **CrabCode 是闭源专有软件**：每个复制文件须有 `SOURCE_PROVENANCE`（权利人、原路径、上游 commit、原/目标 hash、许可证、修改声明、书面授权编号）；workspace 里的 `license = MIT` 是元数据，不等于授权。无授权只能按行为 clean-room 重写（§11.4）。
- **R191用户已裁决本项目走开源共建方向**；根LICENSE/Cargo/SPDX在完成书面发布范围与whole-tree license audit前仍维持原实际许可。只复制自有模块的授权不自动公开整个源项目，不擅自授予第三方权利（§23.2）。
- native thread/memory/realtime 只能依据 OpenBot MIT 源码、开放协议与黑盒可观察契约 clean-room 实现；不得把 Intelligence 私有响应或反编译结果当源码（§23.3）。
- 对外名称、bundle ID、domain、deep-link scheme 不得含 OpenBot / CopilotKit / Codex / OpenAI / Grok / xAI；内部代号 `openbot-rs`。禁止复用 CrabCode 的 updater key、bundle ID、证书、OAuth client（§16.2）。

## 10. 闸门

CI 固定（§16.3）：`cargo fmt --check` · `cargo clippy --all-targets --all-features -D warnings` · `cargo test --locked` · `cargo deny` · `cargo audit` · `cargo vet` · OSV / secret scan · license / NOTICE / provenance 校验 · SBOM · 可复现构建 · 签名校验。`Cargo.lock` 与 engine lockfile 提交；git 依赖必须钉 commit；核心 crate `unsafe_code = "deny"`。

完整本机闸门的单一入口 = `cargo xtask ci`（fmt → clippy → `cargo test --locked` → safe-dialer guard → SAML/xmlsec guard → parity-check → recount，7 段）。驱动器**必须**建在 `target-xtask/`（`.cargo/config.toml` 的 alias 已配 `--target-dir`），与子构建的 `target/` 互不包含：否则第 3 步会去重链正在运行的驱动器自己，Windows 报 `os error 5` 恒红、Linux 恒绿（§28.1 R25）。摆放错了 `cmd_ci` 当场拒跑并打印两条路径，不会退化成"某台机器上能过"。**R63 当前操作覆盖：GitHub Actions manual-only 且未经用户重新授权不得派发；也不运行 `cargo xtask ci`，改为按变更面执行本机定向测试。**

Engine / 参考树闸门（R116 / R117 / R125 / R127 / R128 / R154）：`cargo xtask engine fetch|verify|protocol|bundle|runsc-spike`、`cargo xtask postgres fetch-source|verify-source`、`electron-shim-check`、`grok-inventory --check` 与 v4 overlay 已落；工作区（排除只读 `grok-bot/`）当前 `package.json` 恰 1，键集合固定且零 dependencies/scripts/lockfile。macOS Engine bundle verify 已覆盖 raw zip、`--version`、ASAR/header+block integrity、九 fuse、rebrand、signature、release epoch、协议 hash 与 manifest；双 role host conformance 另证 renderer OS sandbox/no-listener/no-orphan。PostgreSQL只钉PGDG 17.11 source与runtime manifest/lock契约，平台binary build/sign/process尚未闭合。Windows PE/pipe/Job与Linux runsc **代码和真机命令均已落，但只取得macOS上的target check/Clippy**；两平台runtime evidence仍是P1硬缺口。静态跨平台编译不能替代真机证据，runsc spike未PASS前禁止写版本pin。

GUI 另加（设计系统文档 §15）：`cargo xtask tools verify` · `cargo test -p openbot-ui` · `xtask i18n-check` · `xtask design-lint` · `xtask css-check` · `xtask bundle-budget` · `bash tools/check-ui-dependencies.sh` · `bash tools/check-tauri-dependencies.sh` · `bash tools/check-postgres-key-store-dependencies.sh` · `bash tools/check-deny-release-targets.sh` · golden 截图（Web 110 张 / Desktop 每平台 54 张，差异像素 ≤ 0.1% 且无 8×8 全差异块；更新只能随 PR 附 diff 图人工批准）· CDP AX 树检查。Batch81 Cargo Vet target-aware 为 macOS272/Windows269 unvetted，未获明确授权不得批量exemption；MPL/UNIC红灯也不得写绿。

Go/No-Go 走 G0–G8（§24），**任何闸门失败只能修复后重跑，不能以"后续补齐"进入下一阶段**。DoD 十条见 §25——没有 parity ledger 100% 归类、跨 scope 泄漏 = 0、audit-before-action 违规 = 0，不得宣称"全量完成"。

## 11. 协作约定

- **中文**沟通、报告、commit 主题；标识符 / 路径 / 命令原样。commit 主题 `type(scope): 一句话 —— 根因或理由`。
- 分支 `docs|feat|fix/<YYYY-MM-DD>-<主题>`；交付 = push 分支 + 开 PR + 停在移交；合并用 **merge commit**（不 squash / rebase），保留原 commit 可追溯。push 前 `git remote -v` 确认目标是 `acosmi/OpenBot`。
- 实施型任务做到底；合法停止只有两种：用户叫停、撞到需用户裁决的真分歧（设计多选一 / 不可逆或对外动作 / 超出授权）。
- 子代理只写码、不碰 git；其结论主控亲自复核。
