# OpenBot parity ledger —— GUI 视觉面（设计系统文档 §11 点名的 `parity/ui.yaml`）。
#
# 五组 entry，编号连续，前缀固定 T-UI：
#
#   (a) 21 个上游 `app/src/components/ui/*.tsx` 原语 → `openbot_ui::primitives`（§6.1 表逐行）  T-UI-0001..0021
#   (a2) §6.1 末尾点名的 6 个新增原语（上游 primitives 层零对应物）                              T-UI-0022..0027
#   (b) 45 个上游业务组件 → `openbot_ui::features::<组>`（§6.2 十组 + 根）                        T-UI-0028..0072
#   (c) 47 个上游 Tabler 图标 → Lucide 1.33.0 名映射（46 Lucide + 1 品牌标，§4.6.2）              T-UI-0073..0119
#   (d) 6 个运行时 JavaScript 库 → Rust/CSS 替代（§6.3）                                          T-UI-0120..0125
#   (e) 27 个 golden 页面（26 上游 route 页 + memory 页，§10.1）                                   T-UI-0126..0152
#
# 合计 21 + 6 + 45 + 47 + 6 + 27 = 152 条。主控给的"146"是 21+45+47+6+27，不含 (a2) 的 6 条
# 新增原语；主控原话"这 6 条在 146 之外，最终条数写实即可"，故此处写实 152。
#
# ---------------------------------------------------------------------------
# label 判据（CLAUDE.md §4 硬要求，先写死再逐条套用，不许实施期临场解释）
# ---------------------------------------------------------------------------
#
#   parity = 上游有同名 / 同职责的对应物，本项目保持其能力与命名。
#            **实现语言从 React 换成 Leptos 不改变 label** —— 语言替换由 (d) 组的 6 个运行时库
#            各记一条 `替代` 统一承担。否则 152 条会整体塌成 `替代`，label 失去分辨力，
#            "把新增写成当前行为"的反面（把 parity 全写成替代）同样让这份台账不可用。
#   替代   = 上游有对应物，但本项目**明确换了形态 / 换了名字 / 收敛了能力**：
#            §6.2 点名的 7 处（abstract-avatar / orb×2 / composer / stagger / settings-background /
#            app-sidebar / provider-logo / theme-provider）、§4.6.2 的 47 个图标改名、§6.3 的 6 个库。
#   新增   = 上游第一方源零对应物。每条都配了正向对照命令（同一条 grep 在确实存在的东西上能命中），
#            否则"上游没有 X"在"这条命令根本没扫到那里"的世界里同样为真。
#
# ---------------------------------------------------------------------------
# status
# ---------------------------------------------------------------------------
#
# Batch 15–17 已有真实 Leptos/Trunk/WASM 实现，目前只对机器证据闭合的 14 条原语打勾：
# EmptyState/Badge/ThemeToggle/LocaleSwitch + Batch17 十条基础原语；其余即使已有部分源码也
# 保持 todo，不能用 Approval/preferences/design-gallery 冒充 27 原语、45 业务组件或 27 页
# golden 全量完成。
#
# ---------------------------------------------------------------------------
# 未验证项（主控复核用，不得当成已核实）
# ---------------------------------------------------------------------------
#
#   U1. Lucide 1.33.0 zip/许可已在 Phase 0 以精确 size/SHA 记录到 provenance/NOTICE；Batch 15
#       的 design-lint 与 build.rs 又证明 74 个 manifest/SVG 两向相等且形状安全。但 (c) 组
#       暂不批量打勾：46 个映射仍需 ledger→icons.toml 的机械逐条 join，品牌标仍缺原件。
#   U2. 28 个新增图标已进入同一 74 项 allowlist 并由 design-lint 实证；本 ledger 本来就不
#       逐条登记它们。该事实不能替代 U1 剩余的 46 条映射 join 与 Google Drive 品牌标。
#   U3. golden PNG 一张都还不存在（`fixtures/ui/` 目录未建），(e) 组的 evidence 只覆盖
#       "上游确实有这个 route 文件"，不覆盖"golden 已生成"。

schema: "ui"
schema_version: 1
upstream_commit: "891df72f1827454d8b353d108fe5dd2313b7e30d"
generated_by: "manual"

recount:
  # §17 第 1 条：31 个 route 文件 = 26 页 + 5 layout
  - command: "find app/src/routes -name '*.tsx' | wc -l"
    cwd: "upstream"
    expect: 31
  # §17 第 2 条：26 个页面 route，(e) 组前 26 条逐条对应
  - command: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l"
    cwd: "upstream"
    expect: 26
  # §17 第 3 条：21 个原语，(a) 组逐条对应
  - command: "ls app/src/components/ui | wc -l"
    cwd: "upstream"
    expect: 21
  # §17 第 4 条：45 个业务组件，(b) 组逐条对应
  - command: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l"
    cwd: "upstream"
    expect: 45
  # §17 第 6 条：47 个不同 Tabler 图标，(c) 组逐条对应
  - command: "grep -rhoE '\\bIcon[A-Z][A-Za-z0-9]+' --include=*.tsx --include=*.ts app/src | sort -u | wc -l"
    cwd: "upstream"
    expect: 47
  # §1 实测表的 className 数；(a)+(b) 覆盖面的规模锚点
  - command: "grep -rhoE 'className=' --include=*.tsx app/src | wc -l"
    cwd: "upstream"
    expect: 852
  # §17 倒数第 2 条：六库各自的引用文件数，(d) 组 5 条 + (c) 组作用域锚点
  - command: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done"
    cwd: "upstream"
    expect: "13 8 4 3 1 32 "
  # tw-animate-css 是 CSS `@import` 不是 JS `from`，所以上一条的 for 循环照不到它；(d) 第 6 条靠这条
  - command: "grep -c 'tw-animate-css' app/src/styles.css"
    cwd: "upstream"
    expect: 1
  # (a2) 6 个新增原语在上游 primitives 层零命中
  - command: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l"
    cwd: "upstream"
    expect: 0
  # 上一条的正向对照：同一条 grep 加上确实存在的 button.tsx 就命中 1，证明它扫到了那个目录
  - command: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale|button' | wc -l"
    cwd: "upstream"
    expect: 1
  # (a2) Avatar 的第二重对照：primitives 层零 avatar，但业务层有 2 个（agents/abstract-avatar + channels/avatar），(b) 组把它们各记一条 `替代`
  - command: "find app/src/components -name '*avatar*.tsx' | wc -l"
    cwd: "upstream"
    expect: 2
  # §17：上游零 i18n 框架，LocaleSwitch 因此是新增
  - command: "grep -rlE 'useTranslation|i18next|react-intl|next-intl|<Trans\\b' --include=*.tsx --include=*.ts app/src | wc -l"
    cwd: "upstream"
    expect: 0
  # 上一条的正向对照：同一条命令加一个确实存在的符号就命中 38 个文件
  - command: "grep -rlE 'useTranslation|i18next|react-intl|next-intl|<Trans\\b|useQuery' --include=*.tsx --include=*.ts app/src | wc -l"
    cwd: "upstream"
    expect: 38
  # §17：上游不跟随系统主题，ThemeToggle 的 system 第三态因此是新增
  - command: "grep -E 'prefers-color-scheme' app/src/lib/theme.ts app/src/components/theme-provider.tsx app/src/styles.css | wc -l"
    cwd: "upstream"
    expect: 0
  # 上一条的正向对照：同一个 styles.css 里 prefers-reduced-motion 命中 1，证明 media query 确实扫得到
  - command: "grep -c 'prefers-reduced-motion' app/src/styles.css"
    cwd: "upstream"
    expect: 1
  # 设计系统文档 §17 自检第 1 条：§4.6.2 映射表 47 行，与上游实测 47 相等
  - command: "grep -oE '\\| Icon[A-Za-z0-9]+ \\|' 'docs/2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md' | sort -u | wc -l"
    cwd: "repo"
    expect: 47
  # 设计系统文档 §17 自检第 2 条：28 个新增图标（本 ledger 不逐条登记，见头部 U2）
  - command: "grep -E '^新增图标：' 'docs/2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md' | grep -oE '`[a-z0-9-]+`' | wc -l"
    cwd: "repo"
    expect: 28
  # 本文件 entry 总数 = 21 + 6 + 45 + 47 + 6 + 27
  - command: "grep -c '^  - id: ' parity/ui.yaml"
    cwd: "repo"
    expect: 152
  # label 分布 parity = 21 原语 + 35 同名业务组件 + 26 上游 golden 页 = 82
  - command: "grep -c '    label: \"parity\"' parity/ui.yaml"
    cwd: "repo"
    expect: 82
  # label 分布 替代 = 10 业务组件（abstract-avatar / agent-orb / ai-core / app-sidebar / provider-logo / channels-avatar / composer / stagger / settings-background / theme-provider）+ 47 图标 + 6 运行时库 = 63
  - command: "grep -c '    label: \"替代\"' parity/ui.yaml"
    cwd: "repo"
    expect: 63
  # label 分布 新增 = 6 新增原语（Toast/Badge/Kbd/Avatar/ThemeToggle/LocaleSwitch）+ 1 memory 页 = 7
  - command: "grep -c '    label: \"新增\"' parity/ui.yaml"
    cwd: "repo"
    expect: 7
  # Batch 17 精确完成 14 条；其余部分实现或完全未实现均保持 todo
  - command: "grep -c '    status: \"todo\"' parity/ui.yaml"
    cwd: "repo"
    expect: 138
  - command: "grep -c '    status: \"done\"' parity/ui.yaml"
    cwd: "repo"
    expect: 14
  - command: "grep -c '^    done_evidence: ' parity/ui.yaml"
    cwd: "repo"
    expect: 14
  # 152 条 × 9 个缩进键（8 必填 + notes；第 9 个必填键 id 写在 `  - id: ` 行，由上一条数）= 1368；任何键缺失会当场对不上
  - command: "grep -cE '^    (upstream|label|target|owner|test_id|migration_rule|status|evidence|notes): ' parity/ui.yaml"
    cwd: "repo"
    expect: 1368
  # 152 个 test_id 去重后仍是 152 ⇒ 文件内零重复（schema v1 规则 5 的本文件那一半）
  - command: "grep -oE '^    test_id: \"T-UI-[0-9]{4}\"' parity/ui.yaml | sort -u | wc -l"
    cwd: "repo"
    expect: 152

entries:
  # =======================================================================
  # (a) 21 个上游 components/ui 原语 → openbot_ui::primitives（设计系统文档 §6.1 表逐行）
  # =======================================================================

  - id: "prim-button"
    upstream: "app/src/components/ui/button.tsx"
    label: "parity"
    target: "openbot_ui::primitives::button::Button"
    owner: "openbot-ui"
    test_id: "T-UI-0001"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "openbot_ui::primitives::button::Button 以闭集实现 chip/primary/ghost/danger-text、sm/md/lg 与 default/hover/focus-visible/active/disabled/loading/invalid/selected/open；selected/open/invalid/busy 显式映射 ARIA，callback 不暴露 MouseEvent。真实 Chromium 对同一 button 的 Enter/Space 得 count 0→1→2，loading/disabled 不可激活；Rust closed-state test、UI all-features 30/0/0、WASM/Clippy/gates 全绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG button；Space/Enter 激活。状态子集：全部 9 态（default/hover/focus-visible/active/disabled/loading/invalid/selected/open）。替代的无头库 = base-ui Button。variant `chip`（默认）/ `primary` / `ghost` / `danger-text`，size `sm/md/lg`。GUI 裁决：主按钮唯一合法实心色 `--accent-black`，彩色背景与彩色边框一律禁止。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-combobox"
    upstream: "app/src/components/ui/combobox.tsx"
    label: "parity"
    target: "openbot_ui::primitives::combobox::Combobox"
    owner: "openbot-ui"
    test_id: "T-UI-0002"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG combobox + listbox；↑↓ Home End Enter Esc、typeahead。状态子集：default/focus/open/disabled/invalid。替代的无头库 = base-ui Combobox。Composer 的 `@coworker` / `/skill` 弹层复用它（§6.5），键盘模式必须与独立使用时逐条相同。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-dialog"
    upstream: "app/src/components/ui/dialog.tsx"
    label: "parity"
    target: "openbot_ui::primitives::dialog::Dialog"
    owner: "openbot-ui"
    test_id: "T-UI-0003"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG modal dialog；焦点陷阱、Esc 关闭、关闭后焦点归还触发元素。状态子集：open。替代的无头库 = base-ui Dialog。焦点归还触发元素这条是 a11y 闸门（§9.2）的机械判据之一，不是「尽量」。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-dropdown-menu"
    upstream: "app/src/components/ui/dropdown-menu.tsx"
    label: "parity"
    target: "openbot_ui::primitives::menu::Menu"
    owner: "openbot-ui"
    test_id: "T-UI-0004"
    migration_rule: "rename: dropdown-menu → Menu"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG menu button；↑↓ Home End Enter Esc、typeahead、子菜单 → ←。状态子集：open/disabled。替代的无头库 = base-ui Menu。改名而非改能力，故 label 仍是 parity。子菜单的 → ← 是 APG 要求，上游 base-ui 已实现，替代实现必须补齐。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-empty"
    upstream: "app/src/components/ui/empty.tsx"
    label: "parity"
    target: "openbot_ui::primitives::empty_state::EmptyState"
    owner: "openbot-ui"
    test_id: "T-UI-0005"
    migration_rule: "rename: empty → EmptyState"
    status: "done"
    done_evidence: "openbot_ui::primitives::empty_state::EmptyState 为纯展示 section+h2+p，调用方必须给 stable heading id，避免原语级重复 ID；真实浏览器审批清空后呈现命名 region，DOM audit 重复 ID=0、标题跳级=0。UI 定向测试 19/0/0、WASM/Clippy 绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：纯展示，无键盘交互。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。改名理由：`empty` 在 Rust 里作模块名太泛；能力不变，label 仍是 parity。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-field"
    upstream: "app/src/components/ui/field.tsx"
    label: "parity"
    target: "openbot_ui::primitives::field::Field"
    owner: "openbot-ui"
    test_id: "T-UI-0006"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "Field 以 scoped Provider 单点派生 control/description/error ID，避免 sibling context shadow；nested Input/Textarea/Switch 自动得到 id、aria-describedby、aria-invalid 与 disabled。Chromium 实得 labelFor=design-name、describedBy=design-name-description；invalid Textarea 只指向可见 design-notes-error(role=alert)，duplicate id=0。非法/可拆 ARIA token 的 control_id 负向 panic test 绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：label + 控件 + 说明 + 错误；`aria-describedby` / `aria-invalid` 自动接线。状态子集：invalid/disabled。上游此原语无无头库依赖（自绘）。自动接线是 §9.3 编码规则的落点：控件不允许自己手写 aria-describedby，一律由 Field 生成 id 并绑定，否则 a11y 闸门在「两处各写一遍」的漂移里恒绿。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-input-group"
    upstream: "app/src/components/ui/input-group.tsx"
    label: "parity"
    target: "openbot_ui::primitives::input_group::InputGroup"
    owner: "openbot-ui"
    test_id: "T-UI-0007"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "InputGroup + closed Prefix/Suffix affix 保持焦点在内层控件；CSS 同时支持真实 :focus-within 与 data-state~focus-within。Chromium 点击 design-search 后 activeId 精确为 design-search、group outline=2px；compile-time gallery forced state 与 UI/WASM/Clippy/css-check 全绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：前后缀槽；焦点落在内层控件上。状态子集：focus-within。上游此原语无无头库依赖（自绘）。`focus-within` 是唯一状态，靠 CSS `:focus-within` 与 `data-state~=\"focus-within\"` 双通道，使 §10.3 设计画廊能静态渲染该态。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-input"
    upstream: "app/src/components/ui/input.tsx"
    label: "parity"
    target: "openbot_ui::primitives::input::Input"
    owner: "openbot-ui"
    test_id: "T-UI-0008"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "controlled Input 只接受闭集 text/email/password/search/url；Field context 自动接线，standalone 要 id/aria-label；invalid/disabled/focus 有 explicit aria/data state。Chromium fill 后 value=Ada Lovelace、aria-invalid=false、description exact；另一个 Field-disabled Input 原生 disabled=true，search input 可命名且获得真 focus。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG textbox。状态子集：focus/disabled/invalid。替代的无头库 = base-ui Input。invalid 态由 Field 下传 `aria-invalid`，不由 Input 自己判断。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-item"
    upstream: "app/src/components/ui/item.tsx"
    label: "parity"
    target: "openbot_ui::primitives::item::Item"
    owner: "openbot-ui"
    test_id: "T-UI-0009"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "ItemAction 只有 bounded same-origin absolute Link 或 unit-callback Button，scheme-relative/external path 负向拒绝；compound media/title/description/actions slot 与 automatic selected check。真实 Chromium Button Enter/Space count 0→1→2，disabled 不增；selected state=hover selected、check aria-hidden=true、nested interactive=0。selected 本身只改文字/图标，对 forced hover 才换 neutral surface。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：列表行；可渲染为 link 或 button，语义随之切换。状态子集：hover/selected/disabled。上游此原语无无头库依赖（自绘）。selected 只由文字色 + 图标色 + 对勾表达（GUI 裁决），不许上背景色或边框。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-label"
    upstream: "app/src/components/ui/label.tsx"
    label: "parity"
    target: "openbot_ui::primitives::label::Label"
    owner: "openbot-ui"
    test_id: "T-UI-0010"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "Label 只渲染 semantic label[for]，Field 是关联 ID 唯一来源。真实 Chromium 三组 label 分别 exact 绑定 design-name/design-notes/design-switch；AX snapshot 中得到 textbox「显示名称/备注」与 switch「启用设置」，unnamed focusable=0。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`for` 绑定到控件 id。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。id 由 Field 铸造并同时交给 Label 与控件，避免两处各生成一个。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-message-scroller"
    upstream: "app/src/components/ui/message-scroller.tsx"
    label: "parity"
    target: "openbot_ui::primitives::message_scroller::MessageScroller"
    owner: "openbot-ui"
    test_id: "T-UI-0011"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`role=\"log\"`、`aria-live=\"polite\"`。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。自动贴底 + \"回到底部\"胶囊 + 新消息时保持阅读位置。上游用 `IconArrowDown`（见 T-UI-0074），替代实现用 Lucide `arrow-down`。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-message"
    upstream: "app/src/components/ui/message.tsx"
    label: "parity"
    target: "openbot_ui::primitives::message::Message"
    owner: "openbot-ui"
    test_id: "T-UI-0012"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`article` 语义。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。Bot 正文的 markdown 渲染不在本条，见 (d) 组 streamdown → pulldown-cmark + syntect。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-bubble"
    upstream: "app/src/components/ui/bubble.tsx"
    label: "parity"
    target: "openbot_ui::primitives::bubble::Bubble"
    owner: "openbot-ui"
    test_id: "T-UI-0013"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：纯展示外框，无独立语义。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。用户 / Bot 两种气泡外框；卡片无边框、hover 只换底（GUI 裁决）。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-select"
    upstream: "app/src/components/ui/select.tsx"
    label: "parity"
    target: "openbot_ui::primitives::select::Select"
    owner: "openbot-ui"
    test_id: "T-UI-0014"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG select-only combobox；↑↓ Home End Enter Esc、typeahead。状态子集：default/focus/open/disabled/invalid。替代的无头库 = base-ui Select。上游用 `IconChevronUp` + `IconSelector`（见 T-UI-0086 / T-UI-0109），替代实现用 `chevron-up` + `chevrons-up-down`。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-separator"
    upstream: "app/src/components/ui/separator.tsx"
    label: "parity"
    target: "openbot_ui::primitives::separator::Separator"
    owner: "openbot-ui"
    test_id: "T-UI-0015"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "SeparatorOrientation 闭集 horizontal/vertical；decorative 时 role/orientation 均无且 aria-hidden=true，semantic 时 role=separator 并显式 aria-orientation。Chromium/AX 实得 decorative 0 landmark、horizontal/vertical 两条 semantic separator；Rust enum test 与 token-only CSS gates 绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`role=\"separator\"`（装饰用途时 `aria-hidden`）。状态子集：无状态子集。替代的无头库 = base-ui Separator。装饰性分隔线必须 `aria-hidden`，否则屏幕阅读器读出一串无意义的 separator。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-sheet"
    upstream: "app/src/components/ui/sheet.tsx"
    label: "parity"
    target: "openbot_ui::primitives::sheet::Sheet"
    owner: "openbot-ui"
    test_id: "T-UI-0016"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：同 Dialog（焦点陷阱、Esc 关闭、焦点归还）。状态子集：open。上游此原语无无头库依赖（自绘）。侧向 dialog；Sidebar 在窄视口下折叠成 Sheet 形态（见 T-UI-0017）。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-sidebar"
    upstream: "app/src/components/ui/sidebar.tsx"
    label: "parity"
    target: "openbot_ui::primitives::sidebar::Sidebar"
    owner: "openbot-ui"
    test_id: "T-UI-0017"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`nav` landmark；`aria-current=\"page\"`。状态子集：collapsed。替代的无头库 = shadcn sidebar。折叠 / rail / Sheet 三形态。上游业务层的 `app-sidebar` / `admin-sidebar` / `settings-sidebar` 都往它里面填内容——`app-sidebar` 那条按 §6.2 记 `替代`（见 T-UI-0037），另两条同名记 parity。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-skeleton"
    upstream: "app/src/components/ui/skeleton.tsx"
    label: "parity"
    target: "openbot_ui::primitives::skeleton::Skeleton"
    owner: "openbot-ui"
    test_id: "T-UI-0018"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "SkeletonShape 闭集 line/block/circle，三者真实 DOM 均 aria-hidden=true、AX snapshot 零 skeleton；normal-motion Chromium animationName=ob-skeleton-pulse。Rust CSS contract test 同时钉 prefers-reduced-motion 段含 transition-duration:0ms!important 与 animation:none!important；无信息仅靠动画表达。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：`aria-hidden`（加载占位不进无障碍树）。状态子集：无状态子集。上游此原语无无头库依赖（自绘）。动画在 reduced-motion 下必须静止（§4.5），否则 golden 在两种 media 下不确定。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-switch"
    upstream: "app/src/components/ui/switch.tsx"
    label: "parity"
    target: "openbot_ui::primitives::switch::Switch"
    owner: "openbot-ui"
    test_id: "T-UI-0019"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "Switch 为 controlled native button role=switch，aria-checked 永远显式 true/false；Space handler preventDefault 后恰切一次，Field 可自动接线 id/description/invalid/disabled。Chromium Space 得 false→true，disabled+checked 样本仍 aria-checked=true 且不可用；AX name=启用设置，track/thumb 只用 neutral token。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG switch；Space 切换。状态子集：checked/disabled。替代的无头库 = base-ui Switch。语义色只落文字 / 图标 / 状态点，开关本体保持中性（GUI 裁决）。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-textarea"
    upstream: "app/src/components/ui/textarea.tsx"
    label: "parity"
    target: "openbot_ui::primitives::textarea::Textarea"
    owner: "openbot-ui"
    test_id: "T-UI-0020"
    migration_rule: "preserve"
    status: "done"
    done_evidence: "controlled Textarea 是 multiline textbox，Field 自动接线；Rust/WASM 以真实 scrollHeight 设置 inline height，跨引擎 max-height 取 tokens.toml 唯一 --size-textarea-max=218px（十行×20 + padding16 + border2），不用不兼容 calc 乘法。Chromium 输入16行得 client=216/max=218/scroll=374、capped=true；invalid/error/disabled/focus 与 UI/WASM/Clippy 全绿。"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG textbox multiline。状态子集：focus/disabled/invalid。上游此原语无无头库依赖（自绘）。自动增高上限 10 行。Composer 内嵌它（§6.5），输入法组合期间 Enter 不发送那条是 Composer 的新增行为，不在本条。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  - id: "prim-tooltip"
    upstream: "app/src/components/ui/tooltip.tsx"
    label: "parity"
    target: "openbot_ui::primitives::tooltip::Tooltip"
    owner: "openbot-ui"
    test_id: "T-UI-0021"
    migration_rule: "preserve"
    status: "todo"
    evidence: "ls app/src/components/ui | wc -l = 21（上游 891df72f 干净克隆，本轮实跑）"
    notes: "键盘 / ARIA（WAI-ARIA APG）：APG tooltip；hover/focus 显示、Esc 隐藏、延迟 400ms。状态子集：open。替代的无头库 = base-ui Tooltip。400ms 延迟是确定值，golden 截图前必须等过它或强制 `data-state=\"open\"`，否则截图不确定。 状态由 `data-state` 驱动，CSS 以 `&:is(:hover, [data-state~=\"hover\"])` 同时响应真实交互与强制态，使 §10.3 设计画廊能静态渲染每个状态（否则 hover/active 这类态在 golden 里根本拍不到）。"

  # =======================================================================
  # (a2) §6.1 末尾的 6 个新增原语 —— 上游 primitives 层零对应物（正向对照见 recount 第 9/10 条）
  # =======================================================================

  - id: "prim-new-toast"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::toast::Toast"
    owner: "openbot-ui"
    test_id: "T-UI-0022"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "todo"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1"
    notes: "键盘 / ARIA：`role=\"status\"`（不是 alert：非阻塞反馈不该打断屏幕阅读器当前朗读），5s 自动消失，可手动关闭。 用于一切 `accepted:false` 类用户可见反馈——「静默失败」在这套 UI 里是缺陷不是省事。5s 与 `role` 都是确定值，golden 里必须靠 `data-state` 强制而不是等真定时器。"

  - id: "prim-new-badge"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::badge::Badge"
    owner: "openbot-ui"
    test_id: "T-UI-0023"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "done"
    done_evidence: "BadgeTone 封闭 neutral/caution/success/info/danger 五值，渲染 visible text + aria-hidden 状态点，语义色只落文字/currentColor、背景/边框保持中性；tone 单测 + tokens.toml 84 对 WCAG AA 对比度测试通过，Approval 浏览器卡片实显 caution。"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1"
    notes: "键盘 / ARIA：纯展示，无键盘交互。 纯文字状态；语义色（danger / caution / success / info）只落文字与状态点，永不落背景与边框（GUI 裁决）。这条与 §4.2 的对比度单测绑定：文字色对 bg / bg-chip 必须 ≥ 4.5:1。"

  - id: "prim-new-kbd"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::kbd::Kbd"
    owner: "openbot-ui"
    test_id: "T-UI-0024"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "todo"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1"
    notes: "键盘 / ARIA：`<kbd>` 语义，无交互。 渲染快捷键字面（`Enter` / `Shift+Enter` / `Esc` / `⌘K`）。跨平台修饰键名由 i18n + 平台探测决定，不 hardcode 单一平台写法。"

  - id: "prim-new-avatar"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::avatar::Avatar"
    owner: "openbot-ui"
    test_id: "T-UI-0025"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "todo"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1；另 find app/src/components -name '*avatar*.tsx' | wc -l = 2（业务层确实有，原语层确实没有）"
    notes: "键盘 / ARIA：`img` 或首字母文本 + `aria-label`；无交互。 §6.6。上游 primitives 层零 avatar（本条 evidence），但业务层有 2 个（find app/src/components -name '*avatar*.tsx' | wc -l = 2），它们各自在 (b) 组记一条 `替代`（T-UI-0029 abstract-avatar、T-UI-0040 channels/avatar）。本条 label=新增 的作用域是**原语层**，不是「OpenBot 从来没有头像」——两者不冲突，但混起来就是把新增写成当前行为。确定性：同一 principal_id 在任何平台任何时间得到同一头像（SHA-256 前 8 位 → 8 个预设中性低饱和底色），golden 依赖这条。"

  - id: "prim-new-theme-toggle"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::theme_toggle::ThemeToggle"
    owner: "openbot-ui"
    test_id: "T-UI-0026"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "done"
    done_evidence: "真实 Chromium/WASM：APG radiogroup 名称经 i18n；system/light/dark 的 aria-checked 显式 true/false、roving tabindex 0/-1；Arrow 四向循环、Home/End 均即时切换 html class 并把焦点移到选项。Trunk strict-CSP bundle 外部脚本 1/内联 0。偏好持久化不归本新增 control 条目，仍由 T-UI-0072 theme 模块保持 todo。"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1；另 grep -E 'prefers-color-scheme' app/src/lib/theme.ts app/src/components/theme-provider.tsx app/src/styles.css | wc -l = 0，正向对照 grep -c 'prefers-reduced-motion' app/src/styles.css = 1"
    notes: "键盘 / ARIA：APG radiogroup 或 menu button（三选一）；`aria-label` 经 i18n。 §7 三态 `system` / `light` / `dark`。上游只有手动 light/dark（`lib/theme.ts::THEME_STORAGE_KEY = \"openbot-theme\"` + `<html class=\"dark\">`），`prefers-color-scheme` 在 lib/theme.ts / theme-provider.tsx / styles.css 三处合计 0 命中（recount 第 14 条），正向对照 prefers-reduced-motion 在 styles.css 命中 1（recount 第 15 条）⇒ 命令确实扫得到 media query。所以 light/dark 两态是 parity（由 T-UI-0072 theme-provider → theme 模块那条承担），`system` 第三态是本条的新增部分。图标 `sun` / `moon` / `sun-moon` 属 §4.6.2 的 28 个新增图标（见头部 U2）。"

  - id: "prim-new-locale-switch"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::primitives::locale_switch::LocaleSwitch"
    owner: "openbot-ui"
    test_id: "T-UI-0027"
    migration_rule: "n/a: 上游 primitives 层零对应物"
    status: "done"
    done_evidence: "真实 Chromium/WASM：trigger 的 aria-expanded 显式 false/true，Enter/Space/ArrowDown/ArrowUp 打开并聚焦当前语言；menuitemradio 的 aria-checked 显式 true/false、aria-current 只落当前项；Arrow/Home/End 移动焦点，Enter/Space 选择，Escape 关闭并把焦点还给 trigger。选择 zh-CN/en 即时更新 html lang；Batch16 经 typed partial PUT 持久化，reload immediate+settled 均保持所选 locale，失败有本地化 role=alert。UI 19/0/0、WASM/Clippy 绿。"
    evidence: "ls app/src/components/ui | grep -E 'toast|badge|kbd|avatar|theme|locale' | wc -l = 0；正向对照 ls app/src/components/ui | grep -E '…|button' | wc -l = 1；另 i18n 框架 0 命中 / 正向对照 38"
    notes: "键盘 / ARIA：APG menu button；当前语言 `aria-current`。 §8。上游零 i18n 框架命中（grep -rlE 'useTranslation|i18next|react-intl|next-intl|<Trans\\b' … = 0），正向对照同一条命令加 `useQuery` 后命中 38 个文件 ⇒ 作用域确实覆盖到了 app/src。字符串全部硬编码英文，所以整个 i18n 面（含本原语）都是新增，不存在 parity 面。图标 `languages` 属 28 个新增图标。"

  # =======================================================================
  # (b) 45 个上游业务组件 → openbot_ui::features::<组>（设计系统文档 §6.2 十组 + 根，合计 1+8+2+1+8+5+8+4+2+4+2）
  # =======================================================================

  - id: "biz-admin-admin-sidebar"
    upstream: "app/src/components/admin/admin-sidebar.tsx"
    label: "parity"
    target: "openbot_ui::features::admin::admin_sidebar"
    owner: "openbot-ui"
    test_id: "T-UI-0028"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `admin`（该组上游文件数 1）。组 admin 唯一一个。内容填进 `openbot_ui::primitives::sidebar::Sidebar`（T-UI-0017）。上游用 IconArrowLeft / IconBuildingBank / IconCode / IconDeviceDesktop / IconFileText / IconKey / IconLayoutGrid / IconListDetails / IconPuzzle / IconShieldCheck / IconUsers，全部经 (c) 组映射。"

  - id: "biz-agents-abstract-avatar"
    upstream: "app/src/components/agents/abstract-avatar.tsx"
    label: "替代"
    target: "openbot_ui::primitives::avatar::Avatar"
    owner: "openbot-ui"
    test_id: "T-UI-0029"
    migration_rule: "remove: 生成式头像收敛进统一 Avatar 原语"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。§6.2 点名的 7 处替代之一。上游 25 行，配合 `boring-avatars`（(d) 组 T-UI-0124）生成抽象头像；本项目收敛为 §6.6 的确定性 Avatar（SHA-256(principal_id) 前 8 位 → 8 个预设中性低饱和底色）。替代而非 parity 的判据：视觉形态换了、生成算法换了、能力收敛（不再是任意抽象图形）。"

  - id: "biz-agents-agent-card"
    upstream: "app/src/components/agents/agent-card.tsx"
    label: "parity"
    target: "openbot_ui::features::agents::agent_card"
    owner: "openbot-ui"
    test_id: "T-UI-0030"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同名保留。"

  - id: "biz-agents-agent-fields"
    upstream: "app/src/components/agents/agent-fields.tsx"
    label: "parity"
    target: "openbot_ui::features::agents::agent_fields"
    owner: "openbot-ui"
    test_id: "T-UI-0031"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同名保留；表单控件一律经 `Field`（T-UI-0006）接线 aria-describedby / aria-invalid。"

  - id: "biz-agents-agent-profile"
    upstream: "app/src/components/agents/agent-profile.tsx"
    label: "parity"
    target: "openbot_ui::features::agents::agent_profile"
    owner: "openbot-ui"
    test_id: "T-UI-0032"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同名保留；头像位用 Avatar（T-UI-0025），不再用 abstract-avatar。"

  - id: "biz-agents-callback-token-panel"
    upstream: "app/src/components/agents/callback-token-panel.tsx"
    label: "parity"
    target: "openbot_ui::features::agents::callback_token_panel"
    owner: "openbot-ui"
    test_id: "T-UI-0033"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同名保留；token 明文展示位需要 `eye` / `eye-off` / `copy`（属 28 个新增图标）。"

  - id: "biz-agents-new-agent"
    upstream: "app/src/components/agents/new-agent.tsx"
    label: "parity"
    target: "openbot_ui::features::agents::new_agent"
    owner: "openbot-ui"
    test_id: "T-UI-0034"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同名保留。"

  - id: "biz-agents-orb-agent-orb"
    upstream: "app/src/components/agents/orb/agent-orb.tsx"
    label: "替代"
    target: "openbot_ui::features::agents::agent_presence::AgentPresence"
    owner: "openbot-ui"
    test_id: "T-UI-0035"
    migration_rule: "remove: 437 行 canvas/shader orb 收敛为 20px 状态环"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。§6.2 + §6.7 点名的替代。上游实测 437 行（wc -l app/src/components/agents/orb/agent-orb.tsx = 437），canvas/shader + 音频反应；本项目按原则 7 收为 20px 状态环：idle 静止灰环 / thinking 慢速旋转弧（loader-circle，1.2s 每圈）/ speaking 两段弧交替 / error 变 danger 色静止环 + 单次 160ms 横向位移。reduced-motion 下全部静止，只靠颜色与 aria-label 表达状态。上游 orb 与 ai-core 两个文件合并成这一个 Rust 组件，故两条 entry 指向同一 target。"

  - id: "biz-agents-orb-ai-core"
    upstream: "app/src/components/agents/orb/ai-core.tsx"
    label: "替代"
    target: "openbot_ui::features::agents::agent_presence::AgentPresence"
    owner: "openbot-ui"
    test_id: "T-UI-0036"
    migration_rule: "remove: 395 行 shader 内核并入同一状态环"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `agents`（该组上游文件数 8）。同上条的另一半：wc -l app/src/components/agents/orb/ai-core.tsx = 395。两个文件在上游是 orb 的外壳与内核，本项目合并为一个组件——**这是 45 → Leptos 组件数不是 1:1 的两处之一**（另一处是 app-sidebar → shell），台账里仍各记一条，因为台账的单位是上游文件不是目标组件。"

  - id: "biz-app-sidebar-app-sidebar"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx"
    label: "替代"
    target: "openbot_ui::shell::app_sidebar"
    owner: "openbot-ui"
    test_id: "T-UI-0037"
    migration_rule: "remove: 组件下沉为 shell 的一部分"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `app-sidebar`（该组上游文件数 2）。§6.2 点名的替代（原文\"`app-sidebar` → `Sidebar` 内容\"）。上游 387 行是一个业务组件；本项目里它是 App shell 的一部分（§5.1 / §13 的 `shell/`），内容填进 `openbot_ui::primitives::sidebar::Sidebar`（T-UI-0017）。判据：落点从 features/ 换到 shell/，不再是可独立复用的业务组件 ⇒ 替代。"

  - id: "biz-app-sidebar-channel"
    upstream: "app/src/components/app-sidebar/channel.tsx"
    label: "parity"
    target: "openbot_ui::features::app_sidebar::channel"
    owner: "openbot-ui"
    test_id: "T-UI-0038"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `app-sidebar`（该组上游文件数 2）。同名保留（侧栏里的单个 channel 行）。"

  - id: "biz-auth-provider-logo"
    upstream: "app/src/components/auth/provider-logo.tsx"
    label: "替代"
    target: "openbot_ui::icons::brand"
    owner: "openbot-ui"
    test_id: "T-UI-0039"
    migration_rule: "remove: 内联 React SVG 组件 → design/brand/*.svg 随包资产"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `auth`（该组上游文件数 1）。§6.2 + §4.6.3 点名的替代。上游把 Google / Microsoft / Okta 三个标志内联画在 tsx 里（源码注释写明理由：sign-in 页在拿到会话之前就要可信，走网络取的标志会恰好在最需要它的时候缺失，且未认证页向第三方发请求是没人要求的请求）——这条理由本项目**照单继承**：品牌标随包，零远程。形态换成 `design/brand/*.svg` + build.rs 生成，不与 Lucide 混排进 icons allowlist，不改色、不描边。这是 GUI 原则 5\"语义色不落图标\"的唯一例外。每个文件的来源 URL / 版本 / 使用条款登记进 v3 §19.3 的 provenance/sources.spdx.json。"

  - id: "biz-channels-avatar"
    upstream: "app/src/components/channels/avatar.tsx"
    label: "替代"
    target: "openbot_ui::primitives::avatar::Avatar"
    owner: "openbot-ui"
    test_id: "T-UI-0040"
    migration_rule: "remove: 收敛进统一 Avatar 原语"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。§6.2 点名的替代（原文\"`avatar` → `Avatar`\"）。上游 56 行；与 agents/abstract-avatar（T-UI-0029）一起收敛为一个 §6.6 Avatar 原语（T-UI-0025）。用户 / coworker / Bot 三类统一 24 / 32 / 40px 圆形。"

  - id: "biz-channels-channel-chat"
    upstream: "app/src/components/channels/channel-chat.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::channel_chat"
    owner: "openbot-ui"
    test_id: "T-UI-0041"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留。"

  - id: "biz-channels-chat-transcript"
    upstream: "app/src/components/channels/chat-transcript.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::chat_transcript"
    owner: "openbot-ui"
    test_id: "T-UI-0042"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留；markdown / 代码 / 链接 / 图片的渲染规则见 §6.4 与 (d) 组 T-UI-0122。远程 `![]()` 只渲染成带域名的链接芯片、不发起请求，这条是本项目裁决（设计系统文档 §6.4 自标**新增**），上游 streamdown 的远程图片行为未核 —— 见本 ledger 头部未验证项之外的单独提醒：该差异由 T-UI-0122 的 notes 承载。"

  - id: "biz-channels-composer-composer"
    upstream: "app/src/components/channels/composer/composer.tsx"
    label: "替代"
    target: "openbot_ui::features::channels::composer::Composer"
    owner: "openbot-ui"
    test_id: "T-UI-0043"
    migration_rule: "remove: prompt-area 运行时库 → 自研 Composer"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。§6.2 + §6.5 点名的替代。上游 362 行 + `prompt-area` 库（4 个文件，(d) 组 T-UI-0123）。业务逻辑 parity：草稿按 channel 持久（`draft.ts`）、发送队列与 stop/steer（`queue.ts`）、`@coworker` 与 `/skill` 触发（`triggers.ts` / `sources.ts`），上游这 4 份逻辑 + 2 份测试（draft.test.ts / queue.test.ts）在同目录，本轮 ls 实证。**新增**部分：输入法组合期间（compositionstart–compositionend）Enter 不发送——中文输入必需，上游无此行为。上游用 IconArrowUp（发送）与 IconPlayerStopFilled（停止），映射见 T-UI-0076 / T-UI-0102。"

  - id: "biz-channels-conversation-view"
    upstream: "app/src/components/channels/conversation-view.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::conversation_view"
    owner: "openbot-ui"
    test_id: "T-UI-0044"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留。"

  - id: "biz-channels-recipient-field"
    upstream: "app/src/components/channels/recipient-field.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::recipient_field"
    owner: "openbot-ui"
    test_id: "T-UI-0045"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留；基于 Combobox（T-UI-0002）。"

  - id: "biz-channels-tool-boundary"
    upstream: "app/src/components/channels/tool-boundary.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::tool_boundary"
    owner: "openbot-ui"
    test_id: "T-UI-0046"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留。"

  - id: "biz-channels-tool-line"
    upstream: "app/src/components/channels/tool-line.tsx"
    label: "parity"
    target: "openbot_ui::features::channels::tool_line"
    owner: "openbot-ui"
    test_id: "T-UI-0047"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `channels`（该组上游文件数 8）。同名保留。"

  - id: "biz-computer-activity-log"
    upstream: "app/src/components/computer/activity-log.tsx"
    label: "parity"
    target: "openbot_ui::features::computer::activity_log"
    owner: "openbot-ui"
    test_id: "T-UI-0048"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `computer`（该组上游文件数 5）。同名保留；上游用 IconFile / IconFolder / IconTerminal2，映射见 (c) 组。"

  - id: "biz-computer-command-output"
    upstream: "app/src/components/computer/command-output.tsx"
    label: "parity"
    target: "openbot_ui::features::computer::command_output"
    owner: "openbot-ui"
    test_id: "T-UI-0049"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `computer`（该组上游文件数 5）。同名保留。"

  - id: "biz-computer-computer-view"
    upstream: "app/src/components/computer/computer-view.tsx"
    label: "parity"
    target: "openbot_ui::features::computer::computer_view"
    owner: "openbot-ui"
    test_id: "T-UI-0050"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `computer`（该组上游文件数 5）。同名保留；实时 screencast 帧区域是 §10.2 允许的 `data-golden-mask` 区之一（首版 mask 清单进 fixtures/ui/golden/MANIFEST.toml），否则 golden 不可能确定。"

  - id: "biz-computer-live-screen"
    upstream: "app/src/components/computer/live-screen.tsx"
    label: "parity"
    target: "openbot_ui::features::computer::live_screen"
    owner: "openbot-ui"
    test_id: "T-UI-0051"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `computer`（该组上游文件数 5）。同名保留；同上，帧区域需 mask。"

  - id: "biz-computer-placeholder"
    upstream: "app/src/components/computer/placeholder.tsx"
    label: "parity"
    target: "openbot_ui::features::computer::placeholder"
    owner: "openbot-ui"
    test_id: "T-UI-0052"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `computer`（该组上游文件数 5）。同名保留。等待态的装饰 SVG 不在本条，在 settings/background（T-UI-0065）。"

  - id: "biz-gallery-activity"
    upstream: "app/src/components/gallery/activity.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::activity"
    owner: "openbot-ui"
    test_id: "T-UI-0053"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。compiled gallery 的 Leptos 重写（v3 §3.3）。与 parity/components.yaml 的 (a) 组是同一批文件的两个视角：那份台账管 Generative UI 的运行时契约，本条只管视觉组件落点。"

  - id: "biz-gallery-cards"
    upstream: "app/src/components/gallery/cards.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::cards"
    owner: "openbot-ui"
    test_id: "T-UI-0054"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-gallery-charts"
    upstream: "app/src/components/gallery/charts.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::charts"
    owner: "openbot-ui"
    test_id: "T-UI-0055"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上；图表色用 §4.2 的 chart-1..5（对 bg 对比度 ≥ 3.0，已过 §17 的对比度脚本）。"

  - id: "biz-gallery-decisions"
    upstream: "app/src/components/gallery/decisions.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::decisions"
    owner: "openbot-ui"
    test_id: "T-UI-0056"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-gallery-frame"
    upstream: "app/src/components/gallery/frame.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::frame"
    owner: "openbot-ui"
    test_id: "T-UI-0057"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-gallery-preview"
    upstream: "app/src/components/gallery/preview.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::preview"
    owner: "openbot-ui"
    test_id: "T-UI-0058"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-gallery-quote"
    upstream: "app/src/components/gallery/quote.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::quote"
    owner: "openbot-ui"
    test_id: "T-UI-0059"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-gallery-refused"
    upstream: "app/src/components/gallery/refused.tsx"
    label: "parity"
    target: "openbot_ui::features::gallery::refused"
    owner: "openbot-ui"
    test_id: "T-UI-0060"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `gallery`（该组上游文件数 8）。同上。"

  - id: "biz-layout-detail-panel"
    upstream: "app/src/components/layout/detail-panel.tsx"
    label: "parity"
    target: "openbot_ui::features::layout::detail_panel"
    owner: "openbot-ui"
    test_id: "T-UI-0061"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `layout`（该组上游文件数 4）。同名保留。"

  - id: "biz-layout-page-shell"
    upstream: "app/src/components/layout/page-shell.tsx"
    label: "parity"
    target: "openbot_ui::features::layout::page_shell"
    owner: "openbot-ui"
    test_id: "T-UI-0062"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `layout`（该组上游文件数 4）。同名保留；§5.1 App shell 的 parity 面在此，尺寸契约是新增（§5.1 标题原文\"尺寸新增\"），尺寸不在本 ledger 逐条登记。上游用 IconChevronLeft。"

  - id: "biz-layout-row-mark"
    upstream: "app/src/components/layout/row-mark.tsx"
    label: "parity"
    target: "openbot_ui::features::layout::row_mark"
    owner: "openbot-ui"
    test_id: "T-UI-0063"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `layout`（该组上游文件数 4）。同名保留。"

  - id: "biz-layout-stagger"
    upstream: "app/src/components/layout/stagger.tsx"
    label: "替代"
    target: "crates/openbot-ui/design/app.css::stagger-utilities"
    owner: "openbot-ui"
    test_id: "T-UI-0064"
    migration_rule: "remove: JS 交错动画 → CSS 级联工具类"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `layout`（该组上游文件数 4）。§6.2 + §4.5 ③ 点名的替代。上游 68 行、依赖 `motion` 库做逐项延迟入场；本项目换成纯 CSS 工具类（`--stagger-index` + `animation-delay` 级联）。target 是 CSS 落点不是 Rust 模块——这是全 ledger 唯一一条 target 落在 CSS 文件上的 entry，因为替代物本身就不是 Rust 代码。reduced-motion 下必须整体静止（§4.5），否则 golden 在两种 media 下不确定。"

  - id: "biz-settings-background"
    upstream: "app/src/components/settings/background.tsx"
    label: "替代"
    target: "openbot_ui::features::settings::computer_placeholder_art::ComputerPlaceholderArt"
    owner: "openbot-ui"
    test_id: "T-UI-0065"
    migration_rule: "rename: background → ComputerPlaceholderArt（同时重绘为中性线稿）"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `settings`（该组上游文件数 2）。§6.2 点名的替代。上游 162 行，是 computer frame 的等待态装饰 SVG，文件名 `background` 说不清它是什么；本项目改名为 ComputerPlaceholderArt 并按 GUI 裁决重绘为中性线稿（不用彩色、不上阴影）。改名 + 重绘 ⇒ 替代而非 parity。"

  - id: "biz-settings-settings-sidebar"
    upstream: "app/src/components/settings/settings-sidebar.tsx"
    label: "parity"
    target: "openbot_ui::features::settings::settings_sidebar"
    owner: "openbot-ui"
    test_id: "T-UI-0066"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `settings`（该组上游文件数 2）。同名保留；内容填进 primitives::sidebar::Sidebar。上游用 IconPlug 等。"

  - id: "biz-skills-edit-skill"
    upstream: "app/src/components/skills/edit-skill.tsx"
    label: "parity"
    target: "openbot_ui::features::skills::edit_skill"
    owner: "openbot-ui"
    test_id: "T-UI-0067"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `skills`（该组上游文件数 4）。同名保留。"

  - id: "biz-skills-new-skill"
    upstream: "app/src/components/skills/new-skill.tsx"
    label: "parity"
    target: "openbot_ui::features::skills::new_skill"
    owner: "openbot-ui"
    test_id: "T-UI-0068"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `skills`（该组上游文件数 4）。同名保留。"

  - id: "biz-skills-skill-agents"
    upstream: "app/src/components/skills/skill-agents.tsx"
    label: "parity"
    target: "openbot_ui::features::skills::skill_agents"
    owner: "openbot-ui"
    test_id: "T-UI-0069"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `skills`（该组上游文件数 4）。同名保留。"

  - id: "biz-skills-skill-fields"
    upstream: "app/src/components/skills/skill-fields.tsx"
    label: "parity"
    target: "openbot_ui::features::skills::skill_fields"
    owner: "openbot-ui"
    test_id: "T-UI-0070"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `skills`（该组上游文件数 4）。同名保留。"

  - id: "biz-component-preview"
    upstream: "app/src/components/component-preview.tsx"
    label: "parity"
    target: "openbot_ui::features::component_preview"
    owner: "openbot-ui"
    test_id: "T-UI-0071"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `根`（该组上游文件数 2）。上游根目录两个之一，同名保留。它是 sandboxed component 的预览面，运行时契约在 parity/components.yaml，不在本条。"

  - id: "biz-theme-provider"
    upstream: "app/src/components/theme-provider.tsx"
    label: "替代"
    target: "openbot_ui::theme"
    owner: "openbot-ui"
    test_id: "T-UI-0072"
    migration_rule: "remove: React context provider → 非组件的 theme 模块"
    status: "todo"
    evidence: "find app/src/components -name '*.tsx' -not -path 'app/src/components/ui/*' | wc -l = 45（上游 891df72f 干净克隆，本轮实跑，逐个文件名已核对进本 ledger）"
    notes: "§6.2 组 `根`（该组上游文件数 2）。§6.2 点名的替代（原文\"`theme-provider` → `theme` 模块（§7，非组件）\"）。上游 49 行是 React context provider；本项目里主题不是组件而是模块（`openbot_ui::theme`）+ 首帧由 Rust 改写 `<html class lang>`（§13.1 追加条款），零内联脚本。parity 面 = 手动 light/dark 两态（含 localStorage 键 `openbot-theme`，上游 lib/theme.ts::THEME_STORAGE_KEY 实证）；`system` 第三态是新增，由 T-UI-0026 ThemeToggle 承担。"

  # =======================================================================
  # (c) 47 个上游 Tabler 图标 → Lucide 1.33.0 名映射（46 Lucide + 1 品牌标，设计系统文档 §4.6.2 表逐行）
  # =======================================================================

  - id: "icon-IconAlertTriangle"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx::IconAlertTriangle"
    label: "替代"
    target: "openbot_ui::icons::Icon::TriangleAlert"
    owner: "openbot-ui"
    test_id: "T-UI-0073"
    migration_rule: "rename: IconAlertTriangle → Lucide `triangle-alert`"
    status: "todo"
    evidence: "grep -rlE '\\bIconAlertTriangle\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/components/$name.tsx）"
    notes: "Tabler `IconAlertTriangle` → Lucide `triangle-alert`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/triangle-alert.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconArrowDown"
    upstream: "app/src/components/ui/message-scroller.tsx::IconArrowDown"
    label: "替代"
    target: "openbot_ui::icons::Icon::ArrowDown"
    owner: "openbot-ui"
    test_id: "T-UI-0074"
    migration_rule: "rename: IconArrowDown → Lucide `arrow-down`"
    status: "todo"
    evidence: "grep -rlE '\\bIconArrowDown\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/ui/message-scroller.tsx）"
    notes: "Tabler `IconArrowDown` → Lucide `arrow-down`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/arrow-down.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconArrowLeft"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconArrowLeft"
    label: "替代"
    target: "openbot_ui::icons::Icon::ArrowLeft"
    owner: "openbot-ui"
    test_id: "T-UI-0075"
    migration_rule: "rename: IconArrowLeft → Lucide `arrow-left`"
    status: "todo"
    evidence: "grep -rlE '\\bIconArrowLeft\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconArrowLeft` → Lucide `arrow-left`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/arrow-left.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconArrowUp"
    upstream: "app/src/components/channels/composer/composer.tsx::IconArrowUp"
    label: "替代"
    target: "openbot_ui::icons::Icon::ArrowUp"
    owner: "openbot-ui"
    test_id: "T-UI-0076"
    migration_rule: "rename: IconArrowUp → Lucide `arrow-up`"
    status: "todo"
    evidence: "grep -rlE '\\bIconArrowUp\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/channels/composer/composer.tsx）"
    notes: "Tabler `IconArrowUp` → Lucide `arrow-up`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/arrow-up.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconArrowUpRight"
    upstream: "app/src/routes/_authed/admin/plugins/$key.tsx::IconArrowUpRight"
    label: "替代"
    target: "openbot_ui::icons::Icon::ArrowUpRight"
    owner: "openbot-ui"
    test_id: "T-UI-0077"
    migration_rule: "rename: IconArrowUpRight → Lucide `arrow-up-right`"
    status: "todo"
    evidence: "grep -rlE '\\bIconArrowUpRight\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/routes/_authed/admin/plugins/$key.tsx）"
    notes: "Tabler `IconArrowUpRight` → Lucide `arrow-up-right`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/arrow-up-right.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconBolt"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconBolt"
    label: "替代"
    target: "openbot_ui::icons::Icon::Zap"
    owner: "openbot-ui"
    test_id: "T-UI-0078"
    migration_rule: "rename: IconBolt → Lucide `zap`"
    status: "todo"
    evidence: "grep -rlE '\\bIconBolt\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconBolt` → Lucide `zap`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/zap.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconBox"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconBox"
    label: "替代"
    target: "openbot_ui::icons::Icon::Box"
    owner: "openbot-ui"
    test_id: "T-UI-0079"
    migration_rule: "rename: IconBox → Lucide `box`"
    status: "todo"
    evidence: "grep -rlE '\\bIconBox\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconBox` → Lucide `box`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/box.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconBrandGoogleDrive"
    upstream: "app/src/routes/_authed/admin/plugins/index.tsx::IconBrandGoogleDrive"
    label: "替代"
    target: "openbot_ui::icons::brand::GoogleDrive"
    owner: "openbot-ui"
    test_id: "T-UI-0080"
    migration_rule: "remove: Tabler 品牌图标 → design/brand/google-drive.svg 官方标志"
    status: "todo"
    evidence: "grep -rlE '\\bIconBrandGoogleDrive\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/routes/_authed/admin/plugins/index.tsx）"
    notes: "§4.6.2 表里 47 行中**唯一不映射到 Lucide** 的一行：连接器行的 Google Drive 用官方 SVG 标志（§4.6.3），放 design/brand/，不改色、不描边、不与 Lucide 混排进 icons allowlist。这是 GUI 原则 5\"语义色不落图标\"的唯一例外，因为品牌标不是语义色。来源 URL / 版本 / 使用条款登记进 v3 §19.3 的 provenance/sources.spdx.json。所以本组 47 条 = 46 Lucide + 1 品牌标。本条的 U1 不适用（不查 Lucide 集），但**品牌标文件本身尚未取得**，解除条件 = 按 Google 品牌指引取得官方 SVG 并登记 provenance。"

  - id: "icon-IconBuildingBank"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconBuildingBank"
    label: "替代"
    target: "openbot_ui::icons::Icon::Landmark"
    owner: "openbot-ui"
    test_id: "T-UI-0081"
    migration_rule: "rename: IconBuildingBank → Lucide `landmark`"
    status: "todo"
    evidence: "grep -rlE '\\bIconBuildingBank\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconBuildingBank` → Lucide `landmark`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/landmark.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconCheck"
    upstream: "app/src/components/ui/combobox.tsx::IconCheck"
    label: "替代"
    target: "openbot_ui::icons::Icon::Check"
    owner: "openbot-ui"
    test_id: "T-UI-0082"
    migration_rule: "rename: IconCheck → Lucide `check`"
    status: "todo"
    evidence: "grep -rlE '\\bIconCheck\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/ui/combobox.tsx）"
    notes: "Tabler `IconCheck` → Lucide `check`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/check.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconChevronDown"
    upstream: "app/src/components/ui/combobox.tsx::IconChevronDown"
    label: "替代"
    target: "openbot_ui::icons::Icon::ChevronDown"
    owner: "openbot-ui"
    test_id: "T-UI-0083"
    migration_rule: "rename: IconChevronDown → Lucide `chevron-down`"
    status: "todo"
    evidence: "grep -rlE '\\bIconChevronDown\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/ui/combobox.tsx）"
    notes: "Tabler `IconChevronDown` → Lucide `chevron-down`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/chevron-down.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconChevronLeft"
    upstream: "app/src/components/layout/page-shell.tsx::IconChevronLeft"
    label: "替代"
    target: "openbot_ui::icons::Icon::ChevronLeft"
    owner: "openbot-ui"
    test_id: "T-UI-0084"
    migration_rule: "rename: IconChevronLeft → Lucide `chevron-left`"
    status: "todo"
    evidence: "grep -rlE '\\bIconChevronLeft\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/layout/page-shell.tsx）"
    notes: "Tabler `IconChevronLeft` → Lucide `chevron-left`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/chevron-left.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconChevronRight"
    upstream: "app/src/components/ui/dropdown-menu.tsx::IconChevronRight"
    label: "替代"
    target: "openbot_ui::icons::Icon::ChevronRight"
    owner: "openbot-ui"
    test_id: "T-UI-0085"
    migration_rule: "rename: IconChevronRight → Lucide `chevron-right`"
    status: "todo"
    evidence: "grep -rlE '\\bIconChevronRight\\b' --include=*.tsx --include=*.ts app/src | wc -l = 6（首个 = app/src/components/ui/dropdown-menu.tsx）"
    notes: "Tabler `IconChevronRight` → Lucide `chevron-right`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 6 个文件引用它。随包落点 crates/openbot-ui/design/icons/chevron-right.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconChevronUp"
    upstream: "app/src/components/ui/select.tsx::IconChevronUp"
    label: "替代"
    target: "openbot_ui::icons::Icon::ChevronUp"
    owner: "openbot-ui"
    test_id: "T-UI-0086"
    migration_rule: "rename: IconChevronUp → Lucide `chevron-up`"
    status: "todo"
    evidence: "grep -rlE '\\bIconChevronUp\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/ui/select.tsx）"
    notes: "Tabler `IconChevronUp` → Lucide `chevron-up`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/chevron-up.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconClock"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx::IconClock"
    label: "替代"
    target: "openbot_ui::icons::Icon::Clock"
    owner: "openbot-ui"
    test_id: "T-UI-0087"
    migration_rule: "rename: IconClock → Lucide `clock`"
    status: "todo"
    evidence: "grep -rlE '\\bIconClock\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/components/$name.tsx）"
    notes: "Tabler `IconClock` → Lucide `clock`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/clock.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconCode"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconCode"
    label: "替代"
    target: "openbot_ui::icons::Icon::Code"
    owner: "openbot-ui"
    test_id: "T-UI-0088"
    migration_rule: "rename: IconCode → Lucide `code`"
    status: "todo"
    evidence: "grep -rlE '\\bIconCode\\b' --include=*.tsx --include=*.ts app/src | wc -l = 4（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconCode` → Lucide `code`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 4 个文件引用它。随包落点 crates/openbot-ui/design/icons/code.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconDatabase"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx::IconDatabase"
    label: "替代"
    target: "openbot_ui::icons::Icon::Database"
    owner: "openbot-ui"
    test_id: "T-UI-0089"
    migration_rule: "rename: IconDatabase → Lucide `database`"
    status: "todo"
    evidence: "grep -rlE '\\bIconDatabase\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/components/$name.tsx）"
    notes: "Tabler `IconDatabase` → Lucide `database`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/database.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconDeviceDesktop"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconDeviceDesktop"
    label: "替代"
    target: "openbot_ui::icons::Icon::Monitor"
    owner: "openbot-ui"
    test_id: "T-UI-0090"
    migration_rule: "rename: IconDeviceDesktop → Lucide `monitor`"
    status: "todo"
    evidence: "grep -rlE '\\bIconDeviceDesktop\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconDeviceDesktop` → Lucide `monitor`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/monitor.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconDots"
    upstream: "app/src/routes/_authed/_app/skills.tsx::IconDots"
    label: "替代"
    target: "openbot_ui::icons::Icon::Ellipsis"
    owner: "openbot-ui"
    test_id: "T-UI-0091"
    migration_rule: "rename: IconDots → Lucide `ellipsis`"
    status: "todo"
    evidence: "grep -rlE '\\bIconDots\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/_app/skills.tsx）"
    notes: "Tabler `IconDots` → Lucide `ellipsis`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/ellipsis.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconExternalLink"
    upstream: "app/src/routes/_authed/admin/plugins/$key.tsx::IconExternalLink"
    label: "替代"
    target: "openbot_ui::icons::Icon::ExternalLink"
    owner: "openbot-ui"
    test_id: "T-UI-0092"
    migration_rule: "rename: IconExternalLink → Lucide `external-link`"
    status: "todo"
    evidence: "grep -rlE '\\bIconExternalLink\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/plugins/$key.tsx）"
    notes: "Tabler `IconExternalLink` → Lucide `external-link`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/external-link.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconFile"
    upstream: "app/src/components/computer/activity-log.tsx::IconFile"
    label: "替代"
    target: "openbot_ui::icons::Icon::File"
    owner: "openbot-ui"
    test_id: "T-UI-0093"
    migration_rule: "rename: IconFile → Lucide `file`"
    status: "todo"
    evidence: "grep -rlE '\\bIconFile\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/computer/activity-log.tsx）"
    notes: "Tabler `IconFile` → Lucide `file`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/file.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconFileText"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconFileText"
    label: "替代"
    target: "openbot_ui::icons::Icon::FileText"
    owner: "openbot-ui"
    test_id: "T-UI-0094"
    migration_rule: "rename: IconFileText → Lucide `file-text`"
    status: "todo"
    evidence: "grep -rlE '\\bIconFileText\\b' --include=*.tsx --include=*.ts app/src | wc -l = 5（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconFileText` → Lucide `file-text`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 5 个文件引用它。随包落点 crates/openbot-ui/design/icons/file-text.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconFolder"
    upstream: "app/src/components/computer/activity-log.tsx::IconFolder"
    label: "替代"
    target: "openbot_ui::icons::Icon::Folder"
    owner: "openbot-ui"
    test_id: "T-UI-0095"
    migration_rule: "rename: IconFolder → Lucide `folder`"
    status: "todo"
    evidence: "grep -rlE '\\bIconFolder\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/computer/activity-log.tsx）"
    notes: "Tabler `IconFolder` → Lucide `folder`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/folder.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconKey"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconKey"
    label: "替代"
    target: "openbot_ui::icons::Icon::Key"
    owner: "openbot-ui"
    test_id: "T-UI-0096"
    migration_rule: "rename: IconKey → Lucide `key`"
    status: "todo"
    evidence: "grep -rlE '\\bIconKey\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconKey` → Lucide `key`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/key.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconLayoutGrid"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconLayoutGrid"
    label: "替代"
    target: "openbot_ui::icons::Icon::LayoutGrid"
    owner: "openbot-ui"
    test_id: "T-UI-0097"
    migration_rule: "rename: IconLayoutGrid → Lucide `layout-grid`"
    status: "todo"
    evidence: "grep -rlE '\\bIconLayoutGrid\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconLayoutGrid` → Lucide `layout-grid`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/layout-grid.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconLayoutSidebar"
    upstream: "app/src/components/ui/sidebar.tsx::IconLayoutSidebar"
    label: "替代"
    target: "openbot_ui::icons::Icon::PanelLeft"
    owner: "openbot-ui"
    test_id: "T-UI-0098"
    migration_rule: "rename: IconLayoutSidebar → Lucide `panel-left`"
    status: "todo"
    evidence: "grep -rlE '\\bIconLayoutSidebar\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/ui/sidebar.tsx）"
    notes: "Tabler `IconLayoutSidebar` → Lucide `panel-left`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/panel-left.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconListDetails"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconListDetails"
    label: "替代"
    target: "openbot_ui::icons::Icon::ListChecks"
    owner: "openbot-ui"
    test_id: "T-UI-0099"
    migration_rule: "rename: IconListDetails → Lucide `list-checks`"
    status: "todo"
    evidence: "grep -rlE '\\bIconListDetails\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconListDetails` → Lucide `list-checks`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/list-checks.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconLock"
    upstream: "app/src/routes/_authed/admin/people.tsx::IconLock"
    label: "替代"
    target: "openbot_ui::icons::Icon::Lock"
    owner: "openbot-ui"
    test_id: "T-UI-0100"
    migration_rule: "rename: IconLock → Lucide `lock`"
    status: "todo"
    evidence: "grep -rlE '\\bIconLock\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/people.tsx）"
    notes: "Tabler `IconLock` → Lucide `lock`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/lock.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconLogout"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconLogout"
    label: "替代"
    target: "openbot_ui::icons::Icon::LogOut"
    owner: "openbot-ui"
    test_id: "T-UI-0101"
    migration_rule: "rename: IconLogout → Lucide `log-out`"
    status: "todo"
    evidence: "grep -rlE '\\bIconLogout\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconLogout` → Lucide `log-out`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/log-out.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconPlayerStopFilled"
    upstream: "app/src/components/channels/composer/composer.tsx::IconPlayerStopFilled"
    label: "替代"
    target: "openbot_ui::icons::Icon::CircleStop"
    owner: "openbot-ui"
    test_id: "T-UI-0102"
    migration_rule: "rename: IconPlayerStopFilled → Lucide `circle-stop`"
    status: "todo"
    evidence: "grep -rlE '\\bIconPlayerStopFilled\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/channels/composer/composer.tsx）"
    notes: "Tabler `IconPlayerStopFilled` → Lucide `circle-stop`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/circle-stop.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconPlug"
    upstream: "app/src/components/settings/settings-sidebar.tsx::IconPlug"
    label: "替代"
    target: "openbot_ui::icons::Icon::Plug"
    owner: "openbot-ui"
    test_id: "T-UI-0103"
    migration_rule: "rename: IconPlug → Lucide `plug`"
    status: "todo"
    evidence: "grep -rlE '\\bIconPlug\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/settings/settings-sidebar.tsx）"
    notes: "Tabler `IconPlug` → Lucide `plug`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/plug.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconPlus"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconPlus"
    label: "替代"
    target: "openbot_ui::icons::Icon::Plus"
    owner: "openbot-ui"
    test_id: "T-UI-0104"
    migration_rule: "rename: IconPlus → Lucide `plus`"
    status: "todo"
    evidence: "grep -rlE '\\bIconPlus\\b' --include=*.tsx --include=*.ts app/src | wc -l = 7（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconPlus` → Lucide `plus`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 7 个文件引用它。随包落点 crates/openbot-ui/design/icons/plus.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconPresentation"
    upstream: "app/src/lib/markdown.tsx::IconPresentation"
    label: "替代"
    target: "openbot_ui::icons::Icon::Presentation"
    owner: "openbot-ui"
    test_id: "T-UI-0105"
    migration_rule: "rename: IconPresentation → Lucide `presentation`"
    status: "todo"
    evidence: "grep -rlE '\\bIconPresentation\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/lib/markdown.tsx）"
    notes: "Tabler `IconPresentation` → Lucide `presentation`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/presentation.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconPuzzle"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconPuzzle"
    label: "替代"
    target: "openbot_ui::icons::Icon::Puzzle"
    owner: "openbot-ui"
    test_id: "T-UI-0106"
    migration_rule: "rename: IconPuzzle → Lucide `puzzle`"
    status: "todo"
    evidence: "grep -rlE '\\bIconPuzzle\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconPuzzle` → Lucide `puzzle`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/puzzle.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconRefresh"
    upstream: "app/src/routes/_authed/admin/audit.tsx::IconRefresh"
    label: "替代"
    target: "openbot_ui::icons::Icon::RefreshCw"
    owner: "openbot-ui"
    test_id: "T-UI-0107"
    migration_rule: "rename: IconRefresh → Lucide `refresh-cw`"
    status: "todo"
    evidence: "grep -rlE '\\bIconRefresh\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/audit.tsx）"
    notes: "Tabler `IconRefresh` → Lucide `refresh-cw`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/refresh-cw.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconSearch"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconSearch"
    label: "替代"
    target: "openbot_ui::icons::Icon::Search"
    owner: "openbot-ui"
    test_id: "T-UI-0108"
    migration_rule: "rename: IconSearch → Lucide `search`"
    status: "todo"
    evidence: "grep -rlE '\\bIconSearch\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconSearch` → Lucide `search`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/search.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconSelector"
    upstream: "app/src/components/ui/select.tsx::IconSelector"
    label: "替代"
    target: "openbot_ui::icons::Icon::ChevronsUpDown"
    owner: "openbot-ui"
    test_id: "T-UI-0109"
    migration_rule: "rename: IconSelector → Lucide `chevrons-up-down`"
    status: "todo"
    evidence: "grep -rlE '\\bIconSelector\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/ui/select.tsx）"
    notes: "Tabler `IconSelector` → Lucide `chevrons-up-down`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/chevrons-up-down.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconSettings"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconSettings"
    label: "替代"
    target: "openbot_ui::icons::Icon::Settings"
    owner: "openbot-ui"
    test_id: "T-UI-0110"
    migration_rule: "rename: IconSettings → Lucide `settings`"
    status: "todo"
    evidence: "grep -rlE '\\bIconSettings\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconSettings` → Lucide `settings`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/settings.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconShieldCheck"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconShieldCheck"
    label: "替代"
    target: "openbot_ui::icons::Icon::ShieldCheck"
    owner: "openbot-ui"
    test_id: "T-UI-0111"
    migration_rule: "rename: IconShieldCheck → Lucide `shield-check`"
    status: "todo"
    evidence: "grep -rlE '\\bIconShieldCheck\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconShieldCheck` → Lucide `shield-check`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/shield-check.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconShieldLock"
    upstream: "app/src/components/app-sidebar/app-sidebar.tsx::IconShieldLock"
    label: "替代"
    target: "openbot_ui::icons::Icon::ShieldLock"
    owner: "openbot-ui"
    test_id: "T-UI-0112"
    migration_rule: "rename: IconShieldLock → Lucide `shield-lock`"
    status: "todo"
    evidence: "grep -rlE '\\bIconShieldLock\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/app-sidebar/app-sidebar.tsx）"
    notes: "Tabler `IconShieldLock` → Lucide `shield-lock`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/shield-lock.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconTable"
    upstream: "app/src/lib/markdown.tsx::IconTable"
    label: "替代"
    target: "openbot_ui::icons::Icon::Table"
    owner: "openbot-ui"
    test_id: "T-UI-0113"
    migration_rule: "rename: IconTable → Lucide `table`"
    status: "todo"
    evidence: "grep -rlE '\\bIconTable\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/lib/markdown.tsx）"
    notes: "Tabler `IconTable` → Lucide `table`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/table.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconTag"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx::IconTag"
    label: "替代"
    target: "openbot_ui::icons::Icon::Tag"
    owner: "openbot-ui"
    test_id: "T-UI-0114"
    migration_rule: "rename: IconTag → Lucide `tag`"
    status: "todo"
    evidence: "grep -rlE '\\bIconTag\\b' --include=*.tsx --include=*.ts app/src | wc -l = 2（首个 = app/src/routes/_authed/admin/components/$name.tsx）"
    notes: "Tabler `IconTag` → Lucide `tag`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 2 个文件引用它。随包落点 crates/openbot-ui/design/icons/tag.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconTerminal2"
    upstream: "app/src/components/computer/activity-log.tsx::IconTerminal2"
    label: "替代"
    target: "openbot_ui::icons::Icon::SquareTerminal"
    owner: "openbot-ui"
    test_id: "T-UI-0115"
    migration_rule: "rename: IconTerminal2 → Lucide `square-terminal`"
    status: "todo"
    evidence: "grep -rlE '\\bIconTerminal2\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/components/computer/activity-log.tsx）"
    notes: "Tabler `IconTerminal2` → Lucide `square-terminal`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/square-terminal.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconTrash"
    upstream: "app/src/routes/_authed/admin/identity-providers.tsx::IconTrash"
    label: "替代"
    target: "openbot_ui::icons::Icon::Trash2"
    owner: "openbot-ui"
    test_id: "T-UI-0116"
    migration_rule: "rename: IconTrash → Lucide `trash-2`"
    status: "todo"
    evidence: "grep -rlE '\\bIconTrash\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/identity-providers.tsx）"
    notes: "Tabler `IconTrash` → Lucide `trash-2`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/trash-2.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconUser"
    upstream: "app/src/routes/_authed/admin/people.tsx::IconUser"
    label: "替代"
    target: "openbot_ui::icons::Icon::User"
    owner: "openbot-ui"
    test_id: "T-UI-0117"
    migration_rule: "rename: IconUser → Lucide `user`"
    status: "todo"
    evidence: "grep -rlE '\\bIconUser\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/people.tsx）"
    notes: "Tabler `IconUser` → Lucide `user`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/user.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconUsers"
    upstream: "app/src/components/admin/admin-sidebar.tsx::IconUsers"
    label: "替代"
    target: "openbot_ui::icons::Icon::Users"
    owner: "openbot-ui"
    test_id: "T-UI-0118"
    migration_rule: "rename: IconUsers → Lucide `users`"
    status: "todo"
    evidence: "grep -rlE '\\bIconUsers\\b' --include=*.tsx --include=*.ts app/src | wc -l = 3（首个 = app/src/components/admin/admin-sidebar.tsx）"
    notes: "Tabler `IconUsers` → Lucide `users`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 3 个文件引用它。随包落点 crates/openbot-ui/design/icons/users.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  - id: "icon-IconWorld"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx::IconWorld"
    label: "替代"
    target: "openbot_ui::icons::Icon::Globe"
    owner: "openbot-ui"
    test_id: "T-UI-0119"
    migration_rule: "rename: IconWorld → Lucide `globe`"
    status: "todo"
    evidence: "grep -rlE '\\bIconWorld\\b' --include=*.tsx --include=*.ts app/src | wc -l = 1（首个 = app/src/routes/_authed/admin/components/$name.tsx）"
    notes: "Tabler `IconWorld` → Lucide `globe`（尺寸 16 行内 / 20 导航按钮，stroke-width 生成时由 Lucide 默认 2 改写为 1.75，颜色 currentColor）。上游共 1 个文件引用它。随包落点 crates/openbot-ui/design/icons/globe.svg，allowlist crates/openbot-ui/design/icons.toml，build.rs 生成 Icon 枚举；闸门双向零漂移（源码里每个 Icon::X 必在 allowlist，目录里每个 SVG 必被引用）。**未验证（头部 U1）**：Lucide 1.33.0 侧确实有这个名字，本轮无法核实 —— 缺 lucide-icons-1.33.0.zip（设计系统文档 §4.6.1 记 sha256 53831c8def…260a98），本机无该文件且本任务禁网。本条 evidence 只证明\"上游确实用了这个 Tabler 图标\"。"

  # =======================================================================
  # (d) 6 个运行时 JavaScript 库 → Rust/CSS 替代（设计系统文档 §6.3）
  # =======================================================================

  - id: "lib-base-ui-react"
    upstream: "app/package.json::dependencies.@base-ui/react"
    label: "替代"
    target: "openbot_ui::primitives"
    owner: "openbot-ui"
    test_id: "T-UI-0120"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done → 13 8 4 3 1 32（本库 = 13 个文件）"
    notes: "替代物：—（自研，不引入替代库）。无头原语。上游 13 个文件引用（Button / Combobox / Dialog / Input / Menu / Select / Separator / Switch / Tooltip / merge-props / use-render 等）。替代 = §6.1 自研 21 个 Leptos 原语（本 ledger (a) 组），键盘 / ARIA 模式按 WAI-ARIA APG 逐条兑现。**不引入任何 Rust 无头组件库**：现成库会把它自己的 DOM 结构与 class 约定带进来，与 §12.5 的 class 字面量规则和 §10 的 golden 冲突。"

  - id: "lib-motion"
    upstream: "app/package.json::dependencies.motion"
    label: "替代"
    target: "crates/openbot-ui/design/app.css::animations"
    owner: "openbot-ui"
    test_id: "T-UI-0121"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done → 13 8 4 3 1 32（本库 = 8 个文件）"
    notes: "替代物：—（CSS transition/animation + Web Animations API）。动效。上游 8 个文件引用；`app/src/lib/motion.ts` 定义 EASE_OUT = [0.23, 1, 0.32, 1]、ENTRANCE_SECONDS = 0.2。替代 = CSS transition/animation + 必要时 Web Animations API（§4.5），自写 @keyframes 清单。`styles.css` 有 1 个 prefers-reduced-motion 块（grep -c = 1，本轮实跑），本项目必须把 reduced-motion 做成**全局静止**而不是单块特例，否则 §10.2 的 golden 确定性条件（CDP 模拟 reduce）拍不到稳定画面。target 落在 CSS 不是 Rust 模块。"

  - id: "lib-streamdown"
    upstream: "app/package.json::dependencies.streamdown"
    label: "替代"
    target: "openbot_ui::features::channels::markdown"
    owner: "openbot-ui"
    test_id: "T-UI-0122"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done → 13 8 4 3 1 32（本库 = 1 个文件）"
    notes: "替代物：pulldown-cmark 0.13.4 + syntect 5.3.0（default-fancy，纯 Rust，WASM 可用）。Bot 正文流式 markdown 与代码高亮。上游 1 个文件引用 streamdown，高亮经 @shikijs/core 3.23.0（grep -cE '\"@shikijs/core@' bun.lock = 1，本轮实跑）。替代规则见 §6.4：**不开 raw HTML**（Event::Html 一律按文本渲染，模型输出是不可信输入）；增量按 (message_id, block_index) memo 尾块重解析，单测 streaming_render_equals_batch_render 断言与一次性完整渲染逐节点相等；代码块固定 24 种语言，scope → 6 个 token 色，不用 syntect 自带主题。**差异登记**：远程 `![]()` 只渲染成带域名的链接芯片、不发起请求，这是本项目裁决（§6.4 自标新增），上游 streamdown 的远程图片行为本轮**未核**（缺该库源码）。"

  - id: "lib-prompt-area"
    upstream: "app/package.json::dependencies.prompt-area"
    label: "替代"
    target: "openbot_ui::features::channels::composer::Composer"
    owner: "openbot-ui"
    test_id: "T-UI-0123"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done → 13 8 4 3 1 32（本库 = 4 个文件）"
    notes: "替代物：—（自研 Composer，§6.5）。composer 输入区。上游 4 个文件引用。替代 = §6.5 的 Composer（(b) 组 T-UI-0043 记的是上游那个 composer.tsx 业务组件，本条记的是它依赖的库）。两条不重复：一条的单位是上游文件，一条的单位是上游 npm 依赖。"

  - id: "lib-boring-avatars"
    upstream: "app/package.json::dependencies.boring-avatars"
    label: "替代"
    target: "openbot_ui::primitives::avatar::Avatar"
    owner: "openbot-ui"
    test_id: "T-UI-0124"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "cd app/src && for p in '@base-ui/react' motion prompt-area boring-avatars streamdown '@tabler/icons-react'; do grep -rlE \"from [\\\"']$p\" --include=*.tsx --include=*.ts . | wc -l | awk '{printf \"%d \", $1}'; done → 13 8 4 3 1 32（本库 = 3 个文件）"
    notes: "替代物：—（自研 Avatar，§6.6）。生成式头像。上游 3 个文件引用。替代 = §6.6 的确定性 Avatar：SHA-256(principal_id) 前 8 位映射到 8 个预设中性低饱和底色（亮暗各一组，与 fg 对比 ≥ 4.5:1）。\"同一 id 在任何平台任何时间得到同一头像\"是 golden 的前置条件（§10.2），不是锦上添花——上游库的随机性会让每次截图都不同。"

  - id: "lib-tw-animate-css"
    upstream: "app/package.json::dependencies.tw-animate-css"
    label: "替代"
    target: "crates/openbot-ui/design/app.css::keyframes"
    owner: "openbot-ui"
    test_id: "T-UI-0125"
    migration_rule: "remove: 运行时 JavaScript 依赖不进 Rust 发行物"
    status: "todo"
    evidence: "grep -c 'tw-animate-css' app/src/styles.css = 1（本轮实跑；JS import 那条 for 循环对它恒 0，不可用）"
    notes: "替代物：—（自写 @keyframes 清单，§4.5）。动画工具类。**它是 CSS `@import` 不是 JS `from`**，所以本组前 5 条用的那条 for 循环照不到它（那条命令对 tw-animate-css 会给 0，在\"这个库根本没被用\"的世界里同样为真）；正确判据是 grep -c 'tw-animate-css' app/src/styles.css = 1（本轮实跑，命中 styles.css 的 @import 行）。这正是 CLAUDE.md 证据铁律要求的\"期望 0 的断言必须配正向对照\"的反面用法：先换一条能命中的命令，再下结论。"

  # =======================================================================
  # (e) 27 个 golden 页面 = 26 个上游 route 页 + memory 页（设计系统文档 §10.1）
  # =======================================================================

  - id: "page-app-agents-index"
    upstream: "app/src/routes/_authed/_app/agents/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_agents_index"
    owner: "openbot-ui"
    test_id: "T-UI-0126"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_agents_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。coworker（agent）列表页。"

  - id: "page-app-bot"
    upstream: "app/src/routes/_authed/_app/bot.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_bot"
    owner: "openbot-ui"
    test_id: "T-UI-0127"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_bot.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。Bot 页。"

  - id: "page-app-channel-detail"
    upstream: "app/src/routes/_authed/_app/channel/$channelId.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_channel_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0128"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_channel_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。channel 会话页 —— 动态段 `$channelId`，golden 用 fixtures/ui/seed.json 里的固定 channel id，否则 URL 与页面内容都不确定。这是 §10.2 \"数据固定\"条件的直接落点。"

  - id: "page-app-channel-new"
    upstream: "app/src/routes/_authed/_app/channel/new.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_channel_new"
    owner: "openbot-ui"
    test_id: "T-UI-0129"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_channel_new.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。新建 channel 页。"

  - id: "page-app-index"
    upstream: "app/src/routes/_authed/_app/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_index"
    owner: "openbot-ui"
    test_id: "T-UI-0130"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。登录后的首页。"

  - id: "page-app-skills"
    upstream: "app/src/routes/_authed/_app/skills.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::app_skills"
    owner: "openbot-ui"
    test_id: "T-UI-0131"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/app_skills.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。技能页（用户面）。上游用 IconDots。"

  - id: "page-admin-audit"
    upstream: "app/src/routes/_authed/admin/audit.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_audit"
    owner: "openbot-ui"
    test_id: "T-UI-0132"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_audit.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。审计日志页。上游用 IconRefresh。审计行时间戳由注入的 Clock 钉在 2026-01-01T00:00:00Z（§10.2）。"

  - id: "page-admin-boundaries"
    upstream: "app/src/routes/_authed/admin/boundaries.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_boundaries"
    owner: "openbot-ui"
    test_id: "T-UI-0133"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_boundaries.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。边界（boundaries）页。"

  - id: "page-admin-components-detail"
    upstream: "app/src/routes/_authed/admin/components/$name.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_components_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0134"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_components_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。Generative UI 组件详情页 —— 动态段 `$name`。上游用 IconAlertTriangle / IconClock / IconDatabase / IconTag / IconWorld。该页的 sandboxed 渲染契约在 parity/components.yaml，不在本条。"

  - id: "page-admin-components-index"
    upstream: "app/src/routes/_authed/admin/components/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_components_index"
    owner: "openbot-ui"
    test_id: "T-UI-0135"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_components_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。Generative UI 组件列表页。"

  - id: "page-admin-computers"
    upstream: "app/src/routes/_authed/admin/computers.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_computers"
    owner: "openbot-ui"
    test_id: "T-UI-0136"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_computers.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。计算机（computer）管理页。"

  - id: "page-admin-credentials"
    upstream: "app/src/routes/_authed/admin/credentials.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_credentials"
    owner: "openbot-ui"
    test_id: "T-UI-0137"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_credentials.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。凭据页；密文位需 `eye` / `eye-off`（28 个新增图标之一）。"

  - id: "page-admin-identity-providers"
    upstream: "app/src/routes/_authed/admin/identity-providers.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_identity_providers"
    owner: "openbot-ui"
    test_id: "T-UI-0138"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_identity_providers.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。身份提供方页。上游用 IconTrash。"

  - id: "page-admin-index"
    upstream: "app/src/routes/_authed/admin/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_index"
    owner: "openbot-ui"
    test_id: "T-UI-0139"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。管理台首页。"

  - id: "page-admin-people"
    upstream: "app/src/routes/_authed/admin/people.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_people"
    owner: "openbot-ui"
    test_id: "T-UI-0140"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_people.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。人员页。上游用 IconLock / IconUser。"

  - id: "page-admin-playground"
    upstream: "app/src/routes/_authed/admin/playground.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_playground"
    owner: "openbot-ui"
    test_id: "T-UI-0141"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_playground.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。sandboxed component 的编辑 / 预览页。它同时是 parity/components.yaml (b) 组的运行时契约来源，两份台账在此交汇；本条只管视觉 golden，不管沙箱注入语义。"

  - id: "page-admin-plugins-detail"
    upstream: "app/src/routes/_authed/admin/plugins/$key.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_plugins_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0142"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_plugins_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。插件详情页 —— 动态段 `$key`。上游用 IconArrowUpRight / IconExternalLink。"

  - id: "page-admin-plugins-tool-detail"
    upstream: "app/src/routes/_authed/admin/plugins/$key_.tools.$tool.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_plugins_tool_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0143"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_plugins_tool_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。插件工具详情页 —— **双动态段** `$key_` + `$tool`（TanStack Router 的 `_` 后缀表示不嵌套父布局）。golden 需要 seed 里同时固定 key 与 tool 两个值。"

  - id: "page-admin-plugins-index"
    upstream: "app/src/routes/_authed/admin/plugins/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_plugins_index"
    owner: "openbot-ui"
    test_id: "T-UI-0144"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_plugins_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。插件列表页。上游用 IconBrandGoogleDrive（本 ledger 唯一品牌标，见 T-UI-0080）。"

  - id: "page-admin-skills"
    upstream: "app/src/routes/_authed/admin/skills.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::admin_skills"
    owner: "openbot-ui"
    test_id: "T-UI-0145"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/admin_skills.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。技能页（管理面）；与 _app/skills.tsx 同名不同页，golden 文件名靠前缀区分。"

  - id: "page-settings-components-gallery-detail"
    upstream: "app/src/routes/_authed/settings/components-gallery/$name.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::settings_components_gallery_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0146"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/settings_components_gallery_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。组件画廊详情页 —— 动态段 `$name`。注意与 §10.3 的**设计画廊** `/_design` 不是一回事：前者是上游的产品页（27 页之一），后者是本项目新增的、只在 design-gallery feature 下存在的原语对照页（生产关闭），golden 矩阵里单列为「画廊 1 页 × 2 主题」。两者混起来会让 110 这个数算不平。"

  - id: "page-settings-components-gallery-index"
    upstream: "app/src/routes/_authed/settings/components-gallery/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::settings_components_gallery_index"
    owner: "openbot-ui"
    test_id: "T-UI-0147"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/settings_components_gallery_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。组件画廊列表页。"

  - id: "page-settings-connected-accounts-detail"
    upstream: "app/src/routes/_authed/settings/connected-accounts/$key.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::settings_connected_accounts_detail"
    owner: "openbot-ui"
    test_id: "T-UI-0148"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/settings_connected_accounts_detail.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。已连接账户详情页 —— 动态段 `$key`。"

  - id: "page-settings-connected-accounts-index"
    upstream: "app/src/routes/_authed/settings/connected-accounts/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::settings_connected_accounts_index"
    owner: "openbot-ui"
    test_id: "T-UI-0149"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/settings_connected_accounts_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。已连接账户列表页。"

  - id: "page-settings-index"
    upstream: "app/src/routes/_authed/settings/index.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::settings_index"
    owner: "openbot-ui"
    test_id: "T-UI-0150"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/settings_index.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。设置首页。"

  - id: "page-sign"
    upstream: "app/src/routes/sign.tsx"
    label: "parity"
    target: "openbot_ui::shell::routes::sign"
    owner: "openbot-ui"
    test_id: "T-UI-0151"
    migration_rule: "preserve"
    status: "todo"
    evidence: "find app/src/routes -name '*.tsx' | grep -vE '(__root|_authed|_app|route)\\.tsx$' | wc -l = 26（正向对照：不加 grep -v 时 = 31，即 26 页 + 5 layout，本轮均实跑）"
    notes: "golden 落点 fixtures/ui/golden/web/sign.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。登录页 —— **唯一不在 `_authed` 下的页面**，也是唯一渲染品牌标的页面（provider-logo → T-UI-0039）。上游源码注释写明：这一页在拿到会话之前就要可信，标志必须随包不走网络。"

  - id: "page-app-memory"
    upstream: "-"
    label: "新增"
    target: "openbot_ui::shell::routes::app_memory"
    owner: "openbot-ui"
    test_id: "T-UI-0152"
    migration_rule: "n/a: v3 §3.1 条 7 新增页面，上游 route 目录零对应物"
    status: "todo"
    evidence: "find app/src/routes -name '*memory*' | wc -l = 0；正向对照 find app/src/routes -name '*skills*' | wc -l = 2（同一条 find 在确实存在的名字上命中）"
    notes: "golden 落点 fixtures/ui/golden/web/app_memory.<theme>.<w>x<h>.png，theme ∈ {light, dark}，视口 ∈ {1440x900, 1024x640}，DPR 1 ⇒ Web 每页 4 张；Desktop（macos-arm64 / windows-x64）每页 2 张（1440x900 单视口）。全矩阵 Web 27×2×2 + 画廊 1×2 = 110 张，Desktop 27×2 = 54 张/平台。**本轮 0 张 golden 已生成**（fixtures/ui/ 目录尚不存在），status 因此是 todo。memory 页由 v3 §3.1 条 7 引入，上游 26 页里没有它 —— 所以 golden 矩阵是 26 + 1 = 27 而不是 26。图标 `brain` 属 §4.6.2 的 28 个新增图标（见头部 U2）。**注意**：这一条使 27 这个数**不能**用任何一条上游命令复算，只能用「上游 26（recount 第 2 条）+ 本条 1」两段相加得到；把 27 写成某条上游 grep 的输出会是伪造证据。"
