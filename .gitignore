# ---------------------------------------------------------------------------
# 构建产物
# ---------------------------------------------------------------------------
/target/
# `xtask tools fetch` 把钉 sha256 的构建期二进制（Tailwind standalone CLI / trunk /
# wasm-bindgen CLI / wasm-opt）下载到这里（设计系统文档 §12.1）。它们不进发行物，也不入库
# —— sha256 表在 tools/pins.toml 里，那份表才是真源。上面的 /target/ 已经覆盖，写出来是为了
# 让"为什么二进制不入库"这件事在 .gitignore 里可查。
/target/tools/
# `cargo xtask` 驱动器自己的构建树。它**必须**在 `/target/` 之外：`cargo xtask ci`
# 第 3 步的 `cargo test` 会重链驱动器，而 Windows 不允许删除正在运行的 exe。
# 判据在 xtask.rs::driver_conflicts_with_child_target，alias 在 .cargo/config.toml。
/target-xtask/
# trunk 的 WASM 产物目录（设计系统文档 §12.3）。
/dist/
/crates/openbot-ui/dist/

# ---------------------------------------------------------------------------
# 生成物（build.rs / 工具链产出，真源是它们的输入文件）
# ---------------------------------------------------------------------------
# design/tokens.toml -> tokens.css。设计系统文档 §13 逐字标注"生成，不入库"。
/crates/openbot-ui/design/tokens.css

# 注：设计系统文档 §13 还把 `src/icons.rs` 与 `src/tokens.rs` 标为"（生成）"，但**没有**像
# tokens.css 那样写明"不入库"。这两个文件此刻不存在（G6 才会有 build.rs），落点是 `src/`
# 还是 `OUT_DIR` 也还没定，所以本轮**不**替 G6 预先裁决它们的入库策略 —— 写进 .gitignore
# 等于替别人做了决定，而漏写只会在第一次 `git status` 时被看见。G6 落地 build.rs 时同 PR 补。

# ---------------------------------------------------------------------------
# 视觉 golden 的比对中间产物（设计系统文档 §10.4）
# ---------------------------------------------------------------------------
# 基线 PNG（fixtures/ui/golden/{web,macos-arm64,windows-x64}/*.png）**必须入库** —— 它们是
# 视觉 oracle 本身。只有比对时生成的差异图不入库。
/fixtures/ui/golden/_diff/

# ---------------------------------------------------------------------------
# 明确**不**忽略（写在这里防止被"顺手清理"掉）
# ---------------------------------------------------------------------------
# Cargo.lock：v3 §16.3 逐字要求"`Cargo.lock` 和 engine lockfile 提交"。
# 没有它，`cargo test --locked` 与可复现构建校验两条闸门同时失效。
!/Cargo.lock

# ---------------------------------------------------------------------------
# 本机与编辑器噪声
# ---------------------------------------------------------------------------
# .DS_Store 单独点名：目录里混进一个 .DS_Store 就足以让"按目录枚举资产"的闸门整条判空，
# 而症状看起来像"功能压根没接通"。让它进不了库比事后排查便宜得多。
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
!.vscode/extensions.json

# ---------------------------------------------------------------------------
# 机密（secret scan 是 v3 §16.3 的固定闸门之一；.gitignore 是它的第一道，不是唯一一道）
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
