# OpenBot v4 全范围闭合工作单

盘点日期：2026-09-04（America/Los_Angeles）。

本文件只安排执行顺序，不是新的架构或验收真源。约束依次来自
`CLAUDE.md`、`2026-08-21-OpenBot全量Rust重写终版研究与实施方案.md`（v4，尤其§24、§25、§28.1）
及`2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md`。
范围占用和外部候选继续由`2026-09-04-v4并行实施预留台账.md`管理；环境与历史证据见
`2026-08-23-OpenBot-实施移交指南.md`。这里不能覆盖两份第一真源或自行放宽闸门。

## 1. 优化后的目标表述

**完整实施并最终闭合 OpenBot v4 第一真源的全部范围。**

每轮先核对实际 HEAD、工作树、PR、预留台账和机器台账，按依赖推进所有可实施余项。
实现、证据补齐、真实平台验证、供应链与发行、迁移演练和最终文档同步都属于本目标。
Batch、提交、PR、单项测试通过仅为检查点；仅在§24 G0–G8全部具备真实可复算证据并勾选、
§25十条DoD全部满足、parity及fixtures的todo/未分类项归零、跨scope泄漏和audit-before-action违规为零、
规定的本机/平台/供应链/发行/golden/外审闸门全部实际通过并完成最终同步后，才可标记complete。

外部条件缺失时记录具体阻塞、所需输入及解锁验收，继续其它独立工作。只有同一真实阻塞依目标规则
连续重复，且已无其它有意义工作可推进时，才可标blocked。当前远未达到该条件。

全程保持`grok-bot` tree=`86f5a85f560f721677fa7e587a67ac0ffc036cb5`、零npm、仅一个非Grok
`package.json`；Actions manual-only且未经授权不派发；不运行R63禁止的`cargo xtask ci`；
不擅自合并PR。compile-only、fake、静态检查和局部证据均不能替代真实平台、生产或整关证据。

## 2. 实际起点

本轮起点为`9c40e5b`，其前已有Batch113/R188实现与文档；不得退回R165或R163。
2026-09-04只读GitHub查询确认#73、堆叠#74及#75仍开放；后续本地提交不能自动视为已经交付PR。
仓库remote已核对为`https://github.com/acosmi/OpenBot.git`。

已新建分支`feat/2026-09-04-G7-screen-delivery-budget`。盘点时只有一个尚未接入module tree的
Screen预算草稿，未测试、未提交，不计任何完成量。仓内两份未跟踪的既有质量审计报告保持原样。

本轮亲跑`cargo xtask parity-check`通过，0违反；另逐条读取status/label/test_id核对：

| 台账 | done | todo | total |
|---|---:|---:|---:|
| API | 96 | 80 | 176 |
| Browser operations | 17 | 33 | 50 |
| Components | 13 | 11 | 24 |
| Environment | 55 | 25 | 80 |
| Events | 48 | 44 | 92 |
| Routes | 24 | 8 | 32 |
| Tables | 59 | 0 | 59 |
| Tests | 470 | 577 | 1047 |
| UI | 91 | 61 | 152 |
| **parity合计** | **873** | **839** | **1712** |
| **fixtures（不进parity分母）** | **32** | **21** | **53** |

parity的1712个test_id唯一，label未分类=0，status全为done/todo。
overlay=`1275 carry / 429 revalidate / 2 split / 6 superseded`。
§24.1只有G1整关勾选；其余G0、G2–G8仍未通过。此分布不换算成产品完成百分比。

文档里的旧快照不作为当前计数，例如§24.1仍有“其余9 route”等历史措辞；当前待办route是8条，
必须以机器台账和具体T-ID为准。已有实现也不能仅凭符号存在把旧todo批量改done。

## 3. 按依赖重排的工作流

“本机可做”只代表可以继续实现或取得本机证据；不代表已经满足阶段门或可以发布。
P1 Windows/runsc未通过前，不宣称P2/P3/P4已进入；独立开发仍按R61/R125允许的范围推进。

| 顺序/工作流 | 实际剩余工作 | 当前可推进部分 | 解锁及验收 |
|---|---|---|---|
| A 证据基线与交付 | strict recount中断于缺上游；历史todo与实现证据需逐条复核；本地后续提交待整理交付 | 恢复仓外固定上游，strict重跑；按T-ID核对源码、测试与来源；只择取已审单commit；整理检查点PR | recount无mismatch/skipped；每个done均有对应独立实跑证据。PR只供审阅，不合并 |
| B Computer runtime与平台 | residency core后仍缺Server/Desktop真实EngineProcess/Supervisor装配、配置、CPU/RSS/pids/disk、scope生命周期 | 实现唯一runtime owner/配置/租约/回收/故障边界与macOS真实进程证据；准备既有Windows/runsc探针 | Windows真实双role/ACL/Job/PE；Ubuntu24.04 runsc/Xvfb与renderer layer-1+2；无隐式fallback。两个平台实测前P1保持红 |
| C Browser/file/shell与acting | 33个Browser operation、computer相关API/env、snapshot/ref、file路径、shell进程树与协议级cancel | 在B的authority入口接唯一tool管线；snapshot generation、TOCTOU、secret typed effect、decision/attempt先于effect、cancel与unknown commit故障注入 | 真实platform/effect证明跨scope=0、audit-before-action=0；不能用closed enum或编译通过替代执行 |
| D Screen/Handover | Server binary WS后仍缺budget、source装配、auth失效hook、Desktop loopback、viewer input/Leptos、停流/性能/fallback | 先完成WS带宽/存活/有界写，再接Engine需求生命周期及2秒停流；与B共享source；接current frame与HumanLease | ticket/replay/origin/window/generation、multi-viewer、IME/drag、非1 scale/resize/tab/stale-frame、fps/p95/p99、secret canary。完整T-BROP-0027保持todo至整条链成立 |
| E Provider/AG-UI/MCP证据 | OpenAI recorded仅1/3；Anthropic/Google与live缺；AG-UI十一族done但正式fixture缺；RMCP relevant conformance未齐 | 审核预留provider候选；补官方协议fixture、malformed/interruption/resume、MCP drift/hostile/cancel/commit证据；复跑真实PG/SafeDialer | 官方source commit/blob/hash/license与消毒可复算；三家production adapter recorded逐字节回放；live凭据缺失单列；不以function-only覆盖全部provider fixture |
| F Desktop Local OAuth与MCP治理 | installed-app/system-browser/random-loopback callback；private egress/admin backend已落但完整产品面未齐；G8 secret retirement | 实现typed Local OAuth、PKCE/state/loopback/lifecycle；补operator-attestation与retirement机制和故障路径 | 真Desktop绑定、真实provider registration/回调/撤销；厂商verification/安全评估独立验收；不能以Server OAuth替代Local |
| G GUI余路由与共享交互 | 8 route、61 UI todo；AppSidebar/AdminSidebar、跨页/channel Composer、markdown/tool boundary与Screen | 优先审核Personal Skills不可变候选；随后admin plugins/skills/credentials；按B/C结果接admin computers；共享Composer与transcript；每批真实浏览器/权限/双语/键盘验证 | 正式route+核心journey+权限；禁止fake动作、foreign内容进DOM、旧response污染。页面journey与formal golden分开闭合 |
| H Sandboxed components | args/JS/network/callback/MessageChannel正向、Desktop单engine多session与预算/a11y fallback | Web真实隔离正负测试；Desktop runtime与同一ScreenHub组合；viewport启停、256MiB/8active/5s/5fps/error预算 | 真renderer与受限主进程不能越scope；三层零egress；组件参数fallback与Escape焦点；跨平台证据 |
| I Desktop/Wry与sidecar发行 | 实际Wry/window/lifecycle、多窗口ACL、PostgreSQL/Engine sidecar build/sign/update、正式identity/capability | 在既有typed assembly上做macOS真实Wry journey、退出/重启/孤儿清理与更新失败测试；准备发行配置供裁决 | 品牌/bundle/deep-link/签名决定后真实installer；Windows运行、key-store；无compile-only冒充可安装发行 |
| J Golden/a11y/i18n | gate已实现；245 baseline、固定Linux/CJK、CDP/xcap、全页AX/键盘/reduced-motion缺 | 随route补可复用capture/AX/键盘harness与macOS截图；保持生产bundle无gallery/golden feature；locale/预算持续验证 | 137 Web含27中文+54 macOS+54 Windows；同bundle摘要、各自baseline/diff评审；正式verify必须实绿 |
| K G2安全与供应链 | 独立SAML/XSW/整体外审、KMS/HSM、Windows原生；MPL/UNIC/vet与发行图未闭合 | source/license/NOTICE/provenance逐包审计、窄修复与明确证据；KMS adapter接口及secret生命周期；准备可复核材料 | 不批量exemption、不降级不变量；真实KMS/Windows与外审无P0/P1；全发行target规定闸门实际通过 |
| L Migration/G8最终闭合 | 原件、授权legacy export、三次production-scale backup/import/restore、RPO/RTO、第二外审、signed发行/runbook | 深验Drizzle/schema/checksum；实现/复核迁移工具、retention、故障恢复与演练harness；维护最终证据清单 | 取得原件/合法数据与平台后实跑三次；签名OCI/installer/atomic sidecar；§24全部勾、§25逐条证据及全部todo归零，最终同步真源/台账 |

这些工作流覆盖所有机器台账；不能只做表内举例而遗漏某个todo。
每次选择下一批时，从九份`parity/*.yaml`和`fixtures/MANIFEST.yaml`读取全部未done条目；
按具体upstream/target分配到以上工作流，遇到不匹配条目必须补工作单，不能删除或减少分母。
577条tests和44条events中有可能存在已实现但缺独立证据的条目，先逐条审计和重跑再决定状态。

## 4. GUI与fixture具体余项

八条route：`T-ROUTE-0009` channel detail、`0011` personal skills、`0017` admin computers、
`0018` admin credentials、`0022` admin plugins、`0023` plugin detail、`0024` plugin tool grant、
`0025` admin skills。`/skills`有外部未提交候选，其余不得被该候选的通过结论覆盖。

21条fixture继续保留各自完整验收语义：

- `T-FIX-0004–0008`：Web英文/中文、macOS、Windows golden与固定容器来源；
- `T-FIX-0010–0012`：AG-UI官方事件族、malformed/drift、transport interruption/resume；
- `T-FIX-0013–0017`：provider skeleton、partial JSON/UTF-8、interleaved tools、error/cancel、commit divergence；
- `T-FIX-0018–0021`：RMCP conformance、catalog drift、OAuth PKCE、hostile payload；
- `T-FIX-0022–0024`：browser closed operation、stale ref、refused side effects；
- `T-FIX-0026`：固定上游baseline原始测试结果。

这些路径目前部分不存在。不能因为相近的后续fixture或模块测试已绿，就不经逐项复核把旧条目改done。

## 5. 外部阻塞与范围占用

| 条件 | 阻塞范围 | 现在仍可做 | 解锁证据 |
|---|---|---|---|
| 两份输入原件缺失 | G0 | 已有来源与证据审计 | 原件归档到`docs/inputs/`且SHA匹配 |
| Windows真机、Ubuntu runsc/Xvfb真环境未提供 | P1、G5、Windows发行/golden、Server isolation | 平台代码/探针、本机证据、协议和故障测试 | 对应真实平台原始输出与pin/provenance；不接受交叉编译替代 |
| vendor凭据/registration/verification | live provider、Desktop OAuth、Drive assessment | 官方recorded fixture、Local OAuth实现、消毒与负向测试 | 经授权live请求与实际回调/撤销/厂商证明 |
| KMS/品牌/签名决定及独立外审 | G2、G6、G8 | adapter/harness/审计包/发行配置草案 | 具体配置裁决、真实托管/签名结果及两次独立报告 |
| 合法legacy export API/生产规模数据 | G3、G8三次演练 | schema/checksum/迁移工具和故障恢复 | 获授权输入、三次演练与RPO/RTO实测 |
| GitHub Actions无新授权 | Actions运行 | 本机定向验证，准备手动工作流 | 用户明确授权后才可dispatch；当前不申请或代派发 |
| Personal Skills尚无候选SHA | 该scope主线集成 | 只读核验交付状态，继续其它scope | 执行方单commit；主控核对已验源码后逐commit择取与重跑 |
| Anthropic/Google已预留未启动 | 两个recorded scope | 依预留台账等待候选或先明确释放占用；继续其它scope | 不并行重复改同一范围，不擅改外部工作树 |

本轮实查Personal Skills仍在R176 `3ff200a`且未提交，符合预留台账的“待不可变候选”状态。
没有将旧工作树切换、rebase、提交或合并，也没有派发新的子代理任务。

## 6. 当轮动作与循环规则

1. 先完成本盘点和strict recount，恢复实际证据起点；记录失败，不沿用旧绿灯。
2. 收口已开始的Screen transport预算分支：实现→定向tests/真实Engine→局限登记→真源/台账同步；
   仅在实际通过后记录新Batch。2秒停流需要Engine命令生命周期，不能把viewer drop当作已停流。
3. 转入Computer runtime装配与Screen需求生命周期；收到Personal Skills候选后可插入独立集成验收。
   对已有AG-UI/MCP实现补正式fixture也是不依赖外部机器的可执行工作，不因P1红而全部暂停。
4. 每批只按变更面跑规定定向检查；scope/acting路径必须有真实负向及audit原子性证据。
   写第一真源的修订记录时按“原表述/问题/修订/证据”记录，不以工作单代替。
5. 本地实现与检查通过后，按仓库纪律push+创建可审PR检查点；不合并、不派发Actions，继续其它余项。
6. 每个检查点重新扫全部todo与外部阻塞。只有§1的总完成条件全部满足，才做最终close；
   当前目标保持active，单批完成不能调用complete。

## 7. 复算入口

```bash
cargo xtask parity-check
OPENBOT_UPSTREAM_DIR=<仓外固定891df72的干净克隆> cargo xtask recount --require-upstream
cargo xtask grok-inventory --check
git rev-parse HEAD:grok-bot
```

本机已重新取得固定上游`891df72f1827454d8b353d108fe5dd2313b7e30d`的干净仓外checkout，
只作oracle/recount，不运行npm。strict当轮实得**160通过/0失配/0跳过**，恢复了R183–R188缺上游时
不能重跑的完整复算；这只证明固定口径一致，不将尚未实施的能力改为done。

按文件复算done/todo/total及全部未done身份：

```bash
python3 - <<'PY'
from pathlib import Path
from collections import Counter
import yaml
for path in [*sorted(Path('parity').glob('*.yaml')), Path('fixtures/MANIFEST.yaml')]:
    entries = yaml.safe_load(path.read_text())['entries']
    print(path, dict(Counter(e['status'] for e in entries)), 'total', len(entries))
    for entry in entries:
        if entry['status'] != 'done':
            print(entry['test_id'], entry['id'], entry['owner'], entry['target'])
PY
```

## 8. 盘点后的检查点

Batch114/R189已完成Server Screen transport预算：Server233/0/0、真实Engine默认heartbeat/idle1/0/0；
新fixture后总数33/21/54，parity仍873/839/1712，overlay1273/431/2/6，strict160/0/0。
该检查点不结束目标，Engine自动停流与production source仍按工作流B/D继续。
用户可派发的三个范围已写成独立任务书并登记预留：Anthropic、Google recorded trace与AG-UI官方事件fixture；
未收到候选前不计实施或验收通过，Personal Skills不重复派发。

## 9. 用户后续裁决：可用Alpha与开源共建

R191新增受控Alpha优先级，首版三类工作闭环均不可删减；长期v4完整目标不变。
后续优先按`2026-09-04-OpenBot开源共建与渐进上线策略-研究方案.md`准备复用接线、实际平台验收、
安装恢复与开源/贡献规范，全部已有todo继续由中央台账保留。源项目只在副本中处理，不改源仓。

### Batch117 / R193 续记

Plugins 的三条 Server Web 路由已有配置/custom/OAuth/grant子面；真实PG43、UI187、Contracts105、Server233
与release中英/错误/unknown/离页/焦点验证通过。为完整性保留三条route todo，不将分层证据当作production
PG/session到GUI整旅程或Desktop宿主证据；完整Bearer产品面、Desktop Local OAuth、Skills和golden/AX继续推进。
当前parity873/839/1712、fixtures35/20/55，strict160/0/0。全范围目标不变，外部阻塞不妨碍其它独立实施。

### Batch118 / R194 续记

Desktop connections读取/curated启用已补typed framing，Desktop112/Contracts105/Server233、Clippy和Windows
cross-check绿；仍缺实际Wry/GUI、Local OAuth、完整Bearer产品面与正式golden。strict160/0/0，
overlay1271/433/2/6；parity873/839/1712与fixtures35/20/55不变。参见Batch118，不关闭总目标。

### Batch119 / R195 续记

先修复已发现的三类GUI密钥响应式明文违规，改DOM输入、非secret校验状态、零化DTO/Serde/JSON与await前释放。
Contracts108/UI188/Server233/Desktop112和release表单通过；旧遮挡/不回显证据不等于所有权证明。
正式AX/golden、浏览器底层副本物理擦除、真实provider/平台/外审仍不能由本批推出。parity/fixtures不变，
overlay1247/457/2/6、strict160/0/0；继续推进完整Alpha与v4工作链。

### Batch120 / R196 续记

Credential admin完成shared typed→PG/Vault→模型/MCP消费与GUI工作链；字段权威/回读/atomic rotate/audit/
本地退休和原OAuth撤销snapshot实际验证。13条原条目闭合，parity886/826/1712、fixtures35/20/55，
overlay1234/470/2/6。G2外审/KMS、原生宿主/平台、完整GUIs/golden和G8仍按原范围继续，不以本批结束目标。
