# OpenBot v4 并行实施预留台账

更新时间：2026-09-04（America/Los_Angeles）

## 1. 权威与集成规则

- 全范围依赖顺序与完成条件见`docs/2026-09-04-v4全范围闭合工作单.md`（R188实际基线盘点）；该工作单只作执行索引，不覆盖两份第一真源或本表scope占用。
- 仓库纪律：`/Users/fushihua/Desktop/OpenBot/CLAUDE.md`。
- v4 第一真源：`/Users/fushihua/Desktop/OpenBot/docs/2026-08-21-OpenBot全量Rust重写终版研究与实施方案.md`，实施前必须读 §24、§25、§28.1。
- 尚未启动的外部分支统一从 R188 文档提交 `8a91b2d5606891ee28db744c8ad7909a5a68b96e` 创建；已经交付或实施中的旧候选保留原基线并只择取单commit，不得从包含其它未审改动的工作树派生。
- 外部分支只交实现、provider 专属 fixture/provenance 与定向测试；`fixtures/MANIFEST.yaml`、parity 台账、v4、`CLAUDE.md`、`README.md` 和移交指南由主控审计后统一更新。
- “分支测试通过”只表示候选实现可审，不等于 v4 子项或 G4/G6 整关完成。

## 2. 当前范围占用

| 状态 | 分支 / 工作树 | 独占范围 | 合并要求 |
|---|---|---|---|
| 主控Batch118/R194本地framing验收 | `feat/2026-09-04-G6-desktop-plugins-framing`，基于`4124380` | Desktop connections读与curated启用、共享closed DTO | Desktop112/Contracts105/Server233绿；Windows仅cross-check，真实Wry/GUI/Local OAuth和G6不勾 |
| 主控Batch117/R193本地子面验收 | `feat/2026-09-04-G6-admin-plugins-ui`，基于`22945bb` | Plugins三条Server Web路由、认证投影、错误/焦点与夹具 | PG43/UI187/Contracts105/Server233及release浏览器绿；三route仍todo，完整Bearer/production整旅程/Desktop/Skills/golden未关；不碰外部A/B或Personal Skills工作树 |
| 主控Batch115/R190生命周期子项已完成 | `feat/2026-09-04-G7-screen-demand-lifecycle`，基于`54847d1` | Screen capture pause/resume + 唯一Rust owner + current lease执行时重验 | T-FIX-0055闭合；production ComputerManager/auth hook/完整G7仍todo；无远端写入 |
| 主控Batch114/R189预算子项已完成，待push授权 | `feat/2026-09-04-G7-screen-delivery-budget`，从`9c40e5b`起，实现`3a89f7e` | Server Screen bandwidth/control-rate/heartbeat/idle/write budget；后续Engine停流按独立证据验收 | T-FIX-0054已闭合；Server233/0/0、真实Engine默认计时1/0/0、strict160/0/0。自动审批拒绝远端push，保持本地；2秒停流、production source与G7整关不得由socket预算代替 |
| 主控Batch104已完成 | `feat/2026-09-04-G4-openai-recorded-trace-audit`，candidate `cb6e2d69b5ea70d4a046d31aea00c3b2bdd02e77`，hardening `5becad90ee71fd6b84f1bbc6f47d1e24fdb3a1dc`，R178 `455a628f77b5ccb77ab54c3fc8199f789727d631` | OpenAI Responses官方recorded trace、decoder兼容、来源/许可/消毒guard | T-FIX-0046与OpenAI 1/3已闭合；Anthropic/Google仍独立预留，见§6 |
| 主控Batch105/R180已完成 | `feat/2026-09-04-G4-rmcp-protocol-cancel`，原implementation `9b03cca38b0841bce46acc6ed1132b2633064cdd`，correction `3a694658a34ef3b9cbeba60fc32907eba8b50a2b`，R180 `6d7369bba2cd5555cb81c1bfbba7b721a3622299`，正文同步 `ed1335984f539c79859f79ea418a9bcd3086f515` | Rust run→RMCP fresh list/call protocol cancel；现代stream-close/旧版notification分流 | T-FIX-0047已闭合并由R180替换旧wire证据；完整T-FIX-0018 conformance、computer/file/shell cancel仍todo，见§7 |
| 主控Batch106/R181已完成 | `feat/2026-09-04-G4-computer-runtime-budget`，selector `2385493d5e2c6472b4470e39d167e60f272c4f6f`，manager `b92c401a540281460c98542a8f29583343746578`，R181 `c136e32fdd9dd5d658ae6f4b4b6671881ee5239f` | 固定上游browser LRU/idle selector + Rust-owned完整scope residency manager | 13条T-TEST与T-FIX-0048闭合；production EngineProcess/Supervisor配置与CPU/RSS/pids/disk仍todo，见§8 |
| 主控Batch107/R182已完成 | `feat/2026-09-04-G6-golden-png-gate`，comparator `79d849f0db7a4b822a56ec4369e638b229345c8a`，gate `a69b3efcf41edd1c7dd0e089885fbd6143fa02a7`，R182 `44755630044ab7fbdb6be915ed127d302a55b78e` | Golden RGBA core + PNG/manifest/diff gate | T-FIX-0003 gate evidence重验证；245张baseline、CDP/xcap与formal verify仍todo，见§9 |
| 主控Batch108/R183已完成 | `feat/2026-09-04-G5-cdp-input-plan-core`，implementation `b9e464013072b4d190e204aebc0f8afd861ba223`，R183 `76b63ebca91abfae8c69c090fbc5f5e2a0b57a37` | BrowserInput → CDP pure parameter plan | T-FIX-0049闭合；engine/live effect与T-BROP-0037–0044仍todo，见§10 |
| 主控Batch109/R184已完成 | `feat/2026-09-04-G5-engine-input-wire`，implementation `71e7feb7005100accf190e16b4bb3f4c79209149`，R184 `b9ed4885800d5faaf1d5e3201c5ca006fedaae89` | protocol-v2 + HumanLease receipt + macOS双role ordinary CDP input | T-BROP-0037–0043与T-FIX-0050闭合；secret/Screen/production/Windows/runsc仍todo，见§11 |
| 主控Batch110/R185已完成 | `feat/2026-09-04-G7-screencast-backpressure`，implementation `c5f22f424efec517f851c3b495d3de215be1e957`，R185 `d6c3361359a9aa0af5e41882cb1a82dbb5641ff0` | protocol-v3 Page screencast + Rust latest buffer/ACK | T-BROP-0030–0032与T-FIX-0025闭合；viewer/WS/fps/production/跨平台仍todo，见§12 |
| 主控Batch111/R186已完成 | `feat/2026-09-04-G7-screen-hub-ticket-core`，implementation `ddf49ee5b2b09b940528614330a228ab68077c0a`，R186 `43a0bf81b53d8866f4368c16fdb0b3943b29ec5b` | 唯一Engine source + Rust ScreenHub共享latest/multi-viewer + viewer ticket authority核心 | T-FIX-0051闭合；T-BROP-0027与production WS/hook/限额/性能/跨平台仍todo，见§13 |
| 主控Batch112/R187已完成 | `feat/2026-09-04-G7-viewer-coordinate-journey`，implementation `49da4d9c8da7db25ae631c332de5fdc517dba8a1`，R187 `aa38e235ada30164043d641aa6270797bdb532fa` | 官方CDP/DevTools单位 + contain viewer coordinate + macOS drag/IME/wheel journey | T-FIX-0052闭合；Leptos/WS/stale-frame/production/非1 scale/跨平台仍todo，见§14 |
| 主控Batch113/R188已完成 | `feat/2026-09-04-G7-server-screen-ws`，implementation `5a7444e1db068d504157260da66ba9696f513dc6`，R188 `8a91b2d5606891ee28db744c8ad7909a5a68b96e` | actor/generation/target绑定的ScreenSession票据 + Server query-free binary WebSocket + production assembly | T-API-0175/0176与T-FIX-0053闭合；production manager source、PG cookie/TLS、Desktop/input/限流/停流仍todo，见§15 |
| 外部候选已修正择取 | `feat/2026-09-04-G5-cdp-input-mapping-core`，原候选 `1ca08993f725310e97c16c9bb77cdd14f1f61a4e` | BrowserInput → CDP 映射核心 | 以`--no-commit`导入，修正17键与unknown multi-unit=0后提交为`b9e4640…`；旧分支不合并，见§5/§10 |
| 外部候选已审计择取 | `feat/2026-09-04-G6-golden-compare-core`，原候选 `ccd97df2945a84a5b79f85675dcc4289670685c8` | golden comparator 核心 | 已择取为`79d849f…`并由`a69b3ef…`补PNG gate；旧分支不再继续，不合并整分支，见§5/§9 |
| 外部调查待修 | `feat/2026-09-04-G6-google-drive-brand`，候选 `d5166abcd651ce66e514f8b1159f1b6d36390f06` | Google Drive 品牌资产与 provenance | 调查结论成立但未交付 SVG，且 diff-check 有尾随空格，见 §5 |
| 主控已审计择取 | `codex/feat/2026-09-04-openai-recorded-trace`，原候选 `b50dd1caa08f9fecb57ca48270dae8cc22e4978b` | OpenAI Responses recorded trace | 已择取为`cb6e2d69…`并由`5becad90…`加固；旧分支不再继续，不合并整条分支 |
| **预留、未启动** | `feat/2026-09-04-G4-anthropic-recorded-trace` | 仅 Anthropic Messages recorded trace | 见 §3.A |
| **预留、未启动** | `feat/2026-09-04-G4-google-recorded-trace` | 仅 Google `streamGenerateContent` recorded trace | 见 §3.B |
| **主控Batch116/R192已修正验收** | 原候选`dd7febe7f03b01f2c3ffa515cfcd752aacfcb864`；主控分支`feat/2026-09-04-G4-agui-official-fixture-audit`，修正提交`2bdc8bd` | T-FIX-0010官方schema/事件fixture | 修正多package.json、三向集合/bytes库存、报告与归属/SLSA表述；fixture5/Agent57、Clippy/fmt/shim绿；不改外部工作树，T-FIX-0011/0012仍todo |
| **外部实现已通过主控独立验收，待单一候选提交** | `/Users/fushihua/Desktop/OpenBot-G6-personal-skills-ui`，`feat/2026-09-04-G6-personal-skills-ui`，当前HEAD仍为R176 `3ff200a2d6f12ebdc1c7e8b6f6b26a8eae4666ea`且改动未提交 | `/skills` route + 四个 personal-skill 组件的 GUI 竖切 | 当前源码/fixture/release Chromium均绿，但没有immutable candidate；执行方只可提交本次已验收范围，主控随后从R188逐commit择取并复核，见§3.C/§16 |

## 3. 新预留任务

2026-09-04用户将使用其它工具并行实施、主控验收。可直接派发的三份独立任务书：

- `docs/2026-09-04-v4外部任务A-Anthropic录制轨迹.md`：沿用下述A预留。
- `docs/2026-09-04-v4外部任务B-Google录制轨迹.md`：沿用下述B预留。
- `docs/2026-09-04-v4外部任务C-AGUI官方事件夹具.md`：新增AG-UI官方事件fixture预留；字母C仅为本轮派发标签，不是下述既有Personal Skills的§3.C。

以上均从R188不可变提交`8a91b2d5606891ee28db744c8ad7909a5a68b96e`创建隔离工作树，未收到执行方启动或SHA前不写“实施中/已交付”。已有Personal Skills仍只等待候选提交，不重复派发。三任务均不得修改中央台账/真源/共享Cargo依赖；主控逐commit独立验收后集成。

### A. Anthropic Messages recorded trace

允许修改：

- `crates/openbot-infra/src/provider/anthropic.rs`（只有真实回放暴露兼容缺口时才做最小修复）；
- `crates/openbot-infra/tests/anthropic_recorded_trace.rs`；
- `fixtures/provider/anthropic-*.sse`；
- `fixtures/provider/anthropic-*.provenance.json`。

验收边界：固定 Anthropic 官方仓库的 exact commit/blob/hash/license；从官方原始 HTTP/SSE fixture 提取 response body，保留字节级 SHA-256；fixture 不含 API key、请求正文、账号/客户标识或可验证 secret hash。测试必须通过 production `AnthropicProvider` + `SafeDialer` 离线 loopback 回放，并证明整块、非规则分块、逐字节分块输出一致，usage 单调且 terminal 恰一次，坏帧不回显 vendor body。

### B. Google streamGenerateContent recorded trace

允许修改：

- `crates/openbot-infra/src/provider/google.rs`（只有真实回放暴露兼容缺口时才做最小修复）；
- `crates/openbot-infra/tests/google_recorded_trace.rs`；
- `fixtures/provider/google-*.sse`；
- `fixtures/provider/google-*.provenance.json`。

验收边界：只接受 Google 官方仓库中可证明由 replay/golden 录制得到的 `streamGenerateContent?alt=sse` 响应；固定 exact commit/blob/hash/license。若官方材料不是原始 SSE 或无法证明 vendor capture，任务必须停在“证据不足”，不得拿手写 JSON 冒充 recorded trace。通过 production `GoogleProvider` + `SafeDialer` 离线 loopback 回放，至少覆盖 text/functionCall/usage/finish，整块、非规则分块、逐字节分块输出一致，header-only key、terminal 恰一次、坏帧不泄露 vendor body。

### C. Personal Skills GUI 竖切

该任务已由外部工具在`/Users/fushihua/Desktop/OpenBot-G6-personal-skills-ui`实现；当前分支
`feat/2026-09-04-G6-personal-skills-ui`仍基于R176
`3ff200a2d6f12ebdc1c7e8b6f6b26a8eae4666ea`并有未提交改动，主控不得切换、rebase或修改该工作树。
2026-09-04主控已对这份未提交源码完成逐文件、攻击面、Rust/WASM/release bundle与第一方托管
Chromium独立验收，结论见§16。该结论只覆盖当时工作树，不替代immutable candidate：执行方仍须在
原隔离分支只创建一个候选commit并提交SHA；主控收到后在R188文档点
`8a91b2d5606891ee28db744c8ad7909a5a68b96e`之后逐commit审计择取，不得整分支合并。

目标只包括 v4 `/skills` 与 GUI v2 §6.2 的四个组件：

- `T-ROUTE-0011`；
- `T-UI-0067` `edit-skill`；
- `T-UI-0068` `new-skill`；
- `T-UI-0069` `skill-agents`；
- `T-UI-0070` `skill-fields`。

允许修改：`crates/openbot-ui/src/features/skills/**`、UI 的 `features/mod.rs`、`api.rs`、`app.rs`、
`shell/app_sidebar.rs`、`locales/en.json`、`locales/zh-CN.json`，以及仅为真实浏览器 fixture 所需的
`crates/openbot-server/src/bin/openbot-ui-fixture.rs`。不得改 contracts/application/infra/Server 产品
HTTP；Batch100 已提供 `GET /api/plugins`、skill save/delete、grant/revoke/for-Agent typed 后端。

闭合语义：

1. `/skills` 只把当前 actor 自己的 personal skills 列为可管理，把 deployment skills 列为只读；
   admin 在该个人页面也不得把其他 actor 的 personal skill 画进 DOM。
2. `?new=true` / `?edit=<slug>` 由 URL 持有 DetailPanel 状态，创建优先于编辑；query/slug 必须有界，
   hard reload 可恢复，missing/not-owned 不构造空编辑表单。
3. create/edit 共用四字段组件；create 永远发送 `global=false`，edit 锁住作为 key 的 slug；title/
   summary/instructions 与 Rust 后端的 256 B/4 KiB/64 KiB、control/NUL/empty 规则一致，但 Server
   仍是最终 authority。instructions 不进日志、localStorage、URL 或 error text。
4. delete 只对 personal row 提供；deployment row 无 edit/delete/grant affordance。成功写入后接受
   authority receipt 并 refetch，失败保留可重试 UI；stale/unmounted response 不得改新页面状态。
5. `skill-agents` 只列 actor owned Agents；grant/revoke 只发 closed `kind=skill/ref/agentId`，不自报
   actor/role。选中态遵守 GUI 真源“文字/图标/对勾”，不得照搬上游实心彩色按钮。
6. AppSidebar 增加真实 `/skills` destination，但不得宣称 AppSidebar 总项、slash-command Composer、
   `/admin/skills`、Plugins UI、formal golden 或 G6 完成。

验收至少包括 Rust/WASM 单测与 release Chromium journey：loading/empty、personal/deployment 分区、
create→reload、edit slug locked、只对 own Agent grant/revoke、other actor/Agent 不进 DOM、delete、
deployment 只读、双 query precedence、missing edit、失败重试、键盘焦点/命名、en/zh-CN、1280×800 与
600×900 overflow/duplicate-id/console error 均为零。若没有真实浏览器证据，不得把上述五个 T-ID 改
done。外部分支仍不得修改 parity/v4/CLAUDE/README/移交指南，由主控审计后统一落账。

## 4. 外部交付清单

每条任务交付时必须提供：

1. 分支名、工作树绝对路径、基线 SHA、候选提交 SHA；
2. `git diff --name-status <base>..<candidate>` 与 `git status --short`；
3. source URL、source commit、Git blob/hash、原始字节数/SHA-256、fixture 字节数/SHA-256、许可证；
4. 消毒规则与 secret/canary 反向扫描结果；
5. 定向 `cargo test`、`cargo clippy -p openbot-infra --all-targets --all-features -- -D warnings`、`cargo fmt --check` 原始摘要；
6. 未运行项与未解决风险；不得声称 G4、三家 trace 或 v4 全量完成。

主控收到后依次做来源复核、逐文件代码审计、攻击面/secret 检查、独立重跑与台账重算；审计通过前不进入主线。

## 5. A/B/C 首轮主控审计（2026-09-04）

三条候选提交的共同父提交均为 `0f4f1e54bb28d774d5928b48ada141b7a868039c`
（R172），不是本轮要求的 R176 `3ff200a2d6f12ebdc1c7e8b6f6b26a8eae4666ea`。
因此禁止整分支合并；受影响目标文件在 R172→R176 之间无变化，主控只可择取单个候选提交并在
当前基线重跑。

### A：Golden 比较核心

- 审计结论：候选实现本身通过。
- `git diff --check`、`cargo fmt --all -- --check` 通过；`openbot-testkit --lib=19/0/0`；
  all-target/all-feature Clippy `-D warnings` 通过。
- 逐项核对 GUI v2 §10.4：RGBA 任一通道差严格 `>16`、差异像素严格 `>0.1%`、任一
  `8×8` 全差异块独立失败、mask 同时退出分子/分母与差异块，完全 mask/坏 stride/溢出均
  fail-closed。
- 本条是候选审计时边界；主控随后在Batch107只择取该commit为`79d849f…`，再以`a69b3ef…`
  补PNG解码、manifest/mask allowlist与diff gate。截图与245张baseline仍todo，见§9。

### B：Google Drive 品牌调查

- 审计结论：来源与阻断判断成立，但“任务 PASS/品牌完成”不成立。
- 主控独立复现官方当前 PNG 为 4,985 字节、SHA-256
  `39e2c15449e7fa75ebe3a29f3f99e2e9ee11b5ef36aebf4dda3d30e484635495`；旧 DevSite SVG
  为 1,145 字节、SHA-256
  `2897441606067de8560ef21fc1357ce314331f5ac233760b22e3de224cc0bb09`。
- 官方应用内规则只明确列出 Drive 动作按钮或文件存储标识；当前 connector row 用途不能据此
  擅自扩张。故不入库 SVG、不改 provenance/parity 是正确决定，`google-drive` 继续 todo。
- 候选文档第 3 行有 trailing whitespace，`git diff --check` 未通过；修复前不择取。

### C：BrowserInput → CDP 纯映射

- 审计结论：边界设计与 secret 隔离正确，但存在阻断 parity 偏差，暂不接纳。
- `openbot-computer --lib=28/0/0`、映射子集 `10/0/0`、fmt 与 all-target/all-feature Clippy
  均通过；这些测试把错误行为写成了期望，不能替代固定上游对照。
- 固定上游 `screencast.ts` 的 `VIRTUAL_KEY_CODES` 实为 17 项（包含显式空格→32），候选改成
  16 项；行为虽碰巧相同，但不满足当前“键码表逐项固定”的证据要求，中央 parity 备注中的
  “16”也需同批纠正为“17”。
- 更重要的是，固定上游对未知多 UTF-16 单元键返回 virtual key code `0` 并继续发送；候选对
  `F1`/`Meta`/非 BMP 键直接返回 `UnknownKey`。v4 未裁决这一收窄，故违反 preserve/fidelity。
  主控已在Batch108按该要求修正并以`b9e4640…`提交；仍不得把纯映射冒充实际
  CDP/Screen/Handover完成，见§10。

## 6. OpenAI recorded trace 主控审计结果（2026-09-04）

- 原候选基线为R172，主控只择取单commit到R177，不合并旧分支；择取后SHA为
  `cb6e2d69b5ea70d4a046d31aea00c3b2bdd02e77`。
- 主控从OpenAI官方`openai-dotnet@19d0a3cb8e0cf0f3137a5c56c3c70a0c3f6c96f5`
  独立复核13,211-byte source record、Git blob、SHA-256、commit time、MIT与source test；36段
  ResponseBody重建后与9,070-byte fixture逐字节相等。
- `5becad90ee71fd6b84f1bbc6f47d1e24fdb3a1dc`补NOTICE/SPDX、一对一closed provenance guard、
  SSE terminal delimiter规则与done改名/索引漂移/全程无name三条fail-closed单测。
- OpenAI unit=`7/0/0`、production SafeDialer recorded replay=`1/0/0`、Infra lib=`328/0/0`，
  Clippy/fmt/guard、parity与fixed-upstream strict均绿。R178=
  `455a628f77b5ccb77ab54c3fc8199f789727d631`；fixtures=`24/22/46`。
- 结论只为OpenAI **1/3**。Anthropic/Google任务仍未启动；不得复用OpenAI fixture、手写vendor事件或
  把function-only trace冒充T-FIX-0013/text/reasoning覆盖。

## 7. RMCP run protocol cancel 主控结果（2026-09-04，R180纠偏后）

- 原implementation=`9b03cca38b0841bce46acc6ed1132b2633064cdd`，protocol correction=
  `3a694658a34ef3b9cbeba60fc32907eba8b50a2b`，R180=
  `6d7369bba2cd5555cb81c1bfbba7b721a3622299`，正文同步=
  `ed1335984f539c79859f79ea418a9bcd3086f515`。
- private/non-serde run signal经Rust-minted ToolCallId registry进入共享ApplicationService工具管线；
  Server/Desktop production assembly共用同一registry，不新增transport自报槽或第二执行路径。
- pre-network取消socket0；modern 2026-07-28 fresh list/call关闭exact response stream且notification POST=0；
  legacy 2025-11-25 fallback才发送exact requestId/reason的`notifications/cancelled`。固定rmcp 3.1.4
  首event前worker stall由SafeDialer只对modern tool request drop exact future规避；post-send call固定
  Unknown/reconciliation，signal delivery失败不冒充成功。R179相反wire断言已废止。
- R179 Application/Agent/Infra/Server/Desktop=`166/57/328/222/131`且Desktop3 ignored；R180 Infra/Agent=
  `328/57`，RMCP no-PG=`8/0/3 ignored`、PG17.11 SCRAM完整=`11/0/0`、assembly PG=`1/0/0`、transport parity8；
  fixtures=`25/22/47`、strict=`160/0/0`。
- 本批没有占用Anthropic/Google/Personal Skills外部范围。T-FIX-0018完整官方conformance和
  computer/file/shell protocol/process-tree cancel继续保留，不得把T-FIX-0047扩大解释。

## 8. Browser runtime residency budget 主控结果（2026-09-04）

- selector=`2385493d5e2c6472b4470e39d167e60f272c4f6f`，manager=
  `b92c401a540281460c98542a8f29583343746578`，R181=
  `c136e32fdd9dd5d658ae6f4b4b6671881ee5239f`。
- 固定上游三文件blob/bytes/SHA已独立登记；13条browser-eviction测试逐项转done，另加stable
  equal-time与overflow负向。
- `BrowserRuntimeManager`按完整ComputerSecurityScope digest+ComputerId+generation持有opaque driver
  handle；同scope cold launch合并，cap/idle只close inactive LRU，全slot leased时effect前busy，
  generation/stale/collision/launch rollback/stop/shutdown均有closed测试，driver canary不进错误Display/Debug。
- fixture=`1579 B`/SHA-256 `1dc49180…`并明确production assembly/resource budget=false；Computer=
  `43/0/2 ignored`，Clippy/fmt绿。parity=`861/849/1710`、tests=`470/577/1047`、fixtures=
  `26/22/48`、overlay=`1286/416/2/6`、0违反/警告。
- 本批没有占用Anthropic/Google/Personal Skills/Golden/CDP外部范围。Server/Desktop→EngineProcess/
  Supervisor、`COMPUTER_MAX_BROWSERS`/`COMPUTER_BROWSER_IDLE_MS`与CPU/RSS/pids/disk仍todo；不得把
  manager core扩大解释为完整Computer runtime budget、G4/G5或P1/P2完成。

## 9. Golden PNG gate 主控结果（2026-09-04）

- 原候选`ccd97df2945a84a5b79f85675dcc4289670685c8`基于R172，主控只择取单commit到R181
  coordination基线；择取后SHA=`79d849f0db7a4b822a56ec4369e638b229345c8a`，不合并旧分支。
- `a69b3efcf41edd1c7dd0e089885fbd6143fa02a7`新增`cargo xtask golden`，从seed导出exact
  245-path，锁PNG magic/16MiB/4096/64MiB、viewport尺寸、无symlink、reviewed mask与ignored diff；
  formal verify必须先有固定容器sha256/CJK版本与baseline/actual相等inventory。
- `image 0.25.10`只在testkit xtask PNG-only optional图；Cargo.lock新增image/byteorder-lite/moxcms/
  pxfm四包、总829，四包零build.rs。UI dependency与六target deny guard绿；cargo-vet仍373 unvetted，
  未添加exemption。
- core/gate/testkit all-features=`18+7+137/0/10 ignored`，xtask=`102/0/0`，Clippy绿；真实
  1024×1024 PNG自比0差异。`check-manifest=matrix245/masks0/ready=false`，formal verify按预期exit1。
- T-FIX-0003 evidence重验证但fixture数不增；parity=`861/849/1710`、tests=`470/577/1047`、
  fixtures=`26/22/48`、overlay=`1283/419/2/6`、0违反/警告。非strict recount=`71/0/89 skipped`；
  无上游故strict未跑，CI/Actions未运行。
- 本批没有占用Anthropic/Google/Personal Skills/CDP外部范围。Web CDP capture、macOS/Windows xcap、
  245张baseline、固定Linux/CJK、AX/键盘/reduced-motion与G6/G8继续todo；详见Batch107与R182。

## 10. CDP input pure plan 主控结果（2026-09-04）

- 原候选`1ca08993f725310e97c16c9bb77cdd14f1f61a4e`基于R172。主控从R182以
  `cherry-pick --no-commit`导入，解决Batch106 `eviction`模块冲突后先修正再一次提交；错误候选版本
  没有作为中间commit落库，最终implementation=`b9e464013072b4d190e204aebc0f8afd861ba223`。
- 固定上游`screencast.ts`=6906 bytes、blob `9bc27c11fc1b4cd296f7fc9df412aea0bedbbb22`、
  SHA-256 `be79bde5007f03f37e3b99a1ef1388ba672d684ba32ec2b9090c417f9f47f566`；明确17项键表
  含空格→32，unknown multi-unit返回0。候选的16项与`UnknownKey`均已纠正。
- closed plan只表达mouseMoved/Pressed/Released、mouseWheel、keyDown/rawKeyDown/keyUp与insertText；
  SecretInsert普通plan拒绝，key/code/text与insert正文Debug脱敏。
- fixture=1315 bytes/SHA-256 `e6cf14b947d86cd2bc458adc27b75272b619cc90c4f41558c0a3b1438424463f`，
  并把product engine wire/live CDP effect/ScreenHub固定false。mapping=`11/0/0`、Computer=
  `54/0/2 ignored`、Clippy/fmt/diff绿。
- 新增T-FIX-0049后fixtures=`27/22/49`；parity=`861/849/1710`、browser=`7/43/50`、overlay=
  `1283/419/2/6`、0违反/警告。非strict recount=`71/0/89 skipped`，strict/CI/Actions未跑。
- 本批没有占用Anthropic/Google/Personal Skills/Golden外部范围。T-BROP-0037–0044、authenticated
  engine/live effect、坐标/ScreenHub、P1/P2/G5/G7继续todo；详见Batch108与R183。

## 11. Engine protocol-v2 live input 主控结果（2026-09-04）

- implementation=`71e7feb7005100accf190e16b4bb3f4c79209149`；不兼容input扩展显式把
  descriptor/generated module/bundle/handshake升为protocol/release epoch2，没有偷改v1语义。
- fresh、non-Clone/non-serde HumanLease receipt只在Rust核actor/AuthGeneration/scope/generation/epoch/
  expiry后铸造；EngineProcess再核computer/generation/active tab与执行时expiry。cross-scope、exact expiry、
  oversize与ordinary SecretInsert均在operation/pipe前拒绝。
- shim=`483/600 LOC`；所有`sendCommand`必须literal且集合恰为Page enable/runtime probe/capture与三条
  Input method。input八kind逐variant exact-key/number/string/code校验；ACK与独立frame通道解耦。
- 官方macOS archive=`122102881 B/ee939d…`，v2 bundle ASAR=`22819 B`/header=`cb289d04…`/
  fuses=`000011001`，bundle/verify/signature/integrity绿。Browser/Component双Seatbelt role真实=
  `2/0/0`，fixture同跑合计`3/0/0`；hover/active/text/Backspace/F1=0/insert/scroll均有页面效果，
  TCP LISTEN/PID/profile lock全0。
- Contracts=`104/0/0`、Computer lib/default=`56+1/0/2 ignored`、xtask=`103/0/0`；native三组Clippy、
  Windows check/Clippy、fmt/diff绿。Windows/runtime本身未跑。
- T-BROP-0037–0043与T-FIX-0050 done后parity=`868/842/1710`、browser=`14/36/50`、fixtures=
  `28/22/50`、overlay=`1276/426/2/6`、0违反/警告；recount=`71/0/89 skipped`，strict/CI/Actions未跑。
- 本批没有占用Anthropic/Google/Personal Skills/Golden外部范围。T-BROP-0044、Page.startScreencast/
  ScreenHub、production Computer assembly、Windows/runsc runtime及P1/P2/G5/G7继续todo；详见Batch109/R184。

## 12. Formal screencast/backpressure 主控结果（2026-09-04）

- implementation=`c5f22f424efec517f851c3b495d3de215be1e957`；frame wire不兼容扩展显式升
  protocol/release epoch3，OBFRAME2/76-byte header含timestamp/device size+scale/page scale/scroll/
  CDP session，descriptor单源钉Page screencast七项参数。
- shim只写authenticated binary frame，不提前ACK；Rust在分配前验长度并验完整metadata/scope/
  generation/sequence/JPEG，publish进Mutex保护的size-one watch buffer并计算drop后才回frame_ack。
  input ACK与frame channel保持独立。
- macOS官方archive=`122102881 B/ee939d…`，v3 ASAR=`29806 B`/header=`02972340…`/
  fuses=`000011001`；shim=`596/600 LOC`，bundle/verify/signature/integrity绿。
- Browser/Component双真实Seatbelt role最终conformance=`4/0/0`（role2）；多次400ms slow-consumer
  received=ack均成立（49..52帧），drop37..40，stop exact replay、TCP LISTEN/PID/profile lock全0。
- Contracts=`104/0/0`、Computer lib/default=`58+2/0/2 ignored`、xtask=`103/0/0`；native与Windows
  Clippy绿，Windows runtime未跑。
- T-BROP-0030–0032与既有T-FIX-0025 done后parity=`871/839/1710`、browser=`17/33/50`、fixtures=
  `29/21/50`、overlay=`1273/429/2/6`、0违反/警告；recount=`71/0/89 skipped`，strict/CI/Actions未跑。
- 本批没有占用Anthropic/Google/Personal Skills/Golden外部范围。viewer ticket/WS、多viewer、fps/
  latency/fallback/production composition、secret typed effect、Windows/runsc及P1/P2/G5/G7继续todo；
  详见Batch110/R185。

## 13. ScreenHub/viewer ticket core 主控结果（2026-09-04）

- implementation=`ddf49ee5b2b09b940528614330a228ab68077c0a`；launch强制Rust authority提供
  ScreenAudience，active Engine session只交出一次带完整stream key与共享ingress state的source。
- ScreenHub每stream持一份latest，多个viewer共享同一Arc并按sequence gap累计coalesced frame；显式cap
  把active viewer与pending ticket共同计数。viewer `OBSCRN01` binary排除scope/computer/tab/ticket/CDP
  session。
- ticket由OS CSPRNG直接填充唯一16-byte Vec并移动进zeroizing SecretBytes；Hub只保存SHA-256。固定30秒、
  一次消费，绑定tenant/actor/auth generation/stream及Server origin或Desktop origin/window generation；
  generation推进关闭旧source与双viewer。
- 复核中修复source watch clone消费不推进共享ingress consumed sequence造成的虚假drop；最终macOS
  Browser/Component双真实Seatbelt role conformance=`5/0/0`（role2），received=ack为68/69、engine slow
  drop均37，双viewer同sequence/同Arc、各自skip>0。
- Computer lib/fixture=`60+3/0/2 ignored`；native/Windows Clippy、Linux target check、protocol-v3、
  shim596/600、ASAR/fuses/integrity/signature、parity与recount均按变更面绿。
- 新增T-FIX-0051后fixtures=`30/21/51`；parity/browser/overlay仍`871/839/1710`、`17/33/50`、
  `1273/429/2/6`，0违反/警告；strict/CI/Actions未跑。
- 本批未占用Anthropic/Google/Personal Skills/Golden外部范围。T-BROP-0027、Server/Desktop真实WS、
  production auth invalidation hook、frame/bandwidth/idle cap、最后viewer 2秒停流、fps/p95/fallback/
  assembly、secret typed effect、Windows/runsc及P1/P2/G5/G7继续todo；详见Batch111/R186。

## 14. Viewer coordinate/input journey 主控结果（2026-09-04）

- implementation=`49da4d9c8da7db25ae631c332de5fdc517dba8a1`；官方DevTools commit=
  `036dd84bc4fdfb0fd4be2a5ddb3fe37ef24939cd`，InputModel/ScreencastView blob/bytes=
  `cfa97617…/3982`、`978df09a…/38774`，固定mouse/wheel screenZoom与DOM-only pageScale/scroll公式。
- Rust contain mapper拒letterbox/right-bottom、坏aspect、非finite、decoded16..16384外与canvas越界；
  DPR恰消，viewport/document坐标分开，wheel不加scroll/不除pageScale，输出携frame sequence。
- macOS Browser/Component双真实Seatbelt role用映射坐标命中button，依次执行pressed→moved→released，
  `日本語🔐`只走Input.insertText，viewer wheel200映射CDP400后scrollY真实增加；conformance=`6/0/0`
  （role2），两role received=ack69/drop37。
- 首版pure因把合法canvas origin0误作非正而实得1/3；修成4/0后，官方源又推翻“pageScale进入CDP
  input”的错误期望。最终按正确公式重跑Computer/真实/跨target，错误中间结果不计通过。
- fixture=`2707 B/8a03fd68…`；Computer lib/fixture=`64+4/0/2 ignored`；native/Windows Clippy、Linux
  check、protocol/shim/bundle/verify绿。新增T-FIX-0052后fixtures=`31/21/52`；parity/browser/overlay仍
  `871/839/1710`、`17/33/50`、`1273/429/2/6`，0违反/警告；recount=`71/0/89 skipped`。
- 本批未占用Anthropic/Google/Personal Skills/Golden外部范围。Leptos canvas/WS/stale-frame/production、
  非1 DPR/pageScale硬件、nonzero-scroll pointer硬件、resize/navigation/tab、secret typed effect、Windows/
  runsc及P1/P2/G5/G7继续todo；strict/CI/Actions未跑。详见Batch112/R187。

## 15. Server ScreenSession/binary WebSocket 主控结果（2026-09-04）

- implementation=`5a7444e1db068d504157260da66ba9696f513dc6`，R188文档=
  `8a91b2d5606891ee28db744c8ad7909a5a68b96e`。Contracts新增只含computer/generation/tab的
  `ScreenSessionTarget`与Server/Desktop绑定请求；一次性ticket的Debug不显正文且Drop清零。
- Server `POST /api/screen/sessions`在读body前完成fresh trusted-Origin校验，authority binding由transport
  注入并回`no-store`；`GET /api/screen`拒绝query，只接受base+唯一ticket双subprotocol、响应只选base，
  ticket在101前消费。输出仅发Computer单源`OBSCRN01` latest binary；inbound上限1KiB，当前client
  Text/Binary、generation撤权与source关闭均以1008 fail closed。
- production Server main共用同一ScreenHub/ScreenSessionService；Desktop显式保留
  `NoScreenSessionAdministration`，没有伪造loopback完成。真实ignored纵切让managed Electron Browser帧经
  UDS→Hub→Application票据→Axum TCP WebSocket到二进制客户端，最终`1/0/0`。
- Contracts/Application/Computer/Server/Desktop=`105/166/65/226/131`且0失败（Computer/Server/Desktop
  既有ignored如实保留）；screen transport=`4/0/0`。workspace check、八crate Clippy、WASM、Windows
  Clippy、Linux target、WebSocket/发行target/engine protocol-shim-bundle-verify与fmt均绿。
- 新增T-API-0175/0176与T-FIX-0053；fixture=`1970 B`、SHA-256=
  `6e7365a084c8ee9b33ed916ccfbd5e5cca399a23189a8cdd0be5ec784bfb2a2d`。API=`96/80/176`、
  parity=`873/839/1712`、fixtures=`32/21/53`、overlay=`1275/429/2/6`，0违反/警告；recount=
  `71/0/89 skipped`，strict/`cargo xtask ci`/Actions未跑。
- production manager→Engine source、真实PG cookie/TLS、Desktop loopback、viewer input、带宽/idle、最后
  viewer 2秒停流、fps/p95/fallback与跨平台仍todo；T-BROP-0027、完整G7/P1/P2/G5均不勾。详见
  `docs/2026-09-04-G7-Server-screen-WebSocket-batch113.md`与R188。

## 16. 外部任务 D Personal Skills GUI 预集成验收（2026-09-04）

- 验收对象为`/Users/fushihua/Desktop/OpenBot-G6-personal-skills-ui`、分支
  `feat/2026-09-04-G6-personal-skills-ui`、R176基线
  `3ff200a2d6f12ebdc1c7e8b6f6b26a8eae4666ea`上的未提交工作树。7个tracked修改加
  `features/skills/**` 7个新文件全部落在§3.C允许范围；`git diff --check`绿，Grok tree=
  `86f5a85f560f721677fa7e587a67ac0ffc036cb5`、非Grok `package.json=1`、npm锁/日志=0。
- 主控逐文件复核确认：personal/deployment/foreign投影分离；URL优先级与missing/not-owned拒绝；四字段
  UTF-8 byte/control/NUL规则与production backend一致；create固定`global=false`；写前再取authority、
  receipt/202/网络未知统一权威refetch；App-lifetime journal阻止重叠与离页旧响应污染；grant只接受投影后的
  owned Agent。源码未引local/session storage或正文日志，instructions只在表单、typed body与内存postcondition
  hash中出现。
- 独立重跑UI=`194/0/0`、testkit fixture=`13/0/0`；UI与fixture all-target Clippy `-D warnings`、UI
  wasm32 check、fmt、tools verify、i18n=`839`、design-lint=`111 Rust/74 icons`均绿。release/offline/locked
  Trunk连续两次构建八文件SHA逐字相同；CSS=`365`，bundle=`1992243/115524/740216/1/0`（WASM gzip/
  CSS/fonts/external-inline），均在预算内。
- 第一方托管Chromium实际覆盖loading→empty、personal/deployment分区、create→hard reload、edit slug锁定、
  own visible/hidden Agent grant+revoke、foreign actor/Agent与system Agent不进DOM、失败保值重试、202未知提交
  和解、延迟写入后离页/返回收敛、双query由new优先、missing edit、删除二次确认/Escape/最终删除、面板与
  roster焦点、中英切换及reload保持。1280×800与600×900均为main/nav/h1各1、横向溢出0、duplicate-id=0；
  broken labelled-by、未命名可聚焦项、nested interactive与console warning/error均0。
- fixture proof最终为`saveCalls=6/saves=5`（一次预写失败）、`grantCalls=2`（同一自有Agent true→false）、
  `removeCalls=1`、`revision=8`；所有save均`scope=personal/global=false`，proof只有正文byte计数，不含
  title/summary/instructions/actor或浏览器canary。deployment row始终无link/button affordance。
- **尚未落主线，也没有关闭T-ROUTE-0011或T-UI-0067–0070**：外部工作树仍未提交，故不存在可复核的
  candidate SHA。执行方现在可以只把本次已验收改动创建为单一commit；主控收到SHA后仍须验证commit与
  已验工作树一致，再从R188逐commit择取、重跑并统一更新parity/v4，禁止整分支合并。

## 17. 外部任务C主控验收（Batch116/R192）

原候选基于R188，SHA=`dd7febe7f03b01f2c3ffa515cfcd752aacfcb864`；主控独立重跑5项fixture与Agent57项均绿。
但原候选违反单package.json并缺机械三向source对拍、报告计数与许可证表述不实，因此不原样择取。
主控只导入允许文件并修正，六份官方源保持原字节，package manifest仅作文件名变更；T-FIX-0010据此done。
fixture39行/33type/3scenario=4426B，SHA256=`6c13dad31a54a85a7c38b2d8e7623b9799a31d546eb0502d58000244708ec5ab`。
原工作树不修改、不合并；详细来源和修正见Batch116及重写的外部交付报告。版权归属进一步确认、
Sigstore验签、T-FIX-0011/0012和完整G4/G8不由此关闭。

当前主控已同步到R192：Batch115实现`f7947e4`、开源/Alpha研究`506121c`、Batch116修正验收`2bdc8bd`。strict本轮160/0/0；fixtures35/20/55。全部仅本地提交，未突破此前远端push审批限制。

## 18. 主控 Plugins 管理子面（Batch117/R193）

本批继续R192，新增三条Server Web Plugins路由，未占用外部A/B recorded trace或Personal Skills。
副作用前失败/202 unknown不自动重放，离页状态由App owner处理；Rust投影认证三态，键盘/对话框焦点经过实测修复。
真实PG/loopback43、UI187、Contracts105、Server233绿，strict160/0/0；parity873/839/1712和fixtures35/20/55不变。
三route继续todo：仍缺production PG/session→GUI整旅程、完整Bearer产品面、Desktop部分transport/Local OAuth/宿主和golden/AX。
详情与复算命令见Batch117报告；本批无push/PR合并，不改变v4总目标或受控Alpha准入要求。

## 19. Desktop Plugins基础framing（Batch118/R194）

已补Batch117的connections读取/目录启用缺口；未实施或冒充Local installed-app OAuth与实际Wry窗口。
Desktop112/Contracts105/Server233、Clippy与Windows cross-check绿，strict160/0/0；overlay1271/433/2/6。
parity873/839/1712、fixtures35/20/55不变，三Plugins route与总目标继续。未碰外部A/B/Personal Skills和源项目。
