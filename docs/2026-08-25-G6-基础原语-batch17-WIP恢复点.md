# Batch 17 WIP 恢复点：G6 基础原语

> 分支 `codex/2026-08-25-G6-foundation-primitives`，base = Batch 16 正式 head
> `a064012ff4fe12dcd1f7ada72177728435be1df2`。仅本地定向测试，不运行
> `cargo xtask ci`，不派发 Actions，不处理 `grok-bot`。

## 本批范围

- [x] `Button`：四 variant、三 size、native Space/Enter、closed forced visual state，显式
  loading/invalid/selected/open/disabled；
- [x] `Field` + `Label`：Field 单点铸造 control/description/error id 与
  `aria-describedby`/`aria-invalid`/disabled 接线；
- [x] `InputGroup` + `Input`：prefix/suffix slot、focus-within、textbox
  focus/disabled/invalid；
- [x] `Item`：link/button 两种闭合语义，hover/selected/disabled，selected 不使用彩色 surface；
- [x] `Separator`：semantic/decorative 与 horizontal/vertical closed variants；
- [x] `Skeleton`：`aria-hidden`，reduced-motion 静止；
- [x] `Switch`：APG role/Space/explicit aria-checked/disabled，neutral body；
- [x] `Textarea`：multiline、disabled/invalid、Rust/WASM 自动增高且上限十行；
- [x] `design-gallery` feature 下 `/_design` 承载状态/键盘/AX fixture，production bundle 必须
  无该 route 字节。

## 明确不在本批

Dialog/Sheet/Menu/Combobox/Select/Sidebar/Tooltip/MessageScroller 等复杂 APG；任何未完成条目继续
todo，不用静态 HTML 或 test-only handler 冒充 production primitive。

## 当前机器证据

- UI all-features `30/0/0`；WASM all-features check、UI/testkit all-targets Clippy
  `-D warnings`、fmt 全绿；
- i18n=`275` keys、design-lint=`29 Rust/74 icons`、css-check=`67 classes`；production
  bundle wasm gzip=`371205`、CSS=`36123`、fonts=`740216`、external/inline=`1/0`；
- production WASM `_design` byte=0，design-gallery WASM `_design` byte=1；bundle gate 会对前者
  非零直接判红；
- 真实 Chromium：Button Enter/Space count `0→1→2`；Item Enter/Space `0→1→2`，disabled
  不增；Switch Space `false→true`；Field label/description/error ID exact，Input disabled，
  InputGroup 真 focus outline；Textarea 16 行 `client=216/max=218/scroll=374`，capped=true；
  decorative separator 不进 AX，horizontal/vertical semantic separator 均可见；三种 Skeleton
  `aria-hidden=true`，正常 motion 下 animation=`ob-skeleton-pulse`，reduced-motion CSS 单测钉
  `animation:none!important`；
- DOM：duplicate ID/unnamed focusable/nested interactive/remote resource/horizontal overflow
  `0/0/0/0/0`，main/nav/h1=`1/1/1`，当前 hashed WASM console error=0；
- `transport_parity` 的无 wildcard command 台账补齐 Batch12–16 十个专项命令为显式 `None`；
  不把它们冒充 channel transport 对拍。
