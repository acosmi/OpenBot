# OpenBot P0-code Batch52 交付记录

日期：2026-08-28（America/Los_Angeles）

分支：`feat/2026-08-28-P0-code`

基线：PR #34 已先以 merge commit 合入，分支起点 `28c0b0f`。

## 1. 范围与结论

本批只完成 v4 §19.1 的 P0-code 六项产物，不写 P1 shim、不启动 Engine 产品能力、不改 `grok-bot/`、不派发 GitHub Actions。

| P0-code 产物 | 本批结果 |
| --- | --- |
| v4 exception-only overlay | `parity/overlay/v4.yaml` 已建立；`parity-check` join 全部 test_id、拒绝显式 carry、校验 R124 三行初值，并把 branch diff 命中的 done target 强制为 revalidate |
| checked/poisoned HumanLeaseEpoch | `next` 使用 checked 递增；耗尽立即清 lease，human input 与 Bot acting 同一错误 fail-closed，只有更大的 `ComputerGeneration` 可恢复 |
| Grok tier-1 inventory | `cargo xtask grok-inventory` 机械生成 `inventory/grok/files.yaml`；每文件含 path/family/nonempty_loc/maturity，另有整树 SHA-256 防同 LOC 内容漂移 |
| Electron fetch/verify | 当前平台只从 pin 中选择官方 zip；校 size/SHA-256、safe unzip、`--version`；同时逐项 join 五平台 pin 与上游 SHASUMS 副本 |
| Electron shim 静态规则 | `electron-shim-check` 锁 exact file allowlist、非空 LOC ≤600、唯一工作区 package manifest、零 lockfile、Electron/Node import allowlist、forbidden API/string 与 Rust-owned protocol hash；P1 目录缺席时明确报告，不冒充 shim 已存在 |
| Engine T-ID | browser-operations 新增 boot/role/confinement/Browser conformance 4 条；components 新增 render session/Component conformance 2 条；全部 todo，recount 同步 |

## 2. 可复算结果

- parity：`693 done / 999 todo / 1692 total`；browser-operations `7/43/50`，components `13/11/24`。
- overlay：`carry=1684 / revalidate=6 / split=2 / superseded=0`；8 条显式 exception 中，6 条由本分支 `control.rs` diff 机械要求 revalidate。
- inventory：2,110 文件、92 family；maturity 为 production 1,494 / partial 296 / generated 241 / artifact-only 79；tree SHA-256 `bb00e6367b2f4c8eb53d624edbb0a16c8e779df17721839c0e11c0a152e5732f`。
- `grok-bot/` Git tree 保持 `86f5a85f560f721677fa7e587a67ac0ffc036cb5`，`git diff -- grok-bot` 为空。
- macOS arm64 Electron：122,102,881 B；SHA-256 `ee939d1564d83d61032b3b3cb23af4e46005a4900c91f0695f7ed793f0ce6e83`；`--version` = `v43.3.0`。
- Cargo.lock 只给既有 `zip 2.4.2` 增加一个 `openbot-testkit` 直接消费边；新增 package = 0。工作区（排除只读参考树 `grok-bot/`）`package.json` = 0、Node/npm lockfile = 0。

## 3. P0-code 五个 xtask 退出证据

| 命令 | 本轮结果 |
| --- | --- |
| `cargo xtask parity-check` | 通过，0 违反；机械打印 overlay 四类计数与 `git diff 要求 revalidate=6` |
| `cargo xtask grok-inventory --check` | 通过；2,110 文件与钉死 tree/生成文件逐字同步 |
| `cargo xtask engine fetch` | 通过；官方 zip 下载、size/SHA-256、safe unzip 与内置 verify 均绿 |
| `cargo xtask engine verify` | 通过；五平台 pin/SHASUMS join、当前 archive SHA-256 与 `v43.3.0` 均绿 |
| `cargo xtask electron-shim-check` | 通过；P1 shim 明确 absent，规则 active，工作区 package.json 0，LOC 0/600 |

Codex 的默认命令沙箱不能解析 GitHub DNS，且会以 SIGABRT 阻断 macOS GUI-subsystem Electron；同一钉版命令获宿主执行权限后下载和独立 verify 均绿。这里记录的是最终宿主实跑结果，不把沙箱失败写成产品回归，也不把首次 fetch 内嵌 verify 冒充独立重跑。

## 4. 定向质量闸门

| 命令 | 本轮结果 |
| --- | --- |
| `cargo test -p openbot-computer --locked` | 9 passed / 0 failed / 0 ignored；含 `epoch_exhaustion_clears_lease_and_poison_fails_closed_until_generation_advances` |
| `cargo test -p openbot-testkit --features xtask --bin xtask --locked` | 86 passed / 0 failed / 0 ignored |
| `cargo clippy -p openbot-computer --all-targets --all-features --locked -- -D warnings` | 通过 |
| `cargo clippy -p openbot-testkit --features xtask --bin xtask --locked -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| `cargo xtask recount` | repo 口径 71 passed / 0 mismatch；88 条 upstream 命令因未设置 `OPENBOT_UPSTREAM_DIR` 明确 SKIPPED，总计 159 |
| `git diff --check` | 通过 |

## 5. 明确未做

- 未运行 `cargo xtask ci`，未派发 GitHub Actions（R63 manual-only）。
- 未跑完整 workspace test；本批按 computer + xtask/Cargo/parity 变更面定向实跑，不把未跑写成绿。
- 未实现或运行 `cargo xtask engine bundle`；rebrand、ASAR、fuses、integrity、release epoch 属 P1 真 shim/bundle。
- 未创建 `crates/openbot-desktop/engine-shim/`，未新增任何 JavaScript、`package.json` 或 npm/Node 构建链。
- 未人工 census，未复制、翻译或输出 `grok-bot/` 的源码文本；inventory 只含机械文件元数据与摘要。

下一批入口是 v4 §19.1 **P1 Engine 最小闭环**，不是继续扩 P0 规则，也不是新增 Grok 产品能力。
