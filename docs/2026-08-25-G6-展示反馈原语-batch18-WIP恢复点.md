# Batch 18 WIP 恢复点：展示与反馈原语

> 分支 `codex/2026-08-25-G6-display-feedback-primitives`，base = Batch17 正式 head
> `774773dfb792b552714d01517ec2aa89e0128ed4`。只跑本地定向测试，不运行
> `cargo xtask ci`，不派发 Actions，不处理 `grok-bot`。

## 本批范围

- [x] Message compound primitives：article/group/avatar/content/header/footer + start/end；
- [x] Bubble：User/Assistant 两种无边框中性外框、forced hover；
- [x] Kbd：semantic `<kbd>`、闭合 key/modifier、宿主平台 Primary 映射与 i18n；
- [x] Avatar：24/32/40、same-origin image 或 initials、SHA-256 principal palette 8 值、亮暗
  对比度；
- [x] Toast：role=status、5s 自动消失、手动关闭、generation 防旧 timer 关掉新 toast；
- [x] Tooltip compound：button/link trigger、hover/focus 400ms、Escape/blur/leave、role=tooltip、
  compile-gallery forced open。

## 不在本批

MessageScroller、Combobox/Dialog/Menu/Select/Sheet/Sidebar；它们继续 todo。Toast/Tooltip 的 test-only
gallery 只能验证真实 primitive 行为，不冒充正式 golden 或业务 accepted:false 全接线。

## 当前机器证据

- UI all-features=`40/0/0`；WASM all-features、UI/testkit all-targets Clippy `-D warnings`、
  fmt 全绿；
- i18n=`297` keys、design=`36 Rust/74 icons`、css=`85 classes`；production bundle
  wasm gzip=`371457`、CSS=`40781`、fonts=`740216`、external/inline=`1/0`；
- Avatar 8 palette × light/dark 对 fg 的 16 组 WCAG AA 全绿；same principal 两个尺寸均
  palette=1，另一 principal=3；initials=`AL/张`，sizes=`32/24/40`；remote image 负向拒绝；
- Chromium AX：articles=`Ada Lovelace:start / 张三:end`；Bubble assistant/user border=0；Kbd
  `⌘K` 读作 `Command K`，Enter/Shift+Enter/Esc/Slash 均本地化；
- Toast 初始 5s 自动 dismiss count=1；reopen 3s→manual close count=2→立即 reopen，旧 deadline
  后仍 visible/count=2，新 deadline 后 hidden/count=3，证明 generation；preview 强制 status
  `aria-live=polite`；
- Tooltip click/focus immediate hidden、450ms visible+role=tooltip+aria-describedby，Escape 后
  hidden/describedBy=NULL；blur-before-delay 保持 hidden；button Enter activation count 增；
- 最终 gallery WASM `dbc618…` console error=0；duplicate ID/unnamed/nested interactive/remote/
  overflow=`0/0/0/0/0`；
- Cargo.lock package 仍 822；只给 openbot-ui 记录已锁 `wasm-bindgen 0.2.127` direct edge，
  SHA-256 已在 lock 的 sha2 原边从 build+runtime 共用，无新 package。
