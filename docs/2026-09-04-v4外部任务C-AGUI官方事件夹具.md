# 外部任务 C：AG-UI 0.0.57 官方完整事件族 fixture

状态：本轮新增预留，供用户派发；尚未启动。日期：2026-09-04。

先读取以下第一真源，不依赖旧聊天快照：

- `/Users/fushihua/Desktop/OpenBot/CLAUDE.md`
- `/Users/fushihua/Desktop/OpenBot/docs/2026-08-21-OpenBot全量Rust重写终版研究与实施方案.md`，尤其§24、§25、§28.1与任务相关章节。
- `/Users/fushihua/Desktop/OpenBot/docs/2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md`
- `/Users/fushihua/Desktop/OpenBot/docs/2026-09-04-v4并行实施预留台账.md`

固定基线为R188 `8a91b2d5606891ee28db744c8ad7909a5a68b96e`。在自己的独立worktree/checkout创建下述分支，必须核验基线；不要切换、rebase、修改主控工作树或Personal Skills工作树。主控当前Screen分支的未提交改动不属于本任务。

全程零npm，不运行npm/npx或引入node_modules/锁文件；保持`grok-bot` tree=`86f5a85f560f721677fa7e587a67ac0ffc036cb5`和仅一个非Grok `package.json`。禁止`cargo xtask ci`、Actions dispatch、擅自合并PR；不要改Cargo依赖/lock、中央parity、`fixtures/MANIFEST.yaml`、v4、CLAUDE、README、移交指南或预留台账。中央证据状态由主控独立验收后统一更新。

只提交本任务允许范围，最终一个候选commit即可；不得用整分支合并交付。发现超出允许范围的生产缺陷，先交最小可复现证据和修复建议，由主控处理，不自行扩范围。

## 任务

分支：`feat/2026-09-04-G4-agui-official-event-fixture`。
建议独立路径：`/Users/fushihua/Desktop/OpenBot-G4-agui-official-event-fixture`；若已存在先检查，不覆盖。

只处理`T-FIX-0010`的官方事件族fixture前置与协议回放。R172/Batch98已经完成十一事件族production vertical；不要重写AG-UI runtime、durable interrupt或native migration。实际公共入口是`openbot_agent::agui::AguiDecoder`与`RemoteAguiProvider`。

只允许新增：

- `fixtures/agui/official-event-family.jsonl`：每行一个原生AG-UI event object，顶层有`type`，不是自造case wrapper。
- `fixtures/agui/official-event-family.provenance.json`：schema版本、官方来源、case来源/本项目生成说明、byte/hash与scenario边界。
- `fixtures/agui/schema-0.0.57/**`：少量所需官方schema源文件及完整许可证/来源说明，仅作fixture数据，不是生产TypeScript或可执行构建链。
- `crates/openbot-agent/tests/agui_official_fixture.rs`：通过现有公共production decoder/adapter离线消费。
- `docs/2026-09-04-AGUI官方事件fixture-外部交付.md`。

不得改production代码、Cargo依赖、tools/pins、中央provenance/NOTICE/台账；需要这些改动时在交付文档列精确建议，交主控处理。

验收：

1. 从官方Git/release取证固定`@ag-ui/core@0.0.57`对应schema，而不是只读取本仓常量后照填。证明版本与exact commit/blob/hash/license关系，保留离线可复核副本。禁止npm/npx安装或构建。
2. 官方schema事件type集合、fixture出现的type集合、本仓`AGUI_EVENT_TYPES`三者逐字相等；不仅比数量。每种类型至少一条符合官方必需字段/类型的合法样本。样本可按官方schema由本项目确定性构造，须明确标为protocol corpus，不能冒充vendor recorded/live。
3. 为彼此冲突的终态/方便型chunk/thinking/reasoning族定义独立合法scenario；scenario边界放伴随provenance，保持JSONL每行都是事件。不能把所有事件强塞进一个无效run。
4. 逐scenario调用真实`AguiDecoder::ingest/finish`验证lifecycle/identity/terminal，明确预期normalized语义；覆盖RunError/interrupt等非成功终态时不要把合法错误事件误当解析失败。
5. 用现有公开`RemoteAguiProvider`搭测试transport时，只能将其称为协议adapter证据；验证open payload保持untrusted、tool result不能执行local tool、error正文不进入closed failure。不能冒充PG/UI/SafeDialer生产纵切。
6. 确认schema字段与R167/R169/R170/R171/R172当前retention/projection/resume规则一致。旧MANIFEST关于“contracts反序列化”或骨架的描述可能过时；记录差异交主控裁决，禁止为迁就旧文复制第二套业务协议。
7. 不占用`malformed-and-drift.jsonl`或`transport-interruption.jsonl`，不关闭T-FIX-0011/0012。若合法官方case暴露现有实现缺陷，提交失败复现与建议，不改生产实现隐藏它。

必跑：`cargo test -p openbot-agent --test agui_official_fixture --locked`、`cargo test -p openbot-agent --lib --locked`、`cargo clippy -p openbot-agent --all-targets --all-features --locked -- -D warnings`、`cargo fmt --all -- --check`。source与fixture的SHA用本机确定性命令复算；不得为了hash测试新增依赖。

## 交付与验收

执行到底，给出候选SHA，不停在“未提交但测试通过”。交付：

1. 任务名、分支、worktree绝对路径、基线SHA、候选SHA。
2. `git diff --name-status 8a91b2d5606891ee28db744c8ad7909a5a68b96e..<candidate>`、`git diff --check`、`git status --short`结果。
3. 官方source URL、exact commit/blob、原始与提取后字节数/SHA-256、LICENSE/copyright、确定性提取规则；消毒前提与canary扫描结果。不得交API key、customer请求或可验证secret hash。
4. 每个验收命令、退出码、passed/failed/ignored计数与原始输出位置。若基线已有失败，须给差异复现；不能只写“全部通过”。
5. 明确未运行、未完成和证据不足项。候选通过不等于对应T-ID、G4、G6或v4整关关闭。

主控收到后重新核来源/许可/字节、逐文件审计、独立重跑，审计通过才逐commit择取并更新台账。不要改动其它工具的任务范围。
