# Wrok Bot 记忆数据层复用评估与台账

日期：2026-09-05。用户明确允许参考、复制适配 CrabCode 的 Rust 记忆数据管理层，并要求持续更新台账。
该授权已记录，实施中不重复请求同一授权。首版继续按 R204 优先 macOS。

## 本轮核验

只读固定来源提交 `0a3f1aac5a27d1f389700b48a220a5a3c0fe6589`；不使用源仓未提交改动。
已核到真正被记忆请求入口调用的混合检索、scope内排序、结果多样性处理，以及生成结果后的去重调用。
增量处理记录、来源关系和派生结果维护也有可评估实现。此阶段是源码和调用关系核验，未在本项目宣称
迁移或运行通过，也没有运行或修改源仓。

同时发现必须区分的层次：部分session抽取/检索实现虽定义了完整trait和单测，但源项目当前主编排
并未直接调用这些实现；实际生产路径在自己的检索集成与分层处理器中。选型以实际调用链为依据，
不能把模块名、源码注释或单元测试等同于生产接线。源码头的许可证声明只是来源审计输入，
复制时仍逐文件记录完整来源链、修改和目标hash，保留第三方声明。

精确原路径、文件字节和hash仅存本机忽略目录 `target/qa/memory-source-20260905/` 的
`SOURCE_PROVENANCE.json`，当前10个只读评估快照；产品目标文件均为空、product_copied=false。
公开文档只记录能力、固定提交和适配结论，不上传源项目原始文件、内部错误或配置。

## 适配台账

| 编号 | 来源能力 | Wrok Bot当前基线 | 适配决策与优先级 | 当前状态 / 退出证据 |
|---|---|---|---|---|
| MREF-01 | 混合检索、多语言文本召回、可观察降级 | `memory_admin::recall`已做PG全文、tag、owner/scope和过期过滤 | 优先评估中文召回与相关性；保持PG为权威存储，派生检索结果必须按当前ACL回查 | 调用链已核；待本项目中英查询基线、召回率/时延和隔离负向对照 |
| MREF-02 | scope内分数归一、时效与来源权重、多样性重排 | 当前PG rank后按recency稳定排序 | 选择可拆纯Rust排序策略，先过滤authority再排序；仅“真实用于上下文”的反馈可影响权重 | 源码已读；待本项目确定性fixture、有限值/预算和顺序验证 |
| MREF-03 | 内容指纹、精确及近似去重、派生索引维护 | 记忆有supersedes、来源和删除状态，尚无该复用差量 | 优先同owner/同scope的重复识别；不把不同来源事实自动合并，不以清理直接删除用户权威记录 | 实际去重调用已核；待来源保留、并发写、删除/纠正不复活测试 |
| MREF-04 | 每会话处理进度、水位、重放与孤儿清理 | native run/event/outbox已有持久身份 | 适配为现有PG事务/游标语义；不照搬文件锁或另建文件真源 | 源码已读；待restart/cancel/replay/checksum证据 |
| MREF-05 | 来源、重复与关联关系投影 | memory已带source thread/message与supersedes | 后续增强解释与管理的后端投影，GUI需求交前端；不把知识图谱列为macOS首版硬前置 | 候选登记；未新增API、表或产品完成量 |
| MREF-06 | 后台分层抽取、压缩与演化 | v4 §4.3首版只有显式记住入口，summary与事实分开 | 拆分适用的压缩/验证机制；新增自动写入能力需独立规格、授权语义、预算和退出证据 | 候选登记；当前不启用隐式学习或把模型推断作为用户事实 |

优先把MREF-01–04能直接改善首版可靠性的部分落入已有Rust层；评估发现会拖延三条macOS核心故事、
或需要新产品协议/新基础设施的部分单列后续版本，不整套搬入源码目录。源项目能力有价值，
实际复用价值由本项目的适配成本与运行证据判断。

## 与既有第一真源的衔接

- Rust/ApplicationService仍是唯一业务入口；tenant、actor、Bot/thread scope及撤权由本项目铸造。
- PostgreSQL仍是记忆、来源、状态和处理进度的唯一权威数据库；新增搜索缓存必须可重建，不能成为第二真源。
- 事实、偏好、summary和模型建议保持各自语义。纠正/删除/过期后不因旧索引或重试复活。
- 模型、嵌入或后台任务若需外部调用，走本项目已有出口、凭据、预算和取消边界。
- 用户复用授权不等于重新发布整个源项目；每个进入产品的文件补完整SOURCE_PROVENANCE和本项目验证。
- 本表属于来源/实施跟踪，不进原parity分母。没有以“评估完成”替代“实现完成”，原memory与v4闸门不改绿。

下一步先建立当前PG召回的中英输入与隔离基线，再选择最小适配差量。后端接口有变化时同步前端窗口，
本窗口不设计或实施记忆页面。
