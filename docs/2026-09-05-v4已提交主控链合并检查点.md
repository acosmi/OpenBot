# v4 已提交主控链合并检查点

日期：2026-09-05（America/Los_Angeles）。本文件记录本轮用户授权的推送/合并范围和合并前实跑证据，不是发行通过声明。

## 1. 授权与范围

用户本轮明确要求“合并一波”，处理尚未推送合并的主控积压；该指令授权本次已提交主控链的推送、创建汇总 PR 和 merge commit 合并。
目的仓库已核为 `https://github.com/acosmi/OpenBot.git`，默认分支为 `main`。
不扩大到未验收外派候选、未提交在制代码、Actions 派发、许可证切换或产品发行。

核验时远端 main 为 `2accbbe4364618bec5c0d323124f9b828c570448`；已提交主控候选为
`c5c0e98fe433949cbc1e22a8acb1afdfc88ae56d`（R201 / Batch125 后的 Alpha 排期交接文档提交）。
两者为直接祖先关系，候选领先 86 个提交，净 diff 为 270 文件、66,705 行新增、2,237 行删除；
其中 fixtures 为 19,475 行新增，docs 为 6,570 行新增。这些是差异行数，不是任务数量或完成率。
本检查点文档及其索引增量另计。

| 待合并 PR | 核验的 head SHA | 合并安排 |
|---|---|---|
| #73 | `985836725fe625b3f57e2558c9ebff782e5c3866` | 先以 merge commit 合入 main |
| #74 | `ad22e76b3f55fa8d1f7258b488d1af4bd9ca066d` | #73 合入后将 base 改为 main，再核 head 合并 |
| #75 | `957fc1da331da4b080810d4cb95349be657b0622` | #74 合入后将 base 改为 main，再核 head 合并 |
| 后续主控汇总 PR | `feat/2026-09-05-v4-checkpoint-integration` | 覆盖 #75 后 R167–R201 的 79 个既有提交及本轮检查点文档；此前三项合入后以 main 为 base |

汇总候选在独立工作树复核，未把原工作树的 10 文件 helper 加载器改动加入。
本次合并的 engine protocol/release epoch 仍是 4/4；原工作树的 epoch 5 / schema 2 仍是待验收在制差量。
E 的远程准备分支及其它外部工作树不修改，继续按任务书验收。

## 2. 合并前实际验证

所有测试使用 Rust 1.98.0、locked/offline 依赖和 `CARGO_INCREMENTAL=0`；本轮编译设置
`CARGO_PROFILE_DEV_DEBUG=0`、`CARGO_PROFILE_TEST_DEBUG=0` 控制磁盘占用，未关闭 debug assertions。
本轮没有运行 `cargo xtask ci`，没有派发 Actions。

| 验证面 | 本轮实得 | 边界 |
|---|---|---|
| 九个受影响业务库 all-features lib tests | Agent 57、Application 166、Computer 67、Contracts 109、Desktop 134、Domain 372、Infra 328、Server 235、UI 188；合计 1,656 passed / 0 failed / 6 ignored | Computer 3 与 Desktop 3 ignored 不算通过；各套件的最终成功结果合计，不重复计数重跑 |
| Agent / PG / durable runtime | `agent_runtime_postgres` 10/0/0 | 真 PG17.11、合成 provider/协议端点；不是 vendor live 或真实桌面发行 |
| 同一 ApplicationService transport 对拍 | `transport_parity` 8/0/0 | 既有 fixture 边界，不冒充 GUI 用户旅程 |
| Infra 集成 | Anthropic recorded 3、credential admin 4、MCP OAuth 3、MCP protocol 11、OpenAI recorded 1、scoped egress 15；共 37/0/0 | 真隔离 PG 与本机 HTTP/TLS/WS；供应商录制回放与 live 分开 |
| Clippy | 上述九 crate `--all-targets --all-features -- -D warnings` 退出 0 | 编译器另报既有 proc-macro-error2 future-incompat 提示，不据此宣称供应链整关通过 |
| 格式与台账 | fmt、diff-check、parity-check 退出 0；strict recount 160/0/0 | parity 886 done / 826 todo / 1,712；fixtures 40 done / 20 todo / 60 |
| Engine / Grok | protocol check、shim 与 inventory 通过；shim 595/600、3 文件、非 Grok package.json 恰 1 | protocol hash `a2bd3a3978a650e199294437aba1661c7bb0ed0c8861ade85a09ff9a49a1252c`；固定 Grok tree 未变 |
| 生产边界 guards | SafeDialer、provider recorded trace、Application assembly、Tauri background assembly 均退出 0 | 不替代 Windows/runsc、真实 Wry 或完整隔离/发行矩阵 |

初次工具沙箱运行中，Desktop 3 项与 Infra 15 项因本机 Keychain、进程或 socket 权限失败；
使用本机权限重跑后分别为 134/0/3 和 328/0/0，原失败日志保留，没有修改断言或把失败改成 ignored。
PG 初始化先被工具沙箱共享内存限制拒绝；初次 Unix socket 配置又被 production 单 TCP parser 按契约拒绝。
最终使用本轮新建 PG17.11、仅 `127.0.0.1` 随机端口、随机测试密码及 SCRAM 认证实跑。
无业务库或真实 provider 凭据参与；结束时额外临时数据库为 0、专用实例已停止、临时密码文件已删除。

Gitleaks 8.30.1 对 `origin/main..c5c0e98` 扫描（100% redact、无 allow 注释豁免）退出 1，
恰一处 `generic-api-key`：提交 `a7b720171e670999f005b6bd2782e542e011d0c1` 的
`crates/openbot-infra/tests/scoped_egress.rs` 中 `Sec-WebSocket-Key` 示例 nonce。
主控亲读调用点并核对 [RFC 6455 §1.3](https://www.rfc-editor.org/rfc/rfc6455#section-1.3)，确认是协议公开示例，
不是认证凭据；记录为误报，不声称扫描器原始退出码为 0，也不增加全局豁免或改写历史掩盖发现。

原始日志及清理回执保存在本机忽略目录 `target/qa/merge-20260905/`；可复算命令为本表对应
`cargo test --locked --offline`、`cargo clippy`、`cargo xtask recount --require-upstream` 等，
upstream 固定为 `891df72f1827454d8b353d108fe5dd2313b7e30d`。
PG 测试须新建隔离集群并显式设置 `OPENBOT_TEST_DATABASE_URL`，不得重用已删除的测试密码。

## 3. 合并后的接续与真实边界

本轮只把已提交主控成果归并到 main，保持提交 ancestry；不用 squash/rebase，不删除外部工作树，不覆盖原 helper 差量。
合并时逐项重读远端 head/base/state，并用 exact head 限定 merge；合并后重新 fetch main 核 ancestry 和树摘要。
核验的 main、#73、#74、#75 与汇总候选的 workflow tree 均为
`2a0f9ec97266a8ada08d430a537c2233cc2d1651`，唯一 workflow 为 manual-only，推送合并不授权 dispatch。

合并不关闭 Alpha A0–A7、v4 G0–G8 或 §25 DoD，不把上述定向测试当作完整供应链、golden、外审、签名或平台验收。
下一窗口继续按 `docs/2026-09-05-外派验收与Alpha上线优先级重排-移交单.md`：
先验收外派交付，再按实际证据拆分上线阻断项与后续 v4 补齐项；helper 加固仍须单独完成验收。
实际 PR 号、merge SHA 和最后远端状态以 GitHub 与本机合并回执为准，不在合并前预写“已合并”。
