# Batch 25 WIP 恢复点：layout 组四个业务组件

> 日期：2026-08-25。分支 `codex/2026-08-25-G6-layout-components`，base =
> Batch24 正式 head `f42bdc9`。只跑本地定向测试；不运行 `cargo xtask ci`，
> 不派发 Actions，不处理 `grok-bot`，不修改/暂存/提交并行出现的 `docs/assets/`。

## 本批唯一范围

- T-UI-0061 `layout/detail-panel`：受控主/详情双栏、closed width、关闭动作、命名 `aside`、
  关闭时不可聚焦/不持有 detail 子树；
- T-UI-0062 `layout/page-shell`：三种内聚版式、单一 `h1`、可选同源返回链接、
  `PageSection/PageRows/PageEmpty`，并接入现有 production Approval 页；
- T-UI-0063 `layout/row-mark`：只表达供应商/第三方标识的中性 tile；
- T-UI-0064 `layout/stagger`：纯 CSS mount-only entrance，40ms 间隔、12 项 cap、
  `prefers-reduced-motion` 全局静止，不引入 runtime JavaScript/Web Animations 伪实现。

## 构造性边界

- 不勾 T-UI-0121 `motion`：本批只关闭 layout 的两个 motion 消费面，上游 sign、
  transcript、app-sidebar、channel detail、AgentPresence 等真实消费面仍未迁移；
- 不勾 route/golden：现有 `/approvals` 接线只证明 PageShell 在真实页面可达，
  不代表31 route journey 或 27 页视觉 oracle；
- 不增加任意颜色、任意 class、远程资产或第二套 GUI。

## 计划证据

- Rust unit/host + WASM check + all-targets Clippy；
- `i18n-check` / `design-lint` / `css-check` / `bundle-budget`；
- compile-only `/_design` 真实 Chromium 键盘/AX/DOM/视口检查，明确不冒充 golden；
- strict recount/parity 只在实现和证据同时成立后更新。
