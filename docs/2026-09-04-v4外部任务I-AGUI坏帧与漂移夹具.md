# 外部任务 I：AG-UI 坏帧与漂移夹具（无需联网调研）

先读同目录 `2026-09-04-v4第二轮外派任务-总则.md` 和所列第一真源。
固定基线 `87d84bb85d0056dfa4dcc2b35be4c2a610a55ae3`；分支 `feat/2026-09-04-G4-agui-malformed-fixture`。
对应 `T-FIX-0011`；外部任务 C 的官方合法基线已经在 R192 验收，本任务使用已入库资产即可。

## 本地输入与范围

无需外网下载、文献调研、Node 或 provider key。只读：

- `fixtures/agui/official-event-family.jsonl`、同名 provenance。
- `fixtures/agui/schema-0.0.57/`（33 类型官方 schema、LICENSE 与固定哈希）。
- `crates/openbot-agent/src/agui.rs`、`remote_provider.rs`、`tests/agui_official_fixture.rs`。
- v4 §7.5、§21.2、R69/R70、R164/R166–R173、R192；`fixtures/MANIFEST.yaml` 的 T-FIX-0011。

允许修改：

- 新增 `fixtures/agui/malformed-and-drift.jsonl` 与 `malformed-and-drift.provenance.json`。
- 新增 `crates/openbot-agent/tests/agui_malformed_fixture.rs`。
- `crates/openbot-agent/src/agui.rs`：仅真实成对样本暴露的最小兼容/安全错误修复，保留所有既有事件族、终态和预算；不改 runtime/transport/DB。
- 新增 `docs/2026-09-04-AGUI坏帧漂移-外部交付.md`。

不得修改合法基线或其哈希、Cargo、中央台账、GUI、task J transport 文件。生产修复若需穿过以上边界，交最小复现由主控处理。

## 验收

1. 每个 case 记录本地合法 scenario/事件身份、单一 mutation 操作、预期稳定结果；同时保存只差这一处的正例，机械校验确实只有声明变化。
2. 至少覆盖 unknown type、先引用后定义、同逻辑项换 ID、字段类型错误、JSON 截断；兼顾 event 顺序、terminal 后事件、超限输入等已存在边界。
3. 使用 production `AguiDecoder`，不得另写 fixture 专用 parser；验证 whole/不规则边界下相同结果，并明确 JSON decoder 与 SSE framing 各自覆盖范围。
4. unknown type 的可观察跳过、已知坏帧的固定错误或其它已裁决降级必须清楚；无 panic、无隐式建立权威、无 canary/原文进入 Debug/Display/普通日志。若当前实现只是无记录丢弃，与台账要求的差异必须指出，不能通过改预期隐藏。
   特别核对旧 T-FIX-0011 notes 的“跳过”与 R164/R168 error 竖切的 unknown-event 拒绝语义；存在冲突时按较新 R 行保留安全拒绝，提交旧台账修订建议，不擅自放宽 production decoder。
5. message/tool/run/interrupt ID 漂移不能绑定到别的已存在项；mutation 后状态不得部分污染下一条合法事件；终态不重复或复活。
6. corpus 明确是本项目基于官方 schema 的 **negative mutation**，不能叫 vendor recorded/live。固定 source 继承链、fixture 字节/SHA，正文不包含秘密/客户输入。
7. 用现有缓存执行 `cargo test -p openbot-agent --test agui_malformed_fixture --locked --offline`，官方 fixture 与现有 Agent 定向回归，Agent all-target/all-feature Clippy `-D warnings`、fmt、diff-check。

若缓存缺失，交精确缺包记录，不自动联网补依赖；本任务没有新的 Cargo 依赖需求。
本次纯 decoder/corpus 不证明 PG、网络或真实 UI 不崩；未覆盖的 T-FIX-0011 产品面单列，主控最后裁定是否闭合。
