# E 任务 Windows 远程交接

日期：2026-09-04。状态：主控准备并推送任务分支，Windows 真机执行尚未开始。

- 仓库：`https://github.com/acosmi/OpenBot.git`。
- 分支：`feat/2026-09-04-P1-windows-runtime-audit`。
- 代码/协议固定基线：R196 `87d84bb85d0056dfa4dcc2b35be4c2a610a55ae3`；Engine protocol/release epoch为4。
- 分支额外准备提交只增加本文件、E任务书、第二轮总则，不改变任何源码、Cargo、workflow、pin或参考树。
- 用户已授权主控本次push；没有授权Actions、合并PR、force-push或其他在制分支上传。

## Windows 同事开始方式

在新的短路径目录中执行，不覆盖现有clone。关闭自动换行转换以保留fixture原字节；Git长路径仅作用于本仓。

```powershell
git clone --config core.autocrlf=false --config core.longpaths=true --single-branch --branch feat/2026-09-04-P1-windows-runtime-audit https://github.com/acosmi/OpenBot.git OpenBot-Windows-E
Set-Location OpenBot-Windows-E
$taskEBase = git rev-parse HEAD
git rev-parse HEAD
git merge-base --is-ancestor 87d84bb85d0056dfa4dcc2b35be4c2a610a55ae3 HEAD
git diff --name-only 87d84bb85d0056dfa4dcc2b35be4c2a610a55ae3 HEAD
git rev-parse HEAD:grok-bot
```

预期ancestor命令exit0，diff只有上述3个docs文件，Grok tree等于
`86f5a85f560f721677fa7e587a67ac0ffc036cb5`。将首次HEAD与主控回执SHA核对并保存，后续不pull/rebase追主线。

## 先读与执行

1. 仓根 `CLAUDE.md`。
2. `docs/2026-08-21-OpenBot全量Rust重写终版研究与实施方案.md`（尤其R127、R184–R190、§24、§25）。
3. `docs/2026-08-22-OpenBot-GUI设计系统与视觉规格-方案.md`。
4. `docs/2026-09-04-v4并行实施预留台账.md`、本分支第二轮总则、E任务书。

macOS绝对路径只是主控文件位置，Windows使用本checkout的对应相对路径，并在交付里写明本机映射。
基线预留台账早于第二轮任务，E的启动与计数以本交接说明/任务书为准；不要自行修中央台账。

使用Rust工具链文件钉的版本，按E任务书运行Windows boundary、Engine fetch/bundle/verify及真实两role conformance。
只安装运行这些定向任务确需的Windows构建工具；不运行整个workspace CI，也不为此安装npm、改用WSL或关闭sandbox。
原始平台日志留本机，入交付只保留消毒后的可复算机器证据；真实结果缺项或失败就明确报告，不写“平台已通过”。

## 交回主控

在首次`$taskEBase`上创建一个本地实现候选commit，附分支/主机平台/路径、候选SHA、每条测试结果和剩余缺口。

```powershell
git rev-list --count $taskEBase..HEAD
git diff --check $taskEBase..HEAD
git diff --name-status $taskEBase..HEAD
git status --short
```

第一条预期1，diff仅允许E任务书范围。另报告R196→HEAD的完整diff和commit数（准备文档1条+实现1条）。
未获单独授权不push、force-push或合并。异机主控需要获取实际commit内容时，可将该单一候选导出Git补丁交用户转交，
不能只提供主控对象库没有的SHA；补丁必须先检查只包含本次允许源码/消毒证据，不包含原始私有日志或用户文件。

不要把已存在的Windows原生电脑控制重新实现；本任务核验OpenBot的Windows Engine安全边界与运行闭环。
