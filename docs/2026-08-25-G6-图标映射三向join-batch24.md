# G6 Batch 24：46 条 Lucide 图标映射三向 join

> 日期：2026-08-25。分支 `codex/2026-08-25-G6-icon-ledger-join`，base =
> Batch23 正式 head `af7e255b3b9477e81e48ab2f23641862314447ac`，实现 checkpoint
> `f23595b0d3b112226167443faee5eec25bfd4f45`。本批关闭46条 Lucide mapping ledger；
> Google Drive 品牌标仍 todo，不代表业务组件或品牌资产完成。

## 1. 已完成并打勾

- [x] T-UI-0073–0079；
- [ ] T-UI-0080 IconBrandGoogleDrive（继续 todo）；
- [x] T-UI-0081–0119。

UI `27/125/152 → 73/79/152`；全 parity
`463/1209/1672 → 509/1163/1672`。其余 ledger/fixtures 不变。

## 2. 新增机器判据

`cargo xtask design-lint` 现在把三份输入当一张关系表，不再靠文档散文声明“已核对”：

1. 解析 GUI 第一真源 §4.6.2 Markdown 两组列，得到47个 Tabler→target；
2. 解析 `icons.toml` 的46个 `label=parity` `upstream_tabler/name/upstream_usage`；
3. 解析 `parity/ui.yaml` 的47个 `icon-*` entry；
4. 46条 Lucide 逐条要求：文档名=manifest name、ledger target=生成 Rust enum variant、ledger
   首个上游 path 存在于 manifest usage、status=done、done_evidence 引用 exact join gate；
5. source zip SHA-256 声明必须同时出现在 GUI 第一真源和 manifest；既有 manifest/SVG 74/74、
   currentColor/1.75、安全 shape 与源码 allowlist 规则继续执行；
6. Google Drive 必须精确映射 `brand/google-drive.svg` / `brand::GoogleDrive`，但 brand manifest
   与 ledger status 都必须是 todo；谁擅自提前打勾，design-lint 当场红。

新增 parser 单测专门覆盖“一行两映射 + brand exception”，防只读左半表的静默漏项。

## 3. 为什么品牌标不打勾

`design/brand/google-drive.svg` 尚不存在；官方来源 URL、版本、使用条款和
`provenance/sources.spdx.json` 登记也未完成。第一真源 §4.6.3 要求官方 SVG、不改色、不描边、
不混入 Lucide allowlist。用 Lucide `hard-drive`、手绘四色三角或空占位都会把“文件存在”冒充
商标/许可完成，因此 T-UI-0080 保持唯一 icon todo。

同理，Google/Microsoft/Okta sign-in marks 属业务 provider-logo/品牌资产后续面，不由本批46条
Lucide join 代替。

## 4. 本机证据

| 面 | 结果 |
| --- | --- |
| design-lint mapping join | 46/46 Lucide done；Google Drive brand 1/1 todo；document/manifest/ledger exact |
| design-lint existing gates | 45 Rust；74 manifest/SVG；reverse/security rules clean |
| xtask bin tests | 78/0/0 |
| openbot-testkit xtask Clippy `-D warnings` | 绿 |
| parity/recount | 509/1163/1672；UI 73/79/152；0 violation/warning；157/157/0 |
| fixtures | 15/22/37（不变） |

Cargo.lock/package/production UI bundle delta=0。本批没有发行代码/资产变化，因此不运行 Trunk、
bundle budget 或浏览器；沿用这些命令的旧结果会是假“本批证据”，故明确不写。

宿主中途 ENOSPC；只清理可重建 `target-xtask` 1.3GiB 与
`target/debug/incremental` 3.2GiB，随后从当前源码重建 xtask 并重跑上述全部 gate。源码、主依赖
制品、release/WASM 与固定上游克隆未删。并行出现的未跟踪 `docs/assets/` 只读核查后未修改、
未暂存、未提交。

## 5. 仍未完成

- [ ] T-UI-0080 Google Drive 官方品牌标 + 条款/provenance；
- [ ] 45业务组件、6 runtime替代、27页golden；
- [ ] 31 routes 与正式 AX/键盘 journey；
- [ ] Tauri release identity/binary/window lifecycle 与 MPL/UNIC/Vet。

未运行 `cargo xtask ci`，未派发 Actions，未 push/建 PR，未处理 `grok-bot`。
